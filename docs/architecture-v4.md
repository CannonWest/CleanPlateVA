# CleanPlateVA V4 — shared design reference

**Public Full behind an acknowledgement · the barest boot on both data views · real per-view URLs.**

| | |
|---|---|
| Status | **Living design reference.** Governs three sequenced arcs — **CPR** (routes) → **CPD** (boot diet / Contract V4) → **CPF** (public Full). Each arc's runway briefing cites this doc by section instead of restating it. |
| Owner | Cannon decides; the active agent (lead dev) maintains. |
| Written | 2026-08-16, against the V3 snapshot `2026-08-16T15:13:33Z` (27,919 Full markers · 24,990 Lite). Every measured number below is stamped; treat them as dated receipts, not constants. |
| Update rule | When a runway milestone resolves an entry in **§12 Open decisions**, flip its status **in this doc, same PR**. When a principle changes, change it here first, then the briefings. Superseded content is struck or moved to **§13 History**, never silently deleted. |
| Runways | `c-ground-code/runway/archive/cleanplate-routes.md` (CPR, shipped 2026-08-16) · `archive/cleanplate-boot-diet.md` (CPD, **shipped 2026-08-17** — Contract V4 is live on both tiers) · `cleanplate-public-full.md` (CPF, **M0–M3 shipped 2026-08-17**; M4 observing, then D-TRANSPORT-2) · `archive/cleanplate-request-diet.md` (**CPH** — the request-budget arc, **shipped 2026-08-17/18**: hover from memory (zero requests), finder served static, `/data/*` off the Worker; curious session 99 → 20 Worker requests) · `archive/cleanplate-v4-closeout.md` (CPX — the verification net + residue tripwire, **shipped complete 2026-08-18**; the V4 program is closed). |

The shipped contract — **Contract V4 since CPD-M3, 2026-08-17** — is documented in [`README.md`](../README.md), which is authoritative for what is *live*. This doc is authoritative for the design and for where the site is still *going* (CPX: the close-out sweep; CPF-M4's D-TRANSPORT-2 decision) and why.

---

## 1. Why this doc exists

Three things Cannon wants are one design, not three:

1. **The public should get Full.** The only gate should be an acknowledgement — *these scores are CleanPlateVA's own derivation; the underlying records belong to VDH, the address points to VGIN, etc.* — not Cloudflare Access.
2. **Both data-bearing views should boot on the barest payload their job needs.** Map needs broad-and-thin (every marker, few fields). List needs narrow-and-deep (a page of rows, richer per row). Today both boot the same 33-request, 39.66 MB-raw payload.
3. **Map and List should be real URLs** that load different data, deep-link, and survive back/forward — today only `#about` touches the URL.

They interlock: the ack boundary decides *which* data family is gated, the data families decide *what each route loads*, and the routes are the lazy-load seams. Designing any one of them alone produces a shape the other two have to undo. So this doc fixes the shared shape once; the arcs execute it in order.

## 2. Principles (durable)

These are the invariants every V4 decision must satisfy. Several are inherited from V3 and restated here only because V4 work touches them; the full V3 invariant list lives in `README.md` and `c-ground-code/memory/project_clean_plate_va.md`.

| # | Principle | Consequence |
|---|---|---|
| **P1** | **The tier boundary is judgment, not secrecy.** Judgment-free facts (identity, location, source hand-off) are public without ceremony. Judgment-bearing content (derived scores and grades, and inspection detail *as presented through our scoring*) is public **after acknowledgement**. | The ack gates a *fetch decision in the client*, not a transport secret. Lite is "the finder without judgments"; Full is "the finder plus the judgment families." |
| **P2** | **The site never computes at request time.** Static assets and prepared objects only. Every interaction after boot is a local operation. | No query server, no search API, no server-side sort. Boot must therefore carry whatever statewide search/filter needs — which is why the roster is small-and-whole, not windowed. |
| **P3** | **Small mutable pointer, immutable content-addressed payload.** Manifests revalidate on a short TTL; shard filenames embed a hash of their bytes and are cached forever. | A shard's *name* changes iff its *bytes* change. Anything cache-forever must be content-addressed; anything addressed by a stable key (details, manifests) must be short-TTL + ETag. |
| **P4** | **Full → Lite fallback is the transition mechanism.** A client that meets a manifest or shard contract it does not understand degrades to the public finder. It never renders a broken page. | Contract cutovers are *choreographed* (§8) so the worst case during a flip is a brief gray map, and there is no dual-contract compatibility path (V3 invariant, kept). |
| **P5** | **Details stay nested per facility and carry no timestamp.** One inspection rewrites one object; opening a panel is one request; unchanged bytes stay unchanged. | The hover card and the click panel read the *same* object. Nothing else duplicates per-inspection content. |
| **P6** | **Lite is judgment-free forever.** The public finder ships no scores, grades, dates, or inspection content — and, post-CPF, the pre-acknowledgement view is exactly the Lite finder. | The finder family cannot grow a judgment field "for convenience." Judgments live only in overlay / closed / detail. |
| **P7** | **Presentation merge at publish; CouchDB stays one document per permit.** | Marker identity is a *published* concept; families are keyed by the published marker's `permit_id`. |
| **P8** | **`tenant` routes VDH links.** Never hardcode `/virginia/permit/`. | `tenant` stays in the finder row (needed by List's VDH column and the panel in both tiers). |
| **P9** | **Both tiers flip atomically on their own manifest; there is no distributed transaction between them.** | Cross-tier references (a Full family binding to a public shard generation) reintroduce skew windows — avoid them (§6, D-DATA-2). |

## 3. The tier boundary

| | **Shipped behaviour** |
|---|---|
| What separates the tiers | **Acknowledgement.** Full data is *public but conditioned*: the client fetches the judgment families only after the visitor has acknowledged the terms (`localStorage['cleanplateva.ack.v1']`); transport is public and edge-cached (`X-Cache`). |
| Lite is | The same finder family, rendered gray, without the judgment families. Also the explicit fallback (P4) and the `?tier=lite` dev override. |
| Full is | Finder + **overlay** at boot (the overlay carries the hover card whole, sparkline included — CPH); **closed** on toggle; **detail** on click only; standards on the first click. |
| The terms affordance | A first-load blocking dialog ("Agree and View Grades" / "Decline and Use Basic Map") over the empty basemap, fetching nothing until answered; a header control ("View Grades" until agreed, then "Terms") re-opens it. |
| Who sees judgments | Everyone who acknowledges. |

**Why the line sits here:** the ack is *about* the judgments — that's what the disclaimer text disclaims — and "judgment-free vs judgment-bearing" is precisely the line Lite already draws (P6). That is what let the tier split re-base onto the ack without being merged or redesigned, and what makes the finder common to both.

## 4. URL scheme (CPR)

**Ratified at CPR-M0 and shipped in CPR-M1b (2026-08-16)** — every D-URL-* row in §12 is resolved and live. `app/router.ts` (the pure scheme) and `app/useAppRouter.ts` (the hook) own the scheme below — the old client's `router.js` did until CRC, 2026-09-06; the site declares its mount with `<base href>` (`/` in production, rewritten to `/cleanplate/` by the CannonAI passthrough) and the router reads it from `document.baseURI`, so the same client works at any mount depth.

| Route | View | Notes |
|---|---|---|
| `/` | Map — **canonical** | `/map` accepted and normalized to `/` (D-URL-1). |
| `/list` | List | Same filter set as Map plus `sort`, `dir`, `page`. |
| `/about` | About | Legacy `#about` migrates on load. |
| unknown | → `/` | Static-asset `not_found_handling: "single-page-application"` serves `index.html`; the client `replaceState`-normalizes unknown paths (D-URL-2). Mirrored by the Vite dev server and the CannonAI mount. **The SPA setting is all-or-nothing** — measured on production 2026-08-16, it also answers a missing `/data/finder/*.json` (or a mistyped `.js`) with `200 text/html`, which would have `dataClient` parsing markup as JSON — ~~so `src/worker.js` re-404s asset-shaped paths (non-HTML extension in the last segment) that come back as the shell, where the worker runs (`run_worker_first`: `/data/*`)~~ — **since CPH-M3 (2026-08-18) the client keeps it honest instead:** `dataClient` treats the shell (`text/html`) on any data path as a miss — the full tier falls back to R2 for that shard, the basic map reports "no data published yet" — and `/data/*` is plain static (no Worker invocation; cache-control in `public/_headers`). Static assets stay on the assets fast path (a miss there is an immediate console MIME error; a per-file worker invocation would cost request quota). The CannonAI mount decides before serving, so it has no such split. |

**Query state (both views unless noted):** `q` (search — split on whitespace, **every term must match** and each may match a different field; per term, name/address/city substring **plus ZIP prefix**, so `richmond taco` and `23220 taco` both work. A `"quoted phrase"` is one term matched whole **within a single field**, never across the seam between fields. The ZIP select was retired 2026-08-18 and a legacy `?zip=` folds into `q` once, then clears) · `grade` · `restaurants` · `closed` · `new` · `mobile` · `permit` (selected facility → panel open) · List-only `sort`, `dir`, `page` · Map-only stretch: viewport as `#@lat,lon,z` (deferred; D-URL-3). With List paging decided as load-more (D-DATA-11), `page=N` means "N chunks of 50 revealed" — a load-more position stays deep-linkable; it resets when filters or sort change.

**Precedence:** URL wins when present; toggles keep persisting to `localStorage` as the default for URLs that omit them; the client writes only non-default state into the URL so shared links stay short (D-URL-4).

**"Non-default" is measured against what an omitted key falls back to** — which for the four toggles is the visitor's *persisted* value, not the shipped one. That is what makes the address bar reload-stable: `?closed=0` survives over a persisted `closed=1` (drop it and a refresh would silently flip the toggle back), while a toggle that already matches storage stays out of the URL. Toggle preferences remain the visitor's; the content state (place, filters, sort, page, selection) is what a shared link carries. The URL never writes `localStorage` — only the visitor's own clicks persist.

**History:** `pushState` on a view change and on selecting a facility, so Back returns to the previous view / closes the panel; `replaceState` for filter, sort, and page writes and for the load-time normalization, so filter churn never spams history. Inside the CannonAI frame everything is `replaceState` — the parent owns its history — and the frame's URL still tracks the view so a reload or a copied link lands where the visitor was.

**Deployment consequences**

- `wrangler.jsonc` `assets` gains `"not_found_handling": "single-page-application"`. **`run_worker_first` in array form is the complete set of paths that invoke the Worker** (Cloudflare: "define the routes that invoke your Worker explicitly"; it also opts out of the `Sec-Fetch-Mode` heuristic) — so it must list every channel the Worker owns: ~~`/data/*` and `/data-full/*`~~ **since CPH-M3 (2026-08-18) exactly one, `/data-full/*`** — the public channel `/data/*` is static (D-TRANSPORT-4; `public/_headers` carries its cache-control). The full channel is not a static asset; before the SPA fallback a miss fell through to the Worker on its own, and the fallback removed that path. Measured 2026-08-16 (post-#107): the un-Access-gated `www` host answered `/data-full/manifest.json` with the HTML shell instead of the Worker's 403, i.e. the authenticated tier was degrading to gray Lite; hotfixed by adding `/data-full/*` (PR #108) and pinned by `tests/worker-cache-contract.test.mjs` (which now pins the list to `["/data-full/*"]`). Side fact for CPF-M0/M3: the Access application is scoped to the apex host only — `www.cleanplateva.com/data-full/*` reaches the Worker unauthenticated, where the JWT check (defense-in-depth) is what returns 403.  <!-- retired-ok: the #108 hotfix account is a dated measurement of the Access era -->
- **CannonAI embed (shipped, D-URL-6):** `cleanplate_site` now falls back to `index.html` for extension-less paths and rewrites the served `<base href>` to `/cleanplate/` on the way out — so `/cleanplate/list` works on a hard reload without a second committed `index.html`, and the checkout's own file stays the deployable one. The Food tab's iframe carries the sub-route: it re-reads the frame's location (same-origin) so Reload and the tier toggle keep the visitor's view instead of snapping back to the map, and "open in a new tab" opens what is on screen.
- The `?tier=lite` override is orthogonal to routes and survives.

**Module split (decided 2026-08-16, Cannon; shipped CPR-M1a the same day):** `foodDashboard.js` was one 3,866-line class; its own section markers showed the seams. CPR-M1a split it into per-concern ES modules with **zero behavior change** — `constants` · `stacks` · `presentation` · `receipt` (pure) and `map` · `markers` · `hover` · `filters` · `list` · `about` · `detail` · `sparkline` · `inspection` (method bundles installed on `FoodDashboard.prototype`), with `foodDashboard.js` left as the ~370-line orchestrator (constructor, toolbar wiring, load/refresh, view switch) that re-exports the pure helpers. Method bodies moved verbatim; the 16 `node --test` suites (193 tests) and a dark-mode DOM/text fingerprint of Map / hover / panel / receipt / List / About in both tiers were byte-identical before and after. CPD's rewrites of the List, hover card, and presentation layer now land in small named files. Insurance, not a feature. <!-- retired-ok: a dated 2026-08-16 measurement under the runner CRC retired -->

## 5. View → data-need matrix

What each surface needs, which family carries it, and *when* it is fetched. This is the table the routes and the exporter both serve.

| Surface | Needs | Family | When |
|---|---|---|---|
| **Map boot** | coordinates · name/address/city/zip (search) · `tenant` · `is_restaurant` · `mobile` · approximate-location bit | **finder** | boot (both tiers) |
| | grade score (marker color, A–F chips) · `newly_permitted` · status | **overlay** | boot, **after ack** (Full) |
| **Map hover card** | grade + last-visit and last-broad dates + the NEW / no-grade hero text (scope, item count) — *instant* | overlay | already in memory |
| | trend sparkline | **overlay `visits`** (D-DATA-13 — compact `[kind, yyyymmdd, value…]`, one entry per inspection, §6.2; **live since CPH-M2, 2026-08-18**) | already in memory, **zero requests** (the per-hover detail prefetch of D-DATA-10 is retired — see §9 request budget) |
| **Map click panel** | full nested history + checklist decode | detail + standards | click (one detail per distinct facility per session — LRU; repeat clicks free); standards memoized on the first click |
| **List boot** | everything Map boot needs, plus sort/columns: compliance %, trend delta, latest date + scope + OUT ratio | finder + overlay | boot |
| **List page** | a page of the sorted, filtered rows | (in memory) | client-side slice; no fetch |
| **List row click** | detail | detail | click |
| **"Show closed" on** | the non-active permits (finder + overlay shape) | **closed** | lazy, first toggle-on (off by default → boot never pays) |
| **About live cards · footer freshness** | totals · by-grade · by-zip · snapshot time · **newest held report** | **manifest** | boot (`newest_report` moves into the manifest — today the footer scans every `latest.date` client-side, which the diet removes) |
| **Lite / pre-ack / `?tier=lite`** | finder only | finder | boot |

Map is *broad and thin*: every marker, ~10 fields. List is *narrow and deep only at the row you open*: its whole-population needs are the four thin sort keys, which ride the overlay; depth is per-row on click. That is why List does not need its own family or a server (see §10 rejects 4–5).

## 6. Data families — Contract V4

### 6.1 The families

| Family | Tier | Rows | Keyed / addressed by | Cache | Loaded |
|---|---|---|---|---|---|
| **`manifest.json`** (public) | Lite | — | stable name | `public, max-age=60, must-revalidate` | boot |
| **`manifest.json`** (Full) | Full | — | stable name; **atomic pointer, publishes last** | short TTL | boot |
| **`finder/<bucket>-<hash>.json`** ×16 | **both** | active permits, judgment-free identity/location rows | `permit_id` (sha256-bucketed, sorted) | immutable | boot |
| **`overlay/<bucket>-<hash>.json`** ×16 | Full | one row per finder row: status · grade score · new · compliance · trend delta · latest date/scope/OUT | **position-aligned to `finder/<bucket>`**, sha-bound in the manifest (D-DATA-5) | immutable | boot after ack |
| **`closed/<bucket>-<hash>.json`** ×N | Full | non-active permits, finder+overlay-shaped rows inline | `permit_id` | immutable | lazy on "Show closed" |
| **`standards.json`** | Full | checklist vocabulary | stable name | short TTL | first panel |
| **`facility/{permit_id}.json`** ×27,919 | Full | nested history — shape unchanged since V3; contract `cleanplateva.facility-detail.v4` since 2026-08-18 (D-DATA-8 closed) | `permit_id` | `max-age=300` + ETag | click (P5; nothing on hover since CPH-M2) |

**Manifest additions (both):** top-level `freshness: { snapshot_id, newest_report }` (D-DATA-9, resolved) and a `vocab` block for the row code tables (`permit_type` → `pt`, `loc`, `scope`). **Full manifest resources:** `finder`, `overlay`, `closed`, `standards`, `details.path_template` — and the publisher's inventory guard derives the only legal R2 object set from exactly this list, as it does today.

### 6.2 Row shapes (ratified CPD-M0, 2026-08-16 — measured by `cannon-food/tools/developer/cf_v4_prototype.py`)

**finder row** — an object (D-DATA-3: positional measured −7.5% wire, under the pre-registered ≥15% bar), coordinates rounded to **6 dp at export** (D-DATA-4; Couch keeps 9): `{permit_id, name, address, address2, city, zip, tenant, is_restaurant, mobile, pt, lat, lon, loc, ffx_oid}`. `tenant` is the VDH district path a facility's links render under (`virginia` for the aggregate) or — since FFX-M4, 2026-09-05 — the sentinel **`fairfax`** for the Fairfax Health District's facilities, which both clients route to the county's own records (`permitUrl`); it is the one place the finder distinguishes the two inspection programs. **`ffx_oid`** (the same day's deep-link follow-up, Cannon's call) is the county's layer `OBJECTID` — the id its ArcGIS Experience map selects a facility by (`#data_s=id:<source>:<oid>`): an integer on roster-joined Fairfax rows, `null` for a county facility known only from its reports (no roster feature, so no OBJECTID — one such today, in the closed tier), and **`null` on every VDH row**, so the row stays fixed-shape and the incremental materializer's key-by-key comparison stays honest; the clients build the deep link from it and fall back to the county's search page when it is null (a roster-less facility, a merged-in predecessor, or a row published before the key). It exists because the basic map's hand-off and the List's link column render from the row with no detail fetched. `pt` is a small-int **`permit_type` code** into a vocabulary the manifest publishes (D-DATA-6, Cannon: keep both booleans *and* add the code — `mobile` ≡ "Mobile Food Unit" exactly, but 689 of 6,725 non-restaurants are denied by name pattern alone, so `is_restaurant` can never be derived from the code); `loc` is a 4-class location code (0 rooftop-quality · 1 street-level `census_*` · 2 `zip_centroid` · 3 `venue_anchor`, added 2026-08-19 — see §13) whose vocabulary is **append-only**, a code being positional identity — the "approximate-location bit" generalized at zero cost so the panel's street-level note keeps working from the finder. Moved to detail (which already carries the full `location` dict): `precision`, `source`, `site_source`, `site_lat`/`site_lon`, `site_group_id`, `site_count` — same-coordinate stacks are computed client-side from coordinates (`stackKey`), and the shared-point notice depends on current effective coordinates, not historical site count. Measured: 276 B/row raw, **65.4 B/row wire**.

**overlay row** — positional, one per finder row, letter *derived* (`gradeForScore(score)`, never shipped), **nine fields = the union of what Map and List read at boot** (a Map-only / List-only split was measured at 10 KB apart and rejected — one family): `[grade_score|null, new (0/1), trend_delta|null, latest_yyyymmdd|null, base_yyyymmdd|null, latest_scope_code (0 unknown · 1 broad · 2 focused), latest_out|null, latest_items|null, compliance_pct|null]`. Map reads grade/new/delta (marker colour, NEW badge, the declining mark), latest + base dates and scope/items (the hover hero's "Last broad inspection · Last visit" boxes and its NEW / no-grade text); List reads grade/new/delta/latest/scope/out/items/compliance (score, trend arrow, event line, compliance column). **`trend_delta` semantics amended CRP-M1b (2026-08-31, Cannon):** GRADE-to-grade — the current adjusted facility grade minus the PREVIOUS era's (the engine re-run with the newest scored broad + its follow-ups removed, i.e. what the headline read before the current base landed) — replacing the original raw broad-to-broad `score_trend` difference; and "declining" is BANDED, strictly more than 5 points of drop (`TREND_DECLINE_BAND`, one definition in the exporter mirrored by both clients — a −5 exactly ships but does not flag). Wire shape unchanged (`trend_delta|null`); payloads published before the republish carry the old semantic until they age out. `latest_items` is `addressed_item_count` for a focused visit and `applicable_item_count` for a broad one (the client's `count`). **No `status_code`**: the finder is active-only, so every overlay row's status was measured to be literally `Permitted`; status rides on closed rows. ~~**No series at boot**: the hover sparkline comes from the detail prefetched on hover (D-DATA-10) — shipping the V3 trend tuples would have added 0.53 MB, a broad-only score series 0.09 MB.~~ (CPD-M0's trade, history since CPH — the series is back as `visits`, below.) **Reopened 2026-08-17 (CPH, D-DATA-13):** that trade weighed bytes and ignored *request count* — on Workers Free every hover-fetch is a metered request, and a curious mouse spends more requests than the boot. CPH adds one compact `visits` column so the sparkline is in memory again and hover costs zero requests — **live end to end since CPH-M2 (2026-08-18): the exporter writes it (M1), the client draws from it, `prefetchDetail` is deleted.** **Ratified CPH-M0 (2026-08-17, Cannon): variant (b), the last column, oldest-first, one entry per inspection = one x-slot** — `[1, d, score]` broad with a published score · `[1, d]` broad, no score (an x-slot, no mark) · `[2, d, out, addressed]` focused, ratio known · `[2, d]` focused, ratio unknown (tick) · `[3, d, code]` adjudicated verdict (1 all_corrected · 2 priority_corrected · 3 none_corrected) · `[3, d, 4, ins, outs]` adjudicated `items` verdict · `[0, d]` nothing claimable (tick); `d` = yyyymmdd (the overlay's date vocabulary); kinds share the scope code space (0 unknown · 1 broad · 2 focused) plus 3. **Measured +382 KB wire** on the 24,990-row overlay (+34 KB on closed) — 102 B/row raw · 24.4 B/row wire with the column vs 42 · 9.1 without; the V3 tuples would have been +538 KB, day-delta dates +322 KB, broad scores only +88 KB. Cross-checked 27,919 / 27,919 against the real renderer (`tools/visits-crosscheck.mjs`; receipt: `cannon-food/references/retros/cph-m0-request-diet.md`). Nothing the click panel adds rides here (P5). The shard envelope carries `finder_sha256` of the finder shard it aligns to (D-DATA-5). Measured without `visits`: 43 B/row raw, **8.6 B/row wire**.  <!-- retired-ok: CPD-M0's struck trade, kept because why it changed is the point -->

**closed row** — the finder object fields + `status` + `o: [overlay row]` inline (never shared with Lite, so no split needed). Measured 336 B/row raw, 87 B/row wire; 2,929 rows.

**Manifest additions (both tiers):** top-level `freshness: {snapshot_id, newest_report}` (D-DATA-9) and a `vocab` block naming the code tables the rows use (`permit_type` for `pt`, `loc`, `scope`).

**Found while measuring, fixed in CPD-M1:** the V3 roster projection publishes `compliance_rate` / `checklist_out` / `checklist_compliant` / `cos` as `null` on 100% of markers — every inspection doc carries those keys top-level as `null`, and `_shape_marker_inspection` uses `insp.get(k, cs.get(…))`, whose default never fires for a present-but-null key — while `checklist_summary` carries them on ~88K of 97K inspections. The detail reads `checklist_summary` directly and is unaffected. M1 prefers the non-null value, pinned by a test; the List's Compliance column lights up for the first time (under V3 too, from the next publish).

**Sharding is unchanged:** `bucket = int.from_bytes(sha256(permit_id)[:4], "big") % 16`, rows sorted by `permit_id`, filename `<bucket>-<sha256(bytes)[:12]>.json`. Even split (V3: 1,679–1,810 rows/bucket) with no rebalancing logic; a permit's bucket never moves.

### 6.3 Shared finder — the two ways to share

The finder is *one contract, one exporter path, one tripwire test* for both tiers. Whether Full literally fetches the *public URLs* is a separate choice (D-DATA-2):

- **Same URL** — Full manifest points at `/data/finder/*`. Saves R2 storage (14 MB — trivial) and lets a Full visitor's cache reuse public shards, but binds a Full family to a public generation across two transports → every daily publish opens a skew window (git deploy vs R2 flip) in which a position-keyed overlay misaligns and the client must degrade to gray. Violates P9.
- **Same bytes, republished under `/data-full/`** — Full manifest names the same content-addressed shard files, uploaded to R2 alongside the overlay. Each tier is atomic on its own manifest; no cross-transport binding; no daily skew. **Recommended.** Post-CPF, `/data-full/*` is public and cached too, so the "reuse" advantage of same-URL mostly disappears anyway.

## 7. Boot budget — measured (CPD-M0) and live (CPD-M3)

**Measured 2026-08-16 by `cannon-food/tools/developer/cf_v4_prototype.py`** against the live archive (28,561 facility docs → 27,919 markers → 24,990 active + 2,929 closed; snapshot of the same day) with the *ratified* V4 row shapes of §6.2, and against the V3 materialized view (`public/data-full/`) + committed Lite shards **with the same compressor** so the columns are comparable. Units: **MB = 10⁶ bytes throughout** (the earlier version of this table mixed MB and MiB — 39.66 was MiB; the same bytes are 41.59 MB). gzip = level 6; wire = **brotli q4**, calibrated the same day against the live edge (cleanplateva.com served br at 0.91 × gzip-6 on the V3 finder shards; q4 reproduces 0.90 locally). Modern browsers receive brotli.

| Payload | requests | raw | gzip-6 | **wire (br q4)** | vs V3 |
|---|---|---|---|---|---|
| **V4 Lite boot** (manifest + 16 finder) | 17 | 6.90 MB | 1.77 MB | **1.64 MB** | **74%** |
| **V4 Full boot** (manifest + 16 finder + 16 overlay) | 33 | 7.99 MB | 1.95 MB | **1.85 MB** | **28%** |
| · V4 finder — both tiers (276 B/row raw · 65.4 B wire) | 16 | 6.89 MB | 1.76 MB | 1.63 MB | |
| · V4 overlay — Full (43 B/row raw · 8.6 B wire) | 16 | 1.08 MB | 0.19 MB | 0.21 MB | |
| V4 closed supplement — lazy, on first "Show closed" (2,929 rows) | 16 | 0.98 MB | 0.25 MB | 0.26 MB | new |
| **V4 overlay + `visits` (CPH, D-DATA-13; measured 2026-08-17 on the same-size archive)** — the hover series back at boot | 16 | 2.56 MB | 0.57 MB | **0.61 MB** (+0.38) | Full boot → **2.25 MB**; V3 was 6.71 |
| V4 manifests (public · Full) | 1 · 1 | ~0.01 MB each | | ~0.005 MB | |
| Detail corpus — unchanged under V4 (D-DATA-8); n=1000 sample | 27,919 | 425.8 MB on disk · p50 12.7 KB · p90 27.8 KB | | **p50 2.9 KB · p90 5.9 KB** | |

The **requests** column counts *fetches*, not Worker invocations: since CPH-M3 the 16 finder shards and the Lite manifest are static assets, so a Full boot's 33 fetches are 17 Worker requests + 16 static, and a declined boot invokes the Worker 0 times (§9). The `vs V3` column is against the V3 boot this replaced — 2.21 MB wire Lite and 6.71 MB Full, of which the retired `signals/*` family alone was 4.17 MB. The full V3 breakdown is in §13 (2026-08-18).

The design's targets (~1.6 / ~2.0 MB) were met: Lite lands on target, Full beats it. **Live since 2026-08-17 03:50Z** — the cutover publish rebuilt these families from the same archive (Lite 24,990 rows; Full 24,990 + 2,929 closed) and R2 holds exactly the manifest-derived V4 inventory (second dry-run: 0 uploads). Retiring signals removes 4.17 MB of wire (62% of the V3 Full boot); the overlay adds back 0.21 MB. Alternatives measured and not taken (all in the prototype's report): positional finder rows −7.5% wire (−42% raw); a Map-only / List-only overlay split −10 KB on the Map boot; the V3 trend tuples at boot +0.53 MB (broad-score series only +0.09 MB); GUID-keyed overlay +0.50 MB, 8-hex-hash-keyed +0.11 MB; closed as one file −0.05 MB; 9-dp coordinates +0.08 MB; a code-only filter field ±0.02 MB.

**A cost that does not go away:** grades change on every publish and permits scatter across buckets by hash, so all 16 overlay (and most finder) shard names rotate essentially daily — (1 − (15/16)^100) ≈ 99.8% per bucket at 100 changed permits. `immutable` makes caching *correct*, not *warm*; a daily visitor re-pays most of the boot each day. Proportional to the slim payload, so acceptable; just never describe the shards as "cached across days."

## 8. Cutover mechanics (CPD-M3)

The choreography that makes P4 the safety net rather than a broken page:

1. **The publisher runs the site repo's committed Lite contract test as a gate and refuses non-clean / non-`main` / unsynced checkouts.** So the V4 client, the V4 Lite tripwire test, *and* the V4 Lite data (materialized locally by the V4 exporter) must land in **one site PR**. Merge deploys client + Lite data atomically through Workers Builds. → D-CUTOVER-1: same-PR, not a transitional dual-accept.
2. **Then** run `cf_publish_cleanplate.py` for R2. Between (1) and (2), the new client meets the V3 Full manifest → rejects it → renders V4 Lite (gray). Never broken. When the R2 manifest flips, the next load is Full V4.
3. The publisher must recognize "Lite already at this generation" instead of producing a second data-only commit (D-CUTOVER-2 — idempotence on shard shas).
4. After the flip, the inventory guard treats `signals/*` as stray: retained for the one-generation grace the V3 publisher already implements, then pruned. `public/data-full/` (the persistent local materialized view) drops the family the same way.
5. The reverse order (R2 first) also degrades to gray rather than breaking (old client rejects V4 Full → V3 Lite), but the publisher's own gates make it unreachable — the site's committed test would still be V3 and would fail V4 Lite output. **Site PR first is therefore the only sequence.**

## 9. Transport and caching in the public-Full end state (CPF)

- **Worker:** drop the `Cf-Access-Jwt-Assertion` check; `/data-full/*` cache-control flips `private` → `public` with the same TTL shape (manifest 60 s revalidate · content-addressed shards `immutable` · details `max-age=300` + ETag). The site's `worker-cache-contract.test.mjs` is updated in the same PR. **Shipped CPF-M2 2026-08-17** — plus the Cache API (`caches.default`, `X-Cache: HIT`/`MISS`; a HEAD is answered from the GET entry and never stores) and R2 conditional reads (`get(key, { onlyIf: request.headers })` → 304 with no body when the visitor's ETag still matches). The `signin` route is gone. **Side effect worth knowing:** the Access application is apex-scoped, so from M2 the `www` host serves the full channel publicly a step ahead of M3; the apex still 302s to Access until Cannon retires the app.  <!-- retired-ok: §8 records the cutover choreography as it ran -->
- **Edge caching:** Worker responses are **not** CDN-cached by default. Two options (D-TRANSPORT-1): the Worker uses the Cache API (`caches.default`) for GET hits — smallest change, one origin, no CORS; or Full data moves to an **R2 public custom domain** (e.g. `data.cleanplateva.com`) with Cloudflare cache in front and no Worker in the data path. **Plan tier matters:** Workers Free caps at 100K requests/day and Cache API hits still invoke the Worker; the R2 custom domain does not. **Established CPF-M0 (2026-08-17): the account is on Workers Free** (100K requests/day, reset 00:00 UTC, over-limit → Error 1027; cache hits count as requests; static-asset requests are free — ~~but `/data/*` is `run_worker_first`, so Lite boots invoke the Worker too~~ which CPH-M3 made true for the basic map: `/data/*` is static since 2026-08-18). The bucket `cleanplateva-data` has no custom domain and its r2.dev URL is disabled. Client request pattern is Full-first: `data-full/manifest.json` + 16 finder + 16 overlay = **33** Worker requests per acked boot, Lite = **17**, details 1 per hover — ≈40–100 per session, so Free carries roughly **1,000–2,500 visitor-sessions/day** before 1027 (beyond it both channels fail and the map goes empty; the shell keeps serving). **Decision (Cannon): Cache API in the Worker now; M4 observes requests/day; escalate to Workers Paid ($5/mo, no cap) or the R2 custom domain only if volume argues.** Under the chosen D-ACK-1 (no fetch until the visitor answers) nobody pays Lite + Full on the same visit.
- **Access app:** retired only after client and Worker no longer depend on it (D-TRANSPORT-3). Cannon fires the dashboard/connector action; the agent verifies nothing 403s afterward.
- **The git Lite channel** (`public/data/`) stays through CPF for defense-in-depth availability. Whether it is retired afterward — making R2 the sole data transport and ending the daily "Data refresh" commits + redeploys — is D-TRANSPORT-2, deliberately *not* a CPF precondition. **After CPH-M3 the recommendation flips to keep:** the static channel becomes the *cheap* transport for the shared finder (D-TRANSPORT-4).
- **Request budget (CPH, 2026-08-17).** On Workers Free the metered unit is the *request* — cache hits count, static-asset requests served without the Worker do not. Per-session Worker requests today: basic map / decline **17** (`/data/*` is worker-first), acked boot **33** (manifest + 16 finder + 16 overlay), plus **1 per distinct hovered marker** (detail prefetch) and 1 per click — a curious mouse spends more than the boot. Three levers, all in CPH: (1) **hover to zero** — the compact `visits` column in the overlay (D-DATA-13) retires the detail prefetch; click still fetches the detail (P5). **Shipped CPH-M1 (column live, 2026-08-18) + CPH-M2 (client, 2026-08-18): measured on production after deploy — acked boot 33 · 80 distinct hovers +0 (0 late) · first click +2 (detail + `standards.json`, which now rides with the first click) · each further new click +1; the curious-mouse session that cost 99 costs 37.** (2) **finder from static** (D-TRANSPORT-4) — the finder bytes are identical in git and R2 and content-addressed, so the Full client fetches `finder/<bucket>-<sha>.json` from `/data/` first (static, edge-cached, free and unlimited when the Worker does not run) and falls back to `/data-full/` only if that exact filename is absent; same name = same bytes, so a daily-publish skew can never serve wrong data. `/data/*` leaves `run_worker_first`; its cache-control moves to `public/_headers`. Result: basic map / decline **0** Worker requests, acked boot **17**. **Shipped CPH-M3 (2026-08-18, CleanPlateVA #126 + #127, cground-skills #1684): measured on production after deploy — decline 0 Worker requests (17 static) · acked 17 Worker (`/data-full/` manifest + 16 overlay) + 16 static finder · 80 hovers +0 · first click +2, then +1 — the curious session that cost 99 Worker requests when the arc opened costs 20.** `_headers` lists the finder shards **by name** (the publisher rewrites and commits it with every Lite publish): measured, a `/data/finder/*` splat rule also stamped `immutable` on the SPA shell for a name the tree lacks (edge-cached), whereas an unlisted name gets the safe `max-age=0, must-revalidate`; a missing `/data/finder/<name>` now returns the shell with that default — the 404→200 flip that proves the Worker no longer sees `/data/*`. (3) **fail open** (D-TRANSPORT-5) — with Lite static and the `/data-full/*` route set to fail open, hitting the daily cap degrades the site to the basic map instead of a 1027 page until 00:00 UTC. **Recorded CPH-M3: the dashboard offers "Request limit failure mode" (fail closed by default · fail open = "bypass your Worker and proceed to your origin") only when adding a zone Route; the custom-domain edit dialog has no such setting and the zone has no routes — so it is not available to `cleanplateva`, and it is not needed: the client's Full → Lite fallback (measured on HTML-200 / 429 / JSON-404 answers) plus a static basic map is the mechanism.** Steady state after CPH: ~17 + clicks per session (≈5,000 sessions/day on Free vs ≈1,000–1,200 when the arc opened). Independently, **D-PLAN-1**: Workers Paid ($5/mo, 10M requests/month, no daily cliff) before the link is shared anywhere — the diet makes the site cheap; the plan makes it safe. Cannon's call — **resolved CPH-M0: stay on Free and observe** (§12).
- **Measured before CPH (CPH-M0, production, fresh visitor, 2026-08-17):** dialog before answering **0** · decline **17** (`/data/manifest.json` + 16 finder, all worker-first) · agree **33** (`/data-full/` manifest + 16 finder + 16 overlay) · agree + 20 distinct hovers **54** (20 details + `standards.json`, which today rides with the *first detail*, not the first panel) · + 3 clicks on hovered markers **+0**, on new ones **+3** · a curious mouse over one Richmond viewport (65 distinct hovers, 3 clicks) **99** · `/list` cold 33, first row click +2 · `/about` cold 33 (every view boots the whole roster). A warm browser boots on ~1 network request (immutable shards) but names rotate daily, so the fresh number is the honest one. Only one Worker exists on the account (`cleanplateva`), so the 100K/day pool is this site's alone.
- **Transport facts (CPH-M0):** Workers static-asset `_headers` rules *"are not applied to responses generated by your Worker code, even if the request URL matches a rule"* — the file takes effect only once `/data/*` leaves `run_worker_first` (until CPH-M3 `src/worker.js` set those two values itself; since 2026-08-18 the file does); rules ≤100, override the assets default (`public, max-age=0, must-revalidate`); whether the SPA-fallback response for a matching path receives a rule was undocumented — **measured CPH-M3: it does** (a `/data/finder/*` splat stamped `immutable` on the shell for a missing name, edge-cached), hence the by-name rules. Measured on the host: a missing `/data/finder/*` is today the Worker's **404 text/plain**; a missing asset-shaped path outside worker-first is the SPA shell, **200 text/html** — what a static `/data/finder/<missing>` will return, so the client treats an HTML answer on a shard path as a miss, and the 404→200 flip on `/data/finder/zz-000000000000.json` is M3's proof the Worker no longer sees `/data/*`. **Fail mode:** the Free cap (100K/day, 00:00 UTC, Error 1027) fails open (*"bypasses the Worker; requests behave as if no Worker is configured"*) or closed (1027 page) per **route**; `cleanplateva` runs on **custom domains**, where the toggle is undocumented. Ran the client (CPH-M0): a Full channel answering HTML-200, a 429/1027 page, or a JSON 404 all degrade to the basic map — so once Lite is static the site survives the cap under *either* mode; fail-open is belt-and-braces where offered (D-TRANSPORT-5). The real cap event is the only end-to-end proof. **Never enable the opt-in "Workers Caching" (`"cache": {"enabled": true}`) on this Worker** — distinct from `caches.default`, it *"bills requests that are normally free: static asset requests"* and would undo D-TRANSPORT-4.

## 10. Considered and rejected

Written down so no future session re-litigates them without new evidence.

1. **Geo-tiled sharding for first paint.** Statewide name/address search at boot needs the whole roster; geo-tiles would force a second identity copy (a search index) → more total bytes for a faster first frame. Not worth it at ~2 MB. Revisit only if the finder cannot get under ~3 MB wire.
2. **Committing Full to git / static assets.** 27,919 detail objects (406 MB) that churn daily is the wrong channel for git; it also exceeds the Workers Free static-asset cap of 20,000 files (100,000 on Paid, since 2025-09). R2 stays the detail transport.
3. ~~**A separate "hover stub" family.** The detail already carries what hover needs at ~4 KB wire p50; a stub family adds a publisher family, inventory rules, and change-set mapping to save ~15 bytes per visit. Prefetch the detail instead; the click becomes free.~~ **Reopened 2026-08-17 (CPH) — this rejection was wrong on the metric that matters.** It weighed bytes and publisher complexity and never weighed *request count*; on Workers Free (public since CPF) a per-hover fetch is a metered request, and mousing over 50 markers costs more than the boot. Superseded by D-DATA-13: a compact `visits` column *inside the existing overlay* (no new family, no new inventory rule, no new change-set mapping — the overlay already re-materializes on inspection changes), hover from memory, detail only on click.
4. **Pre-sorted index families for List.** The sort keys are thin enough to ride the overlay; the client sorts ~25K numerics in ~10 ms. Only if List gains a sort key that cannot fit the overlay.
5. **A server-side search/query API.** Violates P2 and requires a backend to exist, scale, and be secured. The whole point of the boot payload is that no such server exists.
6. **Shipping the grade letter.** It is `gradeForScore(score)`; ship the score only. (Inspections have scores; facilities have grades — no per-inspection letters, ever.)
7. **Same-URL shared finder** — see §6.3; rejected on P9 (daily skew window).
8. **Bigger page caps instead of pagination.** Today List sorts everything and shows the first 600. Client-side pagination over the in-memory sorted array is strictly better and needs no data change.

## 11. The arcs and their gates

| Arc | Briefing | Lands | Gate |
|---|---|---|---|
| **CPR** — routes ✅ **complete 2026-08-16** | `archive/cleanplate-routes.md` | M0 ratify §4 + adopt §5 ✅ · M1a module split ✅ (PR #104) · M1b routes, URL state, load-more paging, SPA + embed fallbacks ✅ | none — fired first |
| **CPD** — boot diet / Contract V4 ✅ **complete 2026-08-17** | `archive/cleanplate-boot-diet.md` | M0 measure + design ✅ (`cf_v4_prototype.py`; §7 measured; D-DATA-1..9, -12, D-CUTOVER-3 resolved) · M1 cannon-food emits V4 flag-gated ✅ (cground-skills #1667) · M2 site V4 client ✅ built + held (CleanPlateVA #111) · **M3 cutover ✅ 2026-08-17 03:50Z** — cannon-food V3 retirement (#1668) → #111 merged (client + Lite data deployed ~30 s) → `cf_publish_cleanplate.py --full --run-id cpd-m3-cutover --apply` (dry-run reviewed: 48 shards + 26,999 details + manifest, delete 0 → 27,047 uploads, 0 deletes, 643 s) → verified (R2 `full-manifest.v4` 27,919 = 24,990 + 2,929; second dry-run 0 uploads / 32 deletes = the retained V3 generation; public manifest V4) → D-CUTOVER-2 landed (#1669) · M4 stretch not needed | **shipped** — CPF ungated |
| **CPF** — public Full ✅ **complete 2026-08-18** | `archive/cleanplate-public-full.md` | **M0 ✅ 2026-08-17** — attribution inventory (10 upstreams, verified against `cannon_food/location/*` + upstream policy pages; provenance of the 27,919 published pins: VGIN 92.5% · OSM/Nominatim 3.6% · Census 2.2% · ZIP-centroid 0.8% · Overture 0.6% · Foursquare 0.3% · manual 0.1%), plan tier (Free), the Access app inspected (`CleanPlateVA full data`, `c9a0d19f-4935-4344-b1e5-bd7a0a16d30e`, apex-scoped, one email policy), D-ACK-1..3 + D-TRANSPORT-1 resolved, the copy approved by Cannon and the About §06 "Terms & attribution" section landed **hidden** (`d-none`, M1 unhides) · **M1 ✅ 2026-08-17** — `ack.js`: blocking first-load dialog over the empty basemap (no fetch until answered), body cloned from `#aboutTermsBody`, Agree → Full / Decline → basic map, versioned `cleanplateva.ack.v1`, header control "View Grades"/"Terms" replaces `#signInBtn`, §06 shown, §04/§05/live card re-based, footer link, `?tier=lite` bypass; 11 new tests (219 green); QA'd dark/light/phone on a throwaway port; embed acks once per origin — no CannonAI change · **M2 ✅ 2026-08-17** — `src/worker.js` public channel: JWT check + `signin` route removed, `private` → `public` cache-control (same TTL shape), Cache API with `X-Cache`, R2 `onlyIf` → 304; `worker-cache-contract.test.mjs` re-pinned (14 tests; 225 green); README transport lines; measured on the host after deploy (see §13) · **M3 ✅ 2026-08-17** — Cannon deleted `CleanPlateVA full data` (`c9a0d19f-…`) in the dashboard; `list_access_apps` shows only CannonGate / CannonAI / launcher; anonymous apex measured: `/data-full/manifest.json` 200 public (no redirect, `.well-known/cloudflare-access-protected-resource/…` 404), shards immutable, details 300 s + ETag → 304, `X-Cache` MISS→HIT; browser: dialog → Agree → 33 Full requests, 24,288 graded, hover detail fetches, Decline → 17 Lite / gray; hotfix #116 (HIT `Cache-Control` re-asserted — the zone's Browser TTL rewrote `match()`'s header to 14400); README/design ref/memory re-based · **M4 ✅ closed early 2026-08-18** — its one deliverable was deciding D-TRANSPORT-2, which CPH-M3 recorded as **keep** once the static channel became the cheap transport for the shared finder; no new briefing was spawned. The residual watch on requests/day vs the Free cap is D-PLAN-1's standing "re-open on evidence", not an open milestone | **M0–M3 shipped 2026-08-17; CPX ungated** <!-- retired-ok: the CPF arc row names what CPF retired --> |
| **CPH** — request diet ✅ **complete 2026-08-18** | `archive/cleanplate-request-diet.md` | **M0 ✅ 2026-08-17** — `visits` variants measured (`cf_v4_prototype.py --visits-only`: compact +382 KB wire; V3 tuples +538; day-delta +322; scores-only +88), cross-checked 27,919/27,919 against the renderer (`tools/visits-crosscheck.mjs`), production Worker requests counted (decline 17 · acked 33 · +20 hovers 54 · curious mouse 99 · List/About 33), `_headers` / SPA-fallback / fail-mode facts established (§9), D-DATA-13 (b) · D-TRANSPORT-4 do it · D-TRANSPORT-5 fail open if offered (not load-bearing) · D-PLAN-1 Free, observe (Cannon) resolved; retro `cannon-food/references/retros/cph-m0-request-diet.md` · **M1 ✅ 2026-08-18** — cannon-food emits `visits` (cground-skills #1681: `_visit_event` / `_visits` / `_rollup_marker` / `_shape_overlay_v4`, publisher guard, 849 tests); materialized 700 s, overlay 0.61 MB br (24.4 B/row) — CPH-M0's number to the decimal — `visits-crosscheck.mjs --view` 27,919/27,919; **published** `cf_publish_cleanplate.py --full --run-id cph-m1 --apply` (dry-run reviewed: 32 uploads = 16 overlay + 16 closed, manifest last, 32 deletes = the retained V3 finder + signals generation, 0 detail uploads, skip 27,936 → 23 s, 0 failed; Lite stamp-only, nothing committed — D-CUTOVER-2); live snapshot `2026-08-18T17:44:14Z`; **the deployed 9-column client booted on the 10-column shards** — 33 requests, 24,990 rows all carrying `o.visits`, hover/detail unchanged · **M2 ✅ 2026-08-18** — CleanPlateVA #124: `presentation.js` `visitsOf(f)` (visits → the series shape `buildScopeSeries` builds; one `_sparkSvg` for hover and panel), `sparkline.js` `sparkMarkup` / `_sparklineFromSeries`, `hover.js` renders the card whole from the row, `dataClient.prefetchDetail` deleted (standards now load with the first click); tests pin the card's plot byte-identical to the panel's (every mark kind) — 228 green; local QA 20 hovers 0 requests; **production after deploy: boot 33 · 80 hovers +0 · click +2 · curious session 99 → 37** · **M3 ✅ 2026-08-18** — CleanPlateVA #126 (`public/_headers`, `run_worker_first: ["/data-full/*"]`, Worker sheds `publicDataCacheControl` / `looksLikeAsset` / `isSpaFallback`, `dataClient` static-first finder + R2 fallback, HTML-200 on a data path = miss; 228 tests) + #127 (`_headers` by shard name after the splat stamped `immutable` on the fallback) + cground-skills #1684 (the publisher writes and commits `_headers` from the Lite manifest; 850 tests); host measured (`_headers` values, no `X-Cache` on `/data/*`, missing shard → shell with `max-age=0`); production fresh visitor decline **0** Worker · acked **17** · hover 0 · click +2 → curious session 99 → 20; D-TRANSPORT-2 recorded (keep), D-TRANSPORT-5 recorded (not offered on custom domains — checked in the dashboard; the client-side fallback is the mechanism) | **shipped 2026-08-18 — CPX ungated** <!-- retired-ok: the CPH arc row names what CPH retired --> |
| **CPX** — V4 close-out sweep | `cleanplate-v4-closeout.md` | **M0 ✅ 2026-08-18** — 87 patterns from §14.1 run through `git grep` across cannon-food, clean-plate-va, c-ground-code and the CannonAI Food tab (818 raw → 351 exempted → 467 live → 42 word-stem noise); classified **20 rewrite · 2 delete · ~95 keep-as-history · ~310 do-not-flag**; retro `cannon-food/references/retros/cpx-inventory-2026-08-18.md` (cground-skills #1686), D-CLOSEOUT-1..4 registered (#129); gate verified on the host first. Five findings surfaced not absorbed — **F1: `cannon_food/location/candidates.py:42` still pins `full-manifest.v3` and raises on anything else, so the location-candidates bundler hard-fails against every export published since CPD-M3 (own PR)**; F2 CPF-M1 left the retired "Detecting data tier" placeholder in `index.html:166`; F3 §14.1's "c-ground-code mcps" owner has no target; F4 → D-CLOSEOUT-4; F5 a term-grep cannot catch stale framing · **M1 ✅ 2026-08-18** — four PRs (CleanPlateVA #133, cground-skills #1695, CannonAI #332, and the runway close #1696) clearing 22 file/term pairs: this doc collapsed into present tense (§3's before/after columns, §6.1's "V3 predecessor" column and struck `signals/*` row, §7's V3 control rows, §15's four dead symbols/tests — all to §13), two live docstrings stopped calling `/data-full/*` Access-gated, `index.html` stopped shipping "Detecting data tier" on first paint, and two dead `--changed-permits` warnings went. Three M0 rows withdrawn as dated milestone logs. The by-hand passes found what a term-grep structurally cannot: `MEMORY.md` still said "both V3 tiers" (an exempt path) and CannonAI's Lite tooltip still said "What anonymous visitors see" with no §14.1 term in it (F5). `retired-ok` markers deferred to M2, where D-CLOSEOUT-2 fixes their syntax · **M3 ✅ 2026-08-18** — CPF archived with its HISTORY line and INDEX row cleared (CPR/CPD/CPH already were); §11 marks every arc complete; §13 closed. CPX's own briefing stays live until M2 lands, then archives as M2's last act — that is what turns the tripwire green · **M2 ✅ 2026-08-18** — the residue tripwire (cground-skills #1699 `tests/test_retired_vocabulary.py`, Tier-0; CleanPlateVA #135 `tests/retired-vocabulary.test.mjs`; #136 §14.1 says which test owns which rows): per-repo term lists with a reason per term, history/archives/memory/retros exempt, `retired-ok` escape, a self-test that plants a term and proves the marker suppresses it; first green run on `main` = M1's completion proof; verified to fire on a planted term in a live file in both repos (post-ship review) | ✅ **complete 2026-08-18** — M0 → M1 → M3 → M2 (D-CLOSEOUT-4); the tripwires are green on `main` in both repos, the briefing is archived, the V4 program is closed. Was gated on CPF-M3 ✅ and CPH-M3 ✅ (Cannon 2026-08-17: CPH ran before CPX so the sweep saw the final shape) |

CPR and CPD both edit `foodDashboard.js` (or its split successors) — **sequential is mandatory**. The other gates exist so nothing is designed against a shape that is about to change. Each arc: one PR per milestone, pause between, no auto-chain.

**CPX is a net, not a deferral target.** CPD-M3 and CPF-M3 each carry their own README / SKILL.md / memory steps and each *appends what it retires to §14.1*. CPX exists for the files those arcs never opened — prose that described the old world in places nobody edited — and to make the class mechanical afterward.

## 12. Open decisions registry

Every fork the runways inherit. **Resolver** = the milestone that must close it; **Status** flips in this doc, same PR, when it does. Recommendations are the lead dev's; Cannon decides (via `AskUserQuestion` at the resolving milestone).

| ID | Question | Options | Recommendation | Resolver | Status |
|---|---|---|---|---|---|
| **D-URL-1** | Which route is canonical for Map? | `/` canonical, `/map` alias → normalize · `/map` canonical, `/` redirects | `/` canonical | CPR-M0 | **shipped CPR-M1b 2026-08-16** — `/` canonical, `/map` normalizes |
| **D-URL-2** | Unknown path behavior | SPA fallback + client normalizes to `/` · real 404 page | SPA fallback + normalize | CPR-M0 | **shipped CPR-M1b 2026-08-16** — wrangler + `app.py` + the CannonAI mount; missing assets still 404 |
| **D-URL-3** | Which state rides in the URL | filters (both) + `permit` + List `sort/dir/page` · + Map viewport hash | all of the first; viewport as stretch | CPR-M0 | **shipped CPR-M1b 2026-08-16** — filters + `permit` + List `sort/dir/page`; **Map viewport hash deferred** (still open as a stretch) |
| **D-URL-4** | URL vs `localStorage` precedence for toggles | URL wins when present, else stored default; write only non-default state · URL always authoritative | URL-wins-when-present | CPR-M1 | **shipped CPR-M1b 2026-08-16** — non-default is measured against the *persisted* value, which is what makes it reload-stable (§4) |
| **D-URL-5** | Split `foodDashboard.js` into modules in CPR? | yes, zero-behavior-change per-concern split · leave to CPD | **decided: yes-light** | CPR-M1 | **shipped CPR-M1a 2026-08-16** (decided the same day, Cannon) — 13 modules + orchestrator, §4 |
| **D-URL-6** | CannonAI embed deep-linking | Flask `/cleanplate/<path>` falls back to `index.html` for view paths + iframe `src` carries sub-route · embed stays at `/cleanplate/` root only | fallback + sub-route (tiny CannonAI PR) | CPR-M1 | **shipped CPR-M1b 2026-08-16** (CannonAI PR) — fallback + `<base>` rewrite + the frame keeps its view across reload/tier flips |
| **D-DATA-1** | Is the shared finder feasible — do Lite and Full produce the *same* active-marker partition? | verify with `tools/developer/cf_merge_diff.py --compare` · if unequal, Full owns its own slim finder | verify first; expect equal | CPD-M0 | **resolved CPD-M0 2026-08-16 — EQUAL three ways:** committed Lite 24,990 == materialized Full-active 24,990 (Δ 0; Full closed 2,929), by construction (both tiers call one `merge_facilities()`), and order-independent (`cf_merge_diff --shuffle 2`). Shared finder stands. |
| **D-DATA-2** | Shared finder: same URL vs same bytes republished under `/data-full/` | see §6.3 | same bytes under `/data-full/` | CPD-M0 | **resolved CPD-M0 2026-08-16 — same bytes under `/data-full/`** (P9; ~6.9 MB of duplicate R2 storage is trivial) |
| **D-DATA-3** | Finder row encoding | objects (V3 style) · positional arrays | measure; positional if ≥15% wire | CPD-M0 | **resolved CPD-M0 2026-08-16 — objects.** Positional measured −7.5% wire (1.49 vs 1.61 MB at 6 dp; −42% raw), under the pre-registered bar. Positional remains the M4 stretch. |
| **D-DATA-4** | Published coordinate precision (9 dp today) | keep · round to 6 dp at export (Couch untouched) | 6 dp | CPD-M0 | **resolved CPD-M0 2026-08-16 — 6 dp at export** (−5.0% finder wire, −80 KB; Couch keeps 9) |
| **D-DATA-5** | Overlay keying | position-aligned + per-bucket sha binding · full GUID per row · short permit hash per row | position + sha binding | CPD-M0 | **resolved CPD-M0 2026-08-16 — position + per-bucket `finder_sha256` in the overlay envelope** (0.19 MB vs 8-hex hash 0.32 vs GUID 0.71 on the 9-field row) |
| **D-DATA-6** | Filter bits: `permit_type_code` vs booleans | enumerate `permit_type` vocabulary; measure both | *investigate, do not pre-decide* (Cannon 2026-08-16) | CPD-M0 | **resolved CPD-M0 2026-08-16 (Cannon) — all three: `pt` code + `is_restaurant` + `mobile`** (+1.7% finder wire, ~27 KB). 18-value vocabulary published in the manifest; `mobile` ≡ "Mobile Food Unit"; 689 name-pattern-only denials mean `is_restaurant` stays a boolean. Future type filters (schools + child care 3,369 · caterers 751 · institutional ~1,170) need no data change. |
| **D-DATA-7** | Closed permits as their own lazy family | own `closed/*` family · inline in finder+overlay | own family | CPD-M0 | **resolved CPD-M0 2026-08-16 — own family, 16 shards** (0.26 MB lazy; one file measured 0.21 MB, rejected for uniformity with the inventory guard and incremental path) |
| **D-DATA-8** | Detail contract under V4 | keep V3 shape and contract string · bump to v4 for uniformity | keep; bump only if bytes change | CPD-M1 | **CLOSED 2026-08-18 — bumped to `cleanplateva.facility-detail.v4` (Cannon).** Was: *resolved CPD-M0 2026-08-16 — keep V3 shape + `.v3`; the no-churn path — detail bytes don't move at cutover, so the ETag delta skips ~all 27,919 objects.* That reason was moot in practice: the `compliance_rate` fix re-uploaded 26,999 details at the cutover anyway (27,047 uploads, 643 s), and nothing consumed the string (the client validated manifests and shard families only). Bumped for one-contract consistency: cground-skills #1688 (constants), one republish rewriting every detail once, then the client validates the detail on the click path (unsupported → unavailable, uncached, retried next click). Shape still unchanged; a future shape change bumps again. |
| **D-DATA-9** | Freshness field in the manifest | `freshness: {snapshot_id, newest_report}` top-level · under `counts` | top-level `freshness` | CPD-M1 | **resolved CPD-M0 2026-08-16 — top-level `freshness`** (measured `newest_report` 2026-08-07 on the day's archive) |
| **D-DATA-10** | Hover-dwell threshold + prefetch policy | ~150 ms dwell, cancel on leave, LRU ~200 details; touch = click | as stated | CPD-M2 | **SUPERSEDED 2026-08-17 by D-DATA-13 (CPH)** — resolved inside a cost model that had no request count; the per-hover fetch is retired (**deleted from the client at CPH-M2, 2026-08-18**). Was: **resolved CPD-M2 2026-08-16 (Cannon) — fetch on hover immediately (no dwell), LRU 200; touch = click.** The card renders instantly from the overlay and re-renders with the sparkline when the detail lands; a late detail for a marker the pointer already left is dropped; the click panel reuses the cached detail (one fetch total, verified on the throwaway port). <!-- retired-ok: a superseded decision quotes its own former resolution --> |
| **D-DATA-11** | List page size + style | 50/page numbered, URL-carried · load-more | 50 numbered | CPR-M1b (moved from CPD-M2 — pure UI change, no data dependency) | **shipped CPR-M1b 2026-08-16** — **load-more, append 50** (Cannon's call, not the recommendation); `page=N` = N chunks revealed, clamped to the filtered length |
| **D-DATA-12** | Overlay families by view — one shared overlay vs Map-only + List-only | shared union · split | shared (measured 10 KB apart) | CPD-M0 | **resolved CPD-M0 2026-08-16 (Cannon) — one shared UNION-9 overlay** (§6.2); Map-7 and List-8 share six fields; the route seams still gate closed/detail/standards |
| **D-CUTOVER-1** | V4 flip: same-PR Lite data vs transitional dual-accept | see §8 | same-PR | CPD-M3 | **resolved CPD-M3 2026-08-17 — same-PR** (#111 carried client + V4 Lite data + tripwire; committed in prose at the go/no-go, as expected) |
| **D-CUTOVER-2** | Publisher idempotence when Lite is already at the generation | detect by shard shas → skip Lite commit · always commit | detect + skip | CPD-M3 | **resolved CPD-M3 2026-08-17 (Cannon) — detect + skip:** `lite_generation_unchanged()` in `cf_publish_cleanplate.py` restores a stamp-only manifest and commits nothing (cground-skills #1669). The cutover publish itself made the last manifest-only Data-refresh commit. |
| **D-CUTOVER-3** | How cannon-food carries V4 before the site client is V4 | flag-gated `--contract v4` (default v3) landed in M1, flag deleted at M3 · V4 kept off `main` on a branch until M3 | flag-gated; delete at M3 | CPD-M0 (confirm) → CPD-M3 (delete) | **confirmed CPD-M0 2026-08-16, executed CPD-M3 2026-08-17 — flag-gated at M1 (#1667), flag + V3 emit deleted at M3 (#1668)** (no compat path survives; the flag itself is a §14.1 retirement). Cannon's note: the daily `food-log-update` task is **disabled** (store: last ran 2026-08-15T04:07Z), so no publish can carry a V4 manifest to R2 during M1/M2 regardless; the flag remains the mechanism that keeps V3 pinned by tests and makes M3 a flip. |
| **D-ACK-1** | When the acknowledgement appears | first load, map loading gray behind the modal, decline → Lite · on first judgment-bearing interaction · first load, blocking | first load (Cannon: the public *should* get Full) | CPF-M0/M1 | **resolved CPF-M0 2026-08-17 (Cannon) — first-load *blocking* modal (must answer; no dismiss-around) over the blank basemap; NO data fetch until the choice: Agree → Full, Reject → Lite (+ a persistent control on the map that re-opens the terms — label proposed at M1; the terms text promises "can be changed later from the map").** Not the recommendation: the recommended "Lite loads gray behind it" was rejected so no visitor pays Lite + Full on one visit (§9). **Shipped CPF-M1 2026-08-17** — `ack.js`; header control label "View Grades" until agreed, then "Terms" (Cannon approved); Escape on the first-load dialog is an unpersisted decline (asked again next load); Decline is red (Cannon). |
| **D-ACK-2** | Attribution inventory + placement | modal only · modal + footer + About | modal + About; footer keeps the VDH line | CPF-M0 | **resolved CPF-M0 2026-08-17 — ONE terms document ("Terms of Use and Data Acknowledgment", Cannon's wording, formal register) shown in both places: the modal renders it as a scrollable body with the two buttons "Agree and View Grades" / "Decline and Use Basic Map"; About §06 carries it permanently and is the single source (`#aboutTermsBody`; the modal clones it — no second copy). "Basic map" is the public name for the judgment-free tier. Footer keeps the VDH line and gains a "Terms & attribution" link — the exact footer wording is approved at M1 (the friendly draft was rejected; match the terms' register).** Inventory decisions the same day: OSM = attribute + link ODbL only (the OSM-derived slice is ~3.6% of pins); Google = no visitor-facing mention (review-time discriminator; nothing Google-derived is published); Overture = "Overture Maps Foundation, overturemaps.org — CDLA-Permissive 2.0 and other open licences"; Foursquare OS = one line, © 2024 Foursquare Labs, Inc., Apache-2.0 link; our derived data = as-is, no warranty, credit CleanPlateVA, no licence grant. §06 landed **hidden** in the M0 PR; **shipped CPF-M1 2026-08-17** — §06 shown, dialog clones it, footer line approved: "Inspection records: VDH via MyHealthDepartment · archived snapshot, not live · scores and grades calculated by CleanPlateVA · Terms & attribution" (→ `/about#aboutTerms`). **Amended CRP-M0 2026-08-31 (Cannon's call): ", not live" dropped — the line reads "… · archived snapshot · …" on both clients. The React client's chip also carries the snapshot date inline (staleness ambient on every view); the no-build footer already dates itself via `foodFetchedAt`, so it takes only the trim. The §05 limits card "Snapshot, not live" and the §06 terms text are untouched — different surfaces, the terms verbatim.** |
| **D-ACK-3** | `?tier=lite` survives as dev override | yes · no | yes | CPF-M1 | **resolved CPF-M0 2026-08-17 — yes; shipped CPF-M1** — forces the basic map regardless of the answer, asks no terms, hides the header control; pinned by `lite-override.test.mjs` + `ack.test.mjs` |
| **D-TRANSPORT-1** | Edge caching for R2-served Full data | Cache API in Worker · R2 public custom domain · Workers Paid + Cache API · headers only | Cache API first; custom domain if plan tier / volume argues | CPF-M2 | **resolved CPF-M0 2026-08-17 — Cache API in the Worker now (plan tier established: Free, §9); M4 observes requests/day and escalates to Workers Paid or the R2 custom domain only if volume argues. Shipped CPF-M2 2026-08-17** (`caches.default` + `X-Cache`, R2 `onlyIf` 304s; verified on the `www` host after deploy). |
| **D-TRANSPORT-2** | Retire the git Lite data channel after CPF (R2 sole transport) | yes · keep for availability defense | **keep** — after CPH-M3 the static channel is the *cheap* transport for the shared finder (D-TRANSPORT-4); retiring it would push 16 free requests per boot back through the Worker | CPH-M3 records | **recorded CPH-M3 2026-08-18 — KEEP.** The static channel is now the free transport for the shared finder (D-TRANSPORT-4 shipped: basic map 0 Worker requests, acked boot 17); retiring it would push those requests back through the Worker. The daily "Data refresh" commit stays (stamp-only refreshes commit nothing, D-CUTOVER-2). CPF-M4's observation window closes with this decision. |
| **D-DATA-13** | Hover series back at boot: where does the sparkline data live? | compact `visits` column in the existing overlay · sibling position-aligned `trend/*` family (16 shards) · one lazy `trend.json` on first hover · keep the per-hover detail fetch | `visits` in the overlay — zero new families/inventory/change-set rules, zero requests at hover; measured bracket 0.09–0.53 MB (CPD-M0), exact at CPH-M0 | CPH-M0 | **resolved CPH-M0 2026-08-17 (Cannon) — variant (b): compact `[kind, yyyymmdd, value…]`, last overlay column, oldest-first, one entry per inspection** (encoding in §6.2). +382 KB wire (acked boot 1.87 → 2.25 MB); day-delta dates (c) were 60 KB cheaper and not taken (second date convention); cross-checked 27,919/27,919 against the renderer. **M1 publishes as soon as it emits** — the deployed client decodes by `columns`. **Shipped CPH-M1 (column live 2026-08-18, cground-skills #1681) + CPH-M2 (client, CleanPlateVA #124, 2026-08-18): hover reads `f.o.visits` through `visitsOf()`, one renderer with the panel, `prefetchDetail` deleted; production 80 hovers → 0 requests.** <!-- retired-ok: D-DATA-13 names the option it replaced --> |
| **D-TRANSPORT-4** | Serve the finder as static assets for both tiers | Full client fetches `finder/<bucket>-<sha>.json` from `/data/` first (content-addressed → same name = same bytes), R2 fallback on miss; `/data/*` off `run_worker_first`; cache-control via `public/_headers` · leave as is | do it — basic map 0 Worker requests, acked boot 17; keep the CPR honest-404 spirit by treating a `text/html` answer on a shard path as a miss | CPH-M3 | **resolved CPH-M0 2026-08-17 (Cannon) — do it.** `_headers` splat rules (`/data/manifest.json` · `/data/finder/*`); the client treats an HTML-200 on a shard path as a miss (measured: that is what the SPA fallback returns for a missing asset-shaped path outside worker-first); the optional sha check of static-served shards is **skipped** (content-addressing + the overlay's `finder_sha256` binding + row-count/duplicate checks already hold). M3 measures on production whether the fallback HTML receives the `immutable` rule and moves to explicit-name rules only if it does. **Shipped CPH-M3 2026-08-18** (CleanPlateVA #126/#127, cground-skills #1684): it did — the shell for a missing name came back `immutable`, edge-cached — so `_headers` lists the 16 shards **by name**, regenerated and committed by the publisher with every Lite publish (`public_headers_text` / `write_public_headers`; the site test pins the file to the committed manifest). Measured after deploy: decline 0 Worker requests (17 static) · acked 17 + 16 static · missing shard → shell with `max-age=0, must-revalidate`. |
| **D-TRANSPORT-5** | Route fail mode for `/data-full/*` when the Free daily cap trips | fail open (Full misses → client falls back to the static basic map) · fail closed (1027 page) | fail open, once D-TRANSPORT-4 makes Lite static | CPH-M3 | **resolved CPH-M0 2026-08-17 (Cannon) — fail open if the dashboard offers it for the custom domain; not load-bearing:** the client degrades to the basic map on an HTML-200, a 429/1027 page, or a JSON 404 (ran it), and static assets do not invoke the Worker, so the site survives the cap under either mode once Lite is static. Cannon sets it at M3 where the setting exists; the agent records the state. **Recorded CPH-M3 2026-08-18 (checked in the dashboard): "Request limit failure mode" (default fail closed; fail open = bypass the Worker to the origin) is offered only when adding a zone Route; the custom-domain edit dialog has no such setting and `cleanplateva.com` has no routes — not available, not needed. The mechanism is the client's Full → Lite fallback onto the static basic map.** |
| **D-PLAN-1** | Workers plan at release | Free (100K req/day, hard daily cliff) · Paid ($5/mo, 10M req/month, no cliff) | Paid before the link is shared anywhere; the diet is about cost, the plan is about availability | Cannon, recorded at CPH-M3 | **resolved CPH-M0 2026-08-17 (Cannon) — stay on Free and observe** (not the recommendation). After CPH ≈ 17 + clicks per session ⇒ ≈5,000 sessions/day on Free vs ≈1,100 today; CPF-M4 keeps watching requests/day; the basic-map fallback (D-TRANSPORT-5) covers the cliff. Re-open on evidence. |
| **D-TRANSPORT-3** | Access app retirement sequencing | after client+Worker no longer depend on it; Cannon fires | — | CPF-M3 | **shipped CPF-M3 2026-08-17** — M1 (client) and M2 (Worker) landed first; Cannon deleted the app in the Zero Trust dashboard; the agent verified the anonymous apex the same hour. |
| **D-CLOSEOUT-1** | Where the tripwire term lists live | per-repo (cannon-food + site; this §14.1 stays the human-readable union) · one shared file read cross-repo | per-repo | CPX-M2 | *open* |
| **D-CLOSEOUT-2** | How a legitimate historical mention escapes the tripwire | inline `retired-ok` marker (HTML comment in Markdown, trailing comment in code) · per-file allowlist | inline marker — per-file allowlists rot, and CPX-M0 measured ~95 deliberate mentions, ~40 of them in the arcs' own absence-asserting tests | CPX-M2 | *open* |
| **D-CLOSEOUT-3** | Dry inventory between CPD-M3 and CPF to size the sweep | yes · no | no | CPX-M0 | **moot 2026-08-18** — CPF-M3 and CPH-M3 both shipped before CPX fired, so M0 ran once against the final shape |
| **D-CLOSEOUT-4** | M2 cannot be green while the CleanPlate briefings sit on the runway quoting every retired term as their spec (CPX-M0 F4: 35 + 33 hits) | run M3 before M2's green · drop `runway/*.md` from the scanned set · `retired-ok` the briefings | run M3 first — archiving is what makes those files exempt, and the briefings are correct as written | CPX-M2 | **resolved CPX-M0 2026-08-18 (Cannon) — run M3 first.** The arc order becomes M0 → M1 → **M3** → M2; `runway/*.md` stays in the scanned set, and the first green run still proves M1 was complete. |

## 13. History

| Date | Change |
|---|---|
| 2026-08-16 | Doc created from the design conversation of the same day. D-URL-5 decided (module split: yes-light). Three runway briefings created against it. |
| 2026-08-16 | CPR-M0 ratified D-URL-1, -2, -3, -4, -6 as recommended and D-DATA-11 as **load-more** (resolver moved CPD-M2 → CPR-M1b). CPR-M1a shipped the module split (D-URL-5 → shipped): `foodDashboard.js` → 13 per-concern modules + orchestrator, zero behavior change (CleanPlateVA PR #104). |
| 2026-08-16 | **CPR-M1b shipped** — `router.js` (paths, URL state, history, `#about` migration), `<base href>` mount, load-more List paging, SPA fallbacks in `wrangler.jsonc` / `app.py` / the CannonAI mount, `tests/routes.test.mjs`. D-URL-1..4, -6 and D-DATA-11 → shipped; the Map viewport hash stays deferred. **CPR is complete — CPD is ungated.** |
| 2026-08-16 | **Post-CPR review hotfix (PR #108):** the SPA fallback had silently broken the authenticated tier — `run_worker_first` in array form is the *complete* set of Worker-invoking paths, and `/data-full/*` was not on it, so R2 requests were answered with `index.html`. Found via the un-Access-gated `www` host (§4). Fixed by listing `/data-full/*`; §4 corrected; test pinned. Lesson folded into `c-ground-code/memory/feedback_measure_host_behavior_not_the_stand_in.md`. |
| 2026-08-16 | **CPX teed up** (`c-ground-code/runway/cleanplate-v4-closeout.md`, gated on CPF-M3) — the close-out sweep + residue tripwire. §11 gains the CPX row and the "net, not a deferral target" rule; §14.1 seeds the retired-vocabulary list that CPD-M3 / CPF-M3 append to. |
| 2026-08-16 | **CPD-M0 shipped** — `cannon-food/tools/developer/cf_v4_prototype.py` materialized every candidate V4 shape from the live archive and measured it (brotli q4 calibrated to the live edge). §7 replaced with measured numbers in one unit (the V3 table had mixed MB/MiB): **V4 Lite 1.64 MB · V4 Full 1.85 MB wire** vs V3 2.21 / 6.71. §6.2 row shapes ratified: object finder rows at 6 dp with `pt` + `is_restaurant` + `mobile` + `loc`; one shared **UNION-9** overlay (Cannon raised and rejected a Map/List overlay split — measured 10 KB apart; and confirmed the sparkline comes from the detail on dwell, not the boot); closed ×16; detail unchanged; top-level `freshness` + `vocab`. D-DATA-1 EQUAL three ways. D-DATA-6 decided by Cannon (all three fields). D-CUTOVER-3 added and confirmed (flag-gated V4 emit; the daily is currently disabled). **Found:** the V3 roster publishes `compliance_rate` / `checklist_out` / `checklist_compliant` / `cos` as null on 100% of markers (a present-but-null top-level key shadows the `checklist_summary` fallback) — fix scheduled for M1 with a pinned test. |
| 2026-08-16 | **CPD-M1 shipped** (cground-skills #1667) — cannon-food emits Contract V4 behind `--contract v4` (default v3, byte-identical): exporter families + v4 manifests, publisher guard (V3 or V4), incremental route with membership frozen (status flips refuse), orchestrator `assert_contract_roots` (alternate roots + stop after materialization while the site is V3). 27 new tests. Materialized from Couch: finder shard shas identical across tiers; R2 dry-run of the alternate root: guard passed, delete 0, 26,999 detail re-uploads once (the fallback fix lands in `facility.latest`). |
| 2026-08-16 | **CPD-M2 built and held** (this PR — the cutover PR of §8). `dataClient.js` v4: finder + overlay in parallel, per-bucket `finder_sha256` + row-count check, position merge into `f.o` by the shard's `columns`, `counts.active` tripwire; `loadClosed()` memoized; one detail LRU (200) shared by hover prefetch and click; Full → V4 Lite fallback; no V3 loader. Presentation reads the overlay for roster rows and the detail for the panel; `trendInspections` and the roster `trend` tuples are gone. Hover: instant card, sparkline from the prefetched detail (D-DATA-10 → fetch on hover, Cannon). Footer/About freshness from `manifest.freshness` on both tiers. `app.py` gained `CLEANPLATE_DATA_ROOT` / `CLEANPLATE_DATA_FULL_ROOT`. Tests: `data-client-v4.test.mjs` (replaces v3), `lite-roster-contract.test.mjs` for V4 rows, hover/scope/about/lite-override re-pinned — 208 green. **V4 Lite data (24,990 rows, snapshot 2026-08-17T03:16:03Z) committed here.** QA on a throwaway port: boot = manifest + 16 finder + 16 overlay (no `signals/`), markers painted from the overlay, hover instant→enriched, click free after hover, List sorts every column with the Compliance column populated for the first time, closed toggle fetches 16 once, `?tier=lite` gray V4, and the new client meeting the **V3** Full manifest degrades to V4 Lite (the M3 window). README contract sections rewritten to V4 (take effect at merge). |
| 2026-08-17 | **CPD-M3 shipped — Contract V4 is live on both tiers** (Cannon's go). (a) cground-skills #1668: cannon-food V3 retirement — every emit V4; flag, V3 shapers/writers/manifest builders, `_trend_event`, `--changed-permits`, V3 guard branch, V3 incremental route, `assert_contract_roots`, `test_export_contract_v3.py`, `test_export_trend.py` deleted; `tests/v4_fixtures.py` shared. (b) #111 merged 03:49Z; public manifest V4 by 03:50Z. (c) `cf_publish_cleanplate.py --full --run-id cpd-m3-cutover --apply --stop-after r2_dry_run` → plan reviewed (48 shards + 26,999 details + manifest, delete 0, skip 921) → resumed → 27,047 uploads + manifest, 0 deletes, 0 failed, 643 s; Lite commit manifest-only. Verified R2 `full-manifest.v4` (27,919 = 24,990 + 2,929, snapshot 03:50:48Z), second dry-run 0 uploads / 32 deletes (the retained V3 finder + signals generation — §8.4), local view V4 without `signals/`. (d) D-CUTOVER-1 same-PR, D-CUTOVER-2 detect + skip (#1669), identity cards + runway closed; §14.1 rows below marked ✅ and extended. **CPF ungated.** |
| 2026-08-17 | **CPF-M0 shipped** — the acknowledgement arc opens. Facts: Workers plan **Free** (100K req/day; cache hits count; `/data/*` is worker-first so Lite boots count too — §9 carries the capacity math: ≈1,000–2,500 visitor-sessions/day); the Access app is `CleanPlateVA full data` (`c9a0d19f-4935-4344-b1e5-bd7a0a16d30e`, `cleanplateva.com/data-full`, apex only, one email-allow policy, 720 h) — the id M3 hands Cannon, distinct from `CannonAI (private)` (`21e2cb50-…`) and `CannonGate` (`5048e4f4-…`); bucket `cleanplateva-data` has no custom domain and r2.dev disabled. Attribution inventory built against what cannon-food actually wires (VDH · VGIN · Census · OSM/Nominatim + Overpass · Overture Places · Foursquare OS · CARTO · Google review-only · manual pins · ours), with published-pin provenance measured across the 27,919 details (VGIN 92.5% · OSM 3.6% · Census 2.2% · ZIP-centroid 0.8% · Overture 0.6% · Foursquare 0.3% · manual 0.1%) and Nominatim / Overture / Foursquare terms re-read from source. **D-ACK-1 (blocking first-load modal over the blank basemap, no fetch until answered — Cannon's call, not the recommendation), D-ACK-2, D-ACK-3, D-TRANSPORT-1 (Cache API now, observe, escalate on evidence) resolved.** The copy is Cannon's: the agent's drafts were rejected as too friendly and personally worded, and Cannon supplied a formal "Terms of Use and Data Acknowledgment" (source data · scores and grades · location data · attribution · acknowledgment; buttons "Agree and View Grades" / "Decline and Use Basic Map") that is ONE document for both the modal (scrollable body) and About §06 — landed hidden (`d-none`) as `#aboutTermsBody` for M1 to unhide and clone into the dialog. Footer wording is approved at M1. Embed acks once per origin (no CannonAI change). |
| 2026-08-17 | **CPF-M1 shipped — the acknowledgement UX.** `public/static/js/ack.js`: one versioned stored answer (`cleanplateva.ack.v1`; bump `ACK_VERSION` to re-ask every device), a shared in-memory state the data client's gate reads at call time (`createFoodApi({ isAcknowledged })`), and a body-appended dialog cloned from About §06's `#aboutTermsBody`. `load()` now brings the basemap up first and, on a first visit, blocks on the dialog with **zero data requests** until the visitor answers (measured on the throwaway port: 0 → Decline 17 Lite / Agree 33 Full); a changed answer later re-runs the load on the other tier and closes the old tier's panel/hover; Escape on the first-load dialog is a decline for that load only. `#signInBtn` → `#ackTermsBtn` ("View Grades" until agreed, then "Terms"; icon-only under 576px; hidden under `?tier=lite`, which asks no terms). About: §06 shown and styled, §04 tier cards + route line and the "Current access" live card re-based to "Basic map" / "Inspection grades" with the answer stated, §05 names every wired provider (Overture, Foursquare, manual pins). Footer: "Inspection records: … · scores and grades calculated by CleanPlateVA · Terms & attribution" → `/about#aboutTerms` (cold-loadable; the router consumes the hash). Decline is red (Cannon). Tests: `ack.test.mjs` (11), `lite-override` / `about-transparency` re-pinned — 219 green. README tier section carries an interim note (transport still Access-gated until M3). §14.1 rows ✅. **D-ACK-1/-2/-3 shipped.** M2 next. |
| 2026-08-17 | **CPF-M2 shipped — the public transport.** `src/worker.js`: the `Cf-Access-Jwt-Assertion` check and the `signin` route are gone; `/data-full/*` cache-control is `public` with the M0-agreed TTL shape (manifest `max-age=60, must-revalidate` · content-addressed shards `max-age=31536000, immutable` · everything else `max-age=300` + ETag); repeat reads are answered from the edge through the Cache API (`caches.default`, keyed by URL, `X-Cache: HIT`/`MISS`; a HEAD reads the GET entry and never stores; the put rides `ctx.waitUntil`); a conditional request an unchanged object satisfies is a 304 with no body (R2 `get(key, { onlyIf: request.headers })`); the `..`/empty-key guards and the 405/404 shapes are unchanged; `run_worker_first` keeps both channels. Every edge hit still invokes the Worker (Free counts it — §9); the cache saves R2 reads and latency, not requests. `worker-cache-contract.test.mjs` re-pinned (14 tests, incl. hit/miss/HEAD/304 with a fake `caches`) — 225 green. README transport lines re-based; interim note now says the apex still 302s to Access until M3 while `www` (outside the apex-scoped Access app) serves the full tier publicly from this point. **D-TRANSPORT-1 shipped.** M3 next: Cannon disables/deletes `CleanPlateVA full data` (`c9a0d19f-4935-4344-b1e5-bd7a0a16d30e`); the agent verifies the anonymous apex path. |
| 2026-08-17 | **CPF-M3 shipped — Access retired; the full tier is public.** Cannon deleted the Access application `CleanPlateVA full data` (`c9a0d19f-4935-4344-b1e5-bd7a0a16d30e`, `cleanplateva.com/data-full`) in the Zero Trust dashboard; `list_access_apps` afterwards shows only `CannonGate (MFA Enabled)`, `CannonAI (private)`, and the App Launcher — untouched. Anonymous apex measured within the hour: `/data-full/manifest.json` 200 with `public, max-age=60, must-revalidate` + ETag and no redirect (the `.well-known/cloudflare-access-protected-resource/…` metadata 404s); overlay shards `public, max-age=31536000, immutable`; details `public, max-age=300` + ETag with `If-None-Match` → 304; `X-Cache` MISS→HIT. Browser, fresh anonymous visitor: dialog over the empty basemap (0 requests) → Agree → 33 Full requests, 24,288 graded rows, chips back, a hover detail fetches, About "Inspection grades / Terms acknowledged on this device"; Decline → 17 Lite requests, gray, "Basic map / Terms declined on this device". **Found and fixed the same hour (#116):** an edge HIT from `caches.default.match()` came back with `Cache-Control: public, max-age=14400` — the zone's Browser Cache TTL rewrites the stored header — so a browser holding a HIT would have kept the manifest 4 h instead of revalidating in 60 s (P3); the Worker now re-asserts the contract's `Cache-Control` on both HIT and MISS. **The §3 "Today (V3)" column is history from this line on:** the tiers were separated by Access (Full secret behind `Cf-Access-Jwt-Assertion`, cache-control `private`, a "Sign in" button in Lite, judgments visible to whoever held an Access session ≈ Cannon); they are now separated by the acknowledgement. README "Two tiers, one site" + Architecture rewritten; `project_clean_plate_va.md` re-based. **D-TRANSPORT-3 shipped. CPF M0–M3 complete; M4 observes; CPX ungated.** |
| 2026-08-17 | **Acknowledgement dialog polish** (Cannon's review of the shipped M1 UI). The dialog head is the title alone — the "CleanPlateVA" kicker and the "Before using CleanPlateVA, please review and acknowledge the following terms." lede are gone, and that lede is dropped from About §06's heading too (it was the one sentence of the terms surface that was not in Cannon's document). The terms now sit in their own **inset box**, narrower than the dialog (`min(36rem, 100% − 2.2rem)`, centred), outlined and recessed to `--cp-bg`, and the box is the scroll container so its outline stays put. Its scrollbar is **persistent**: `overflow-y: scroll` plus `::-webkit-scrollbar*` rules, because Chrome's overlay scrollbar reserves no gutter and fades — and those pseudo-elements only apply while `scrollbar-width`/`scrollbar-color` are unset, so the standard properties live in a Firefox-only `@supports not selector(::-webkit-scrollbar)` block (measured: 9–10 px gutter in both themes). Actions are centred with **Decline left, Agree right**. `ack.test.mjs` pins all of it (226 green). |
| 2026-08-17 | **The accepted view has no terms control; About §06 carries the one switch** (Cannon). The header `#ackTermsBtn` is now purely the invitation INTO the grades — shown while the terms are declined or unanswered, hidden once they are accepted, so an accepted visitor's chrome carries nothing to act on. The way back out is a state panel at the bottom of §06 (`#aboutTermsStatus`), deliberately **outside** `#aboutTermsBody` so the dialog's clone does not grow a second button: accepted → "These terms are acknowledged on this device…" + **Switch to the basic map** (`btn-outline-danger`; declines outright, since a downgrade needs no acknowledgement); not accepted → "These terms are not acknowledged on this device…" + **Review the terms and view grades** (`btn-outline-primary`; opens the dialog); `?tier=lite` → states the override and offers no button. The dialog's colour language carries over — red is the way to the basic map, blue the way to the grades — outlined on both sides because the panel is a standing preference, not the dialog's fork. Round trip verified without leaving About: accept → header gone → switch → Lite + header returns → review → agree → grades. Cannon also approved the one sentence of the terms this invalidated: "changed later from the **map**" → "from the **About page**" (one document, so the dialog moved with it). |
| 2026-08-17 | **CPH teed up — the request-budget correction.** Cannon's review of the shipped V4: hover-fetching the detail turned 33 requests + 0/hover (V3) into 33 + 1 per hovered marker, and the sparkline lands late; on Workers Free (public since CPF) requests are the metered unit and a curious mouse outspends the boot. The design ref's §10 reject #3 (hover-stub family) weighed bytes and publisher complexity, never request count — reopened; D-DATA-10 superseded; D-DATA-13 / D-TRANSPORT-4 / D-TRANSPORT-5 / D-PLAN-1 registered; §5 hover row and §6.2 overlay note amended; §9 gains the request-budget accounting and levers; §11 gains CPH before CPX (Cannon: run CPH first). Lesson recorded as `c-ground-code/memory/feedback_name_the_metered_unit.md`. |
| 2026-08-17 | **CPH-M0 shipped — the request diet, measured.** `cf_v4_prototype.py --visits-only` materialized the ratified V4 families with the exporter's own shapers plus four `visits` encodings on the day's archive (97,166 events / 27,919 markers; active rows mean 3.62 · p90 6 · max 30 visits): V3 tuples +538 KB (reproduces CPD-M0's +0.53), **compact `[kind, yyyymmdd, value…]` +382 KB (ratified, D-DATA-13)**, day-delta dates +322, broad scores only +88; acked boot 1.87 → 2.25 MB. New `tools/visits-crosscheck.mjs` ran the real renderer pipeline over all 27,919 details against the encoding — 0 mismatches. Production Worker requests counted (§9): decline 17 · acked 33 · +20 hovers 54 (`standards.json` rides with the first detail) · re-clicks free, new clicks +1 · curious mouse 99 · List/About 33 (every view boots the roster). Transport facts: `_headers` only applies to assets-layer responses (so `/data/*` must leave `run_worker_first`); a missing asset-shaped path outside worker-first is the SPA shell 200 text/html (the client will treat HTML on a shard path as a miss); the Free cap's fail mode is a per-route toggle undocumented for custom domains — and, run against today's client, HTML-200 / 429 / JSON-404 Full answers all degrade to the basic map, so fail-open is belt-and-braces once Lite is static; the opt-in "Workers Caching" would bill static requests — never enable it. **D-DATA-13 (b) · D-TRANSPORT-4 do it (sha check skipped) · D-TRANSPORT-5 fail open if offered · D-PLAN-1 Free, observe (Cannon's call, not the recommendation); M1 publishes on emit.** Found: the prototype's D-DATA-1 check misread status-less V4 finder rows as closed (fixed) and its V3-tuple row had measured empty arrays since CPD-M3 (re-derived). Receipt: `cannon-food/references/retros/cph-m0-request-diet.md`. |
| 2026-08-18 | **CPH-M1 shipped — the overlay carries `visits`, live.** cground-skills #1681: `cf_export_site.py` `_visit_event` (the ratified `[kind, yyyymmdd, value…]`), `_visits` (oldest-first by `(date, inspection_id)` — the detail's order reversed), `_rollup_marker` sets it, `_shape_overlay_v4` appends it (last column; closed rows' inline `o` follow); `cf_publish_full.py` guard shape-checks the column on overlay and closed rows; 849 tests. Materialized from Couch in 700 s (details 0 changed / 27,919 unchanged — Couch == the published state); the emitted overlay is CPH-M0's prediction to the decimal (0.61 MB br, 24.4 B/row; closed 0.30 MB); `tools/visits-crosscheck.mjs --view public/data-full` 27,919/27,919. Published as a normal publish: `--apply --stop-after r2_dry_run` reviewed (32 uploads = 16 overlay + 16 closed + manifest last; 32 deletes = the V3 finder + signals generation retained since the cutover; 0 detail uploads; skip 27,936), resumed → 23 s, 0 failed; Lite generation unchanged → stamp restored, nothing committed (D-CUTOVER-2). Live snapshot `2026-08-18T17:44:14Z`. **No cutover, proven live:** the deployed 9-column client booted on the 10-column shards (33 requests, 24,990 rows all carrying `o.visits`, hover/detail unchanged, no console errors) — the extra key rides along until M2 reads it. §5's hover row and §6.2 stay in "today → after CPH-M2" tense until M2. |
| 2026-08-18 | **CPH-M2 shipped — the hover card draws from memory; zero requests per hover.** CleanPlateVA #124: `presentation.js` `visitsOf(f)` turns the overlay's `visits` back into the series shape `buildScopeSeries()` builds from a detail — the same presentation functions and the same `_sparkSvg` draw hover and panel (`VISIT_KIND` / `VISIT_VERDICT`; an `items` verdict's IN/OUT counts ride as a synthetic adjudication that `narrativeVerdictPresentation` reads identically); `sparkline.js` shares `sparkMarkup()` between `_sparkline` (detail) and `_sparklineFromSeries` (visits); `hover.js` renders the card instantly and completely from the row — the on-hover prefetch and its late re-render are gone; `dataClient.prefetchDetail` deleted (the LRU stays for clicks; `standards.json` now loads with the first click). Tests: the card's plot from `visits` is byte-identical to the panel's from the detail on two fixtures covering every mark kind; `visitsOf` edge cases; source pins (no `prefetchDetail`, no dwell); 228 green. Local QA on the live-shaped `public/data-full/`: 20 hovers → 20 sparklines in 168 ms, 0 requests. **Production after deploy (fresh visitor): boot 33 · 80 distinct hovers +0 (0 late) · first click +2 (detail + standards) · each further new click +1 — the curious-mouse session that cost 99 costs 37.** §3 / §5 / §6.1 / §6.2 / §9 flipped to present tense; D-DATA-13 shipped; §14.1 rows below. M3 (static finder + `_headers` + `/data/*` off `run_worker_first` + fail-open where offered) next. |
| 2026-08-18 | **CPH-M3 shipped — the public channel is static; CPH complete; CPX ungated.** CleanPlateVA #126: `public/_headers` (the two `/data/*` rules), `wrangler.jsonc` `run_worker_first: ["/data-full/*"]`, `src/worker.js` sheds `publicDataCacheControl` / `looksLikeAsset` / `isSpaFallback` (the Worker serves the full channel and nothing else), `dataClient` reads the finder from `/data/` first by content-addressed name with an R2 fallback and treats an HTML-200 answer on any data path as a miss (the honest 404 the Worker used to re-impose, now client-side); tests re-pinned, 228 green. **Measured on the host after deploy:** `/data/*` keeps its Cache-Control values (from `_headers`), no `X-Cache`; a missing `/data/finder/zz-000000000000.json` flipped from the Worker's 404 text/plain to the assets layer's 200 text/html — the proof the Worker no longer sees `/data/*` — **but carrying the splat rule's `immutable`, edge-cached** (an un-ruled fallback carries `max-age=0, must-revalidate`). Fixed the same hour: #127 lists the 16 shards **by name** (17 rules) and cground-skills #1684 makes the publisher write and commit `_headers` from the Lite manifest with every Lite publish (`public_headers_text` / `write_public_headers`; the site's `worker-cache-contract` pins the file to the committed manifest); re-measured: a missing name → the shell with `max-age=0`. **Production, fresh visitor: decline 0 Worker requests (17 static) · acked 17 Worker (manifest + 16 overlay) + 16 static finder · 80 hovers +0 · first click +2, then +1 — the curious session that cost 99 Worker requests when the arc opened costs 20.** D-TRANSPORT-2 recorded (keep); D-TRANSPORT-5 recorded — the dashboard's "Request limit failure mode" (fail closed default / fail open) exists only for zone Routes; custom domains have no such setting; the client-side fallback onto the static basic map is the mechanism. §4 / §9 / §11 / §12 present tense; §14.1 rows below. **CPH is complete; CPX (the close-out sweep) is ungated.** |
| 2026-08-18 | **D-DATA-8 closed — the detail contract bumped to `facility-detail.v4`** (Cannon: "we made a change anyway"). The keep-`.v3` rationale (avoid re-uploading ~27,919 details) had been moot since the cutover's `compliance_rate` fix re-uploaded 26,999 of them; nothing consumed the string. cground-skills #1688 flipped `DETAIL_SCHEMA_VERSION`/`FULL_DETAIL_CONTRACT`; an attended `--full` republish rewrote every detail once (`--run-id detail-v4`, receipts in §13 below when verified); the client then gained validation of the detail contract on the click path (unsupported → unavailable, uncached, retried), deployed only after every object was v4. §6.1 / §14.1 amended: no `.v3` string is published anywhere; CPX's tripwire carries no `.v3` exemption. |
| 2026-08-18 | **CPX-M1 — the doc collapsed into present tense.** §3 lost its before/after columns (the Access-era half is in the 2026-08-17 CPF-M1/M3 rows above); §6.1 lost its "V3 predecessor" column and the struck `signals/*` row; §7 lost the V3 control rows; §15 stopped calling `dataClient.js` the V3 client and naming four deleted symbols and tests (`write_shard_set`, `_trend_event`, `data-client-v3.test.mjs`, `test_export_contract_v3.py`). **The V3→V4 family mapping**, for reading old retros: `finder-manifest.v3` → `finder-manifest.v4`; `full-manifest.v3` → `full-manifest.v4`; `finder-shard.v3` + the identity half of `full-finder-shard.v3` → one shared `finder-shard.v4`; `full-signal-shard.v3` → `overlay-shard.v4` + `closed-shard.v4`; `facility-detail.v3` → `.v4` (2026-08-18). **The V3 boot budget it replaced:** Lite 17 requests / 11.40 MB raw / 2.44 gzip / **2.21 MB wire**; Full 33 / 41.59 / 7.68 / **6.71 MB wire**, of which the finder alone was 13.91 MB raw (498 B/row · 90.8 B wire) and `signals/*` alone 27.67 MB raw (991 B/row · 149.5 B wire) — the family whose removal is most of the saving. |
| 2026-08-18 | **The V4 program closed (CPX-M3).** CPF archived — its M4 closed early, the one deliverable being D-TRANSPORT-2, which CPH-M3 recorded as *keep*; the residual watch on requests/day is D-PLAN-1's standing "re-open on evidence". §11 now marks CPR, CPD, CPF and CPH complete. **Final shape, measured on the host this day** (GET ×2 — a HEAD does not match a cached GET in the Cache API, which reads as a false MISS): `/data/manifest.json` → 200 `public, max-age=60, must-revalidate` and **no `X-Cache`**, the public channel being plain static since CPH-M3; `/data-full/manifest.json` → 200, same cache-control, `X-Cache` **MISS→HIT** with the contract header correctly re-asserted on HIT (hotfix #116 holding against the zone's Browser TTL); an overlay shard → `public, max-age=31536000, immutable`, `X-Cache: HIT`; a missing `/data/finder/<name>` → the SPA shell at `max-age=0, must-revalidate`, which the client treats as a miss. Boot: **basic map 0 Worker requests**, acked 17 (+16 static), hover 0, click +2 — the curious session that cost 99 costs 20. Wire **1.64 MB Lite / 2.25 MB Full** (overlay + `visits`) against V3's 2.21 / 6.71. No `.v3` string is published anywhere; there is no login and no Access app. CPX-M2's tripwires and the archiving of CPX's own briefing are what remain. |
| 2026-08-19 | **`loc` gains class 3, `venue`** — the first append to a V4 code vocabulary, and the shape of every future one: codes are positional identity, so a class may only join the END and the frozen prefix is now pinned by `lite-roster-contract`. Upstream, `cannon_food/location/venue_anchor.py` seats a site whose address is a room or space number (`WNA-Terminal B Space 32`, `Richmond International Airport Concourse B`) on the venue its already-resolved neighbours occupy, publishing site source `venue_anchor` — ranked above `vgin_street`, below `nominatim_auto`, and applying ONLY to a site already on `zip_centroid`, so it can never overwrite a real geocode. Measured on the live archive: 51 of 242 centroid sites placed (37 by VDH's own venue tag at Reagan National, 14 by address anchors incl. all of Richmond International's, which sat 7.6 km from the terminal); median move 1.14 km. Client side: the venue badge, and the fix for an asymmetry this work surfaced — the hover card qualified an approximate pin on Lite only, so the tier with MORE information gave LESS warning. Both tiers now qualify all three approximate classes through one `approximateLabel` switch. |
| 2026-09-05 | **FFX-M4 — the Fairfax Health District joins, Contract V4's shape untouched.** Fairfax County runs its own inspection program (Accela/PLUS + a public ArcGIS FeatureServer), so its ~3,900 facilities have no MyHealthDepartment path; cannon-food (cground-skills #1857) publishes them with `tenant: "fairfax"` — the finder's existing link-routing field — `Active` read as live, each report's `report_url` the county's stored Accela link verbatim, and two OPTIONAL detail-row fields: `source_outcome` (the county's own Passed / Partial Pass / Failed — provenance shown with its explanation, never an input; OQ-D) and `report_available: false` (a visit the county lists whose report the archive does not hold — a scope-unknown row on its date, linking to the county's copy; OQ-I); the detail facility names `jurisdiction_source: "fairfax"`. VDH rows and details are byte-stable; the `pt` vocabulary grows by the county's 21 `CATEGORY_TYPE` strings (append-only), so the first Fairfax publish is a full build. Both clients: `permitUrl` routes `fairfax` to the county's inspection-reports search (the finder carries no county record id, so no per-facility deep link — each report links itself, a PDF download), `isActivePermit` accepts `Active` exactly, `sourceDepartment` names the department in the panel's fact line / Source title / hand-off / List link, the county's outcome and the unavailable row render in the history, the hero and report card carry the OQ-G disclosure (a Fairfax grade is its newest full visit — no re-check channel exists). Copy (OQ-B, Cannon's four calls): the terms name both agencies once and generalize elsewhere, keeping a paragraph on the county's recorded outcome; footer "VDH and Fairfax County Health Department"; title "archived Virginia health inspection finder"; §03/§04/§05 department-neutral with "Open Fairfax County reports" beside the portal button. The Lite-roster contract accepts the `fairfax` tenant. |
| 2026-09-05 | **The county's per-facility deep link — the finder row gains `ffx_oid`.** Cannon supplied two of the county's own Experience-map links (`…#data_s=id%3AdataSource_5-17e77d67cec-layer-3%3A<n>`) and the trailing number matched the roster `OBJECTID` the loader already stores (`fairfax.objectid`: ANDYS PIZZA 42459, DOMINION EATS 335393 — confirmed against the live FeatureServer). The finder row (what the basic map's hand-off and the List's link column render from, with no detail fetched — every view boots the same roster; List paging reveals rows already in memory) gains **`ffx_oid`**: the OBJECTID on Fairfax rows, `null` on every VDH row (fixed-shape, so VDH rows stay byte-stable through the incremental comparison). Both `permitUrl`s build `FAIRFAX_EXPERIENCE_URL#data_s=id%3A<source>%3A<oid>` from it and keep the county search page as the fallback; the Source title says "Open…" when it deep-links and "Find…" otherwise. Both Lite-roster contract tests accept the key (typed when present) ahead of the publish that carries it. Stability note: the county could reassign OBJECTIDs on a layer reload (`GLOBALID` is the durable identity, but the map selects by OBJECTID); the routine's nightly `index` pull refreshes every row's `ffx_oid`, and a stale id opens the county map with nothing selected — not a broken page. |
| 2026-09-05 | **`ffx_oid` is live.** cannon-food (cground-skills #1859) publishes the key; the `ffx-oid` full publish rebound all 48 shards and rewrote the 3,886 Fairfax details with the 28,250 VDH details byte-identical (site data commit `6bac55c`, snapshot `2026-09-05T13:10:35Z`). Lite: 29,209 rows — 3,884 Fairfax integers, 25,325 nulls, no row without the key; the one Fairfax `null` sits in the closed tier — FROM SCRATCH LC (`HMFUR-2026-00009`), a facility known only from its reports, with no roster feature and so no OBJECTID — which fixes the rule as *roster-joined* rows carry it. Verified on the materialized bytes before the R2 write: the List link, the panel Source ("Open…") and the basic-map hand-off all open the county map at DOMINION EATS. Both Lite-roster contract tests now require the key on every row (`FIELDS` gains it; integer or `null` on Fairfax rows with ≥95% linked; `null` elsewhere). |

## 14. Terminology

- **tier** — Lite (judgment-free) or Full (judgment-bearing). Post-CPF the boundary is the acknowledgement, not Access.
- **ack** — the visitor's acknowledgement of the terms (derived scoring; source attribution), stored client-side; gates the client's fetch of judgment families.
- **family** — a named prepared-data resource set in the manifest (`finder`, `overlay`, `closed`, `standards`, `details`).
- **shard / bucket / generation** — one content-addressed file of a family / its sha256-of-`permit_id` slot (00–0f) / the set of shard names a manifest points at.
- **overlay** — the Full-only, position-aligned per-row judgment family that replaces `signals`.
- **judgment-free / judgment-bearing** — identity/location/source facts vs anything derived by CleanPlateVA's scoring or presented through it.

### 14.1 Retired vocabulary — the CPX tripwire seed

> CPH's retirements (M1–M3) are in the table below.

Every term, name, path, header, or number that an arc retires goes here **in the PR that retires it** (CPD-M3, CPF-M3, and any hotfix). CPX-M0 greps live prose and code for this list; CPX-M2 turns it into per-repo tests. Columns: what · replaced by · retired by · owner repo(s) for the tripwire.


**Which test owns which rows (CPX-M2, D-CLOSEOUT-1 — per-repo lists).** `tests/vitest/retired-vocabulary.spec.ts` here (ported from `tests/retired-vocabulary.test.mjs` at CRC, 2026-09-06) covers the rows owned by *site*; `cannon-food/tests/test_retired_vocabulary.py` (Tier-0) covers *cannon-food* and *c-ground-code*, scanning both from the shared checkout. Neither list is the whole table: both are deliberately restricted to strings that are dead outright, and each file names the rows it skips and why. Rows needing context a regex cannot supply — `signals/`, `facilities.json`, `Contract V3`, roster-level field names, `#about`, `run_worker_first` — stay here, with this section as the human-readable union. A deliberate historical mention escapes with an inline `retired-ok` marker on its line (D-CLOSEOUT-2).

**Exempt from the tripwire (history is allowed to say old words):** `c-ground-code/runway/archive/**`, `runway/HISTORY.md`, `cannon-food/references/retros/**`, `c-ground-code/memory/**` (memories are dated observations; CPX-M1 re-bases identity cards by hand), this doc's §13 and this §14.1, `frontend-redesign.md` §11 (its history table), git history, and any line carrying an explicit `retired-ok` marker for a deliberate historical mention.

| Retired | Replaced by | Retired by | Tripwire owner |
|---|---|---|---|
| `#about` hash as the About route; `_setView(mode, syncHash)` writing hashes | `/about` route via `router.js`; legacy hash migrates on load | CPR-M1b ✅ | site |
| `CAP = 600` / the "N more" List tail | load-more, 50 per chunk, `page=N` in the URL | CPR-M1b ✅ | site |
| "the 3,866-line `foodDashboard.js`" as present tense | 13 per-concern modules + a ~380-line orchestrator | CPR-M1a ✅ | site, c-ground-code refs |
| `run_worker_first: ["/data/manifest.json", "/data/finder/*"]` or `["/data/*"]` | `["/data/*", "/data-full/*"]` — array form is the complete worker-invoking set | CPR-M1b + hotfix #108 ✅ | site |
| "`/data-full/*` still routes to the Worker" (unqualified) | it does **only because** it is listed in `run_worker_first` | hotfix #108 ✅ | site |
| `signals/`, `cleanplateva.full-signal-shard.v3`, "sparse signal shards", "16 signal shards" | `overlay/` (`cleanplateva.overlay-shard.v4`, named CPD-M0) + `closed/` (`cleanplateva.closed-shard.v4`) | CPD-M3 ✅ | cannon-food, site |
| `cleanplateva.finder-shard.v3` · `full-finder-shard.v3` · `finder-manifest.v3` · `full-manifest.v3` | `cleanplateva.finder-shard.v4` (one string, both tiers) · `finder-manifest.v4` · `full-manifest.v4` (named CPD-M0; `facility-detail.v3` was the one deliberate keep at the time — D-DATA-8, since **closed**: see the 2026-08-18 row below) | CPD-M3 ✅ | cannon-food, site |
| roster-level `trend` tuples, `_trend_event` feeding the roster, "hover card without a fetch" | detail-derived hover (prefetched on hover, no dwell — D-DATA-10) | CPD-M3 ✅ | cannon-food, site |
| roster-level `latest_assessment`, `newly_permitted`, 17-field `latest` | overlay row fields (`f.o`) | CPD-M3 ✅ | cannon-food, site |
| the client-side "newest held report" scan of every `latest.date`; "Not exposed publicly" as the Lite footer's newest-report text | `manifest.freshness.newest_report` (both tiers) | CPD-M3 ✅ | site |
| "33 requests / 39.66 MB raw / ~6.7 MB" as the *current* boot | the measured V4 numbers (Lite 1.64 · Full 1.85 MB wire) | CPD-M3 ✅ | site README, cannon-food SKILL |
| `data-client-v3.test.mjs` · `test_export_contract_v3.py` · `test_export_trend.py` · "Contract V3" as present tense | `data-client-v4.test.mjs`, `test_export_contract_v4.py`, `tests/v4_fixtures.py`; "Contract V4" | CPD-M3 ✅ | site, cannon-food |
| the transitional `--contract v4` flag itself; `assert_contract_roots`; the `--changed-permits` partial-export path | default v4, flag deleted; `--lite-out` stays | CPD-M3 ✅ | cannon-food |
| `trendInspections` (client tuple decoder), `f.trend`/`f.latest`/`f.grade` roster reads, `f.location.{lat,lon,source}` roster reads, `_geoNote(source)` | `f.o` + `coordsOf` / `locationClass` / `latestDateOf` / `isNewlyPermitted` / `isActivePermit` in `presentation.js`; `_geoNote(f)` | CPD-M3 ✅ | site |
| exporter V3 surface: `_shape_lite`, `_shape_finder`, `_sparse_marker_inspection`, `_sparse_grade`, `_shape_signal`, `write_shard_set`, `build_public_manifest`, `build_full_manifest`, `_trend_event`; publisher V3 guard branch + `_validated_family_ids`; incremental `_load_family` / `_full_counts` / `_lite_counts` / `_signal_declining` | `_shape_finder_v4` / `_shape_overlay_v4` / `_shape_closed_v4` / `bucketize_v4` / `write_v4_family` / `build_*_manifest_v4`; `_validated_family_shards` + `_assert_export_contract_v4`; `_load_v4_keyed` / `_load_v4_active` / `v4_full_counts` | CPD-M3 ✅ | cannon-food |
| `manifest.counts.scored` / `.declining` / `.open_repeat` | dropped (never read by the client; `open_repeat` not derivable from V4 rows); `counts` = `total, active, closed, by_grade, by_zip` | CPD-M3 ✅ | cannon-food, site |
| "Full is authenticated / Access-gated"; "authenticated full-data channel" | "Full is acknowledged" — the tier boundary is the ack | CPF-M3 ✅ (site README, `project_clean_plate_va.md`); cannon-food SKILL + c-ground-code refs/mcps left for CPX | site, cannon-food SKILL, c-ground-code refs/mcps |
| `Cf-Access-Jwt-Assertion` check in `src/worker.js`; the `data-full/signin` route | none (the tier boundary is the ack); public cache-control + Cache API | CPF-M2 ✅ | site |
| `#signInBtn`, "Sign in to view inspection detail", `cp-signin-label` | `#ackTermsBtn` / `.cp-ack-label` — the terms control ("View Grades" / "Terms") | CPF-M1 ✅ | site |
| About tier vocabulary "Public finder" / "Authenticated archive"; the live card's "Detecting data tier"; "Browser tries full → falls back to finder" | "Basic map" / "Inspection grades"; the live card states the answer on file; "Terms acknowledged → inspection grades load; otherwise the basic map remains" | CPF-M1 ✅ | site |
| the footer's "Data snapshot: … · source IDs retained" line | "Inspection records: … · scores and grades calculated by CleanPlateVA · Terms & attribution" | CPF-M1 ✅ | site |
| "Show inspection grades" as the affordance label (design placeholder) | "View Grades" / "Terms" | CPF-M1 ✅ | c-ground-code refs |
| the `#foodZipFilter` select, `_populateZipFilter()`, `_filters.zip`, and `zip` as *written* URL state | one search box: name/address/city substring **plus ZIP prefix** (`filters.js`). `zip` stays on `URL_KEYS` as a legacy-in key — parsed once into `q`, then cleared — so old shared links keep their meaning | 2026-08-18 ✅ | site |
| `private, max-age=…` on `/data-full/*`; "browser-private immutable" | `public, …` (shards immutable, details 300 s + ETag); `X-Cache` diagnostic | CPF-M2 ✅ | site, c-ground-code memory (`project_clean_plate_va.md` says browser-private — CPX/M3 re-bases) |
| the Cloudflare Access application "CleanPlateVA full data" (apex-scoped, `c9a0d19f-4935-4344-b1e5-bd7a0a16d30e`) | retired 2026-08-17 (deleted by Cannon) — **not** CannonAI's own Access app | CPF-M3 ✅ | c-ground-code mcps/memory |
| `prefetchDetail` (the on-hover detail fetch), "prefetched on hover, no dwell", "fetch on hover immediately", "the hover card … fills its sparkline in from the facility detail", D-DATA-10 as a live policy | the overlay's `visits` column + `visitsOf()` — the card renders whole from the row; the detail is fetched on click only (P5) | CPH-M1 (column) + CPH-M2 (client) ✅ 2026-08-18 | site, cannon-food SKILL, c-ground-code refs/memory |
| "no series at boot" / "no series rides the roster" (as present tense) | `visits` rides the overlay (UNION-9 + `visits`); "the roster carries nothing else per inspection" | CPH-M1 ✅ 2026-08-18 | site README, cannon-food SKILL, c-ground-code memory |
| "nine-field overlay row" / "UNION-9" as the *complete* row; a 9-column `V4_OVERLAY_COLUMNS` | ten columns — UNION-9 + `visits` | CPH-M1 ✅ 2026-08-18 | cannon-food, site |
| `run_worker_first: ["/data/*", "/data-full/*"]`; "both data channels route through the worker"; "`/data/*` is worker-first, so Lite boots invoke the Worker"; "17 Worker requests for the basic map" / "33 for the acked boot" as present tense | `run_worker_first: ["/data-full/*"]` — the public channel is static; basic map 0 Worker requests, acked boot 17 (+ 16 static finder) | CPH-M3 ✅ 2026-08-18 | site, c-ground-code refs/memory, cannon-food SKILL |
| the Worker's `publicDataCacheControl`, `looksLikeAsset`, `isSpaFallback`, `HTML_EXTENSIONS`; "the worker re-404s a missing `/data/` shard" | `public/_headers` (by shard name, publisher-written) + `dataClient` treating an HTML answer on a data path as a miss | CPH-M3 ✅ 2026-08-18 | site |
| a `/data/finder/*` splat rule in `_headers` | one immutable rule per committed shard, by name (`public_headers_text`) — the splat stamped `immutable` on the SPA shell for a missing name | CPH-M3 ✅ 2026-08-18 (#127) | site, cannon-food |
| "the fail-open route toggle" as a thing `cleanplateva` can set | not offered on custom domains (zone Routes only); the client-side fallback onto the static basic map is the mechanism | CPH-M3 ✅ 2026-08-18 | c-ground-code refs/memory |
| `cleanplateva.facility-detail.v3` (was the one deliberate `.v3` keep, D-DATA-8) | `cleanplateva.facility-detail.v4` — every published object is Contract V4 | 2026-08-18 ✅ (cground-skills #1688 + republish + site client validation) | cannon-food, site — **the tripwire needs no `.v3` exemption** |
| Bootstrap 5.3.3 + bootstrap-icons + the jsDelivr CDN as a host (`cdn.jsdelivr.net` · `bootstrap-icons` · `data-bs-theme`) | Tailwind v4 + Lucide in a first-party bundle — no CDN host at all | CRC-M1 ✅ 2026-09-06 | site |
| `static/css/style.css` | `app/theme.css` (tokens as `@theme`) | CRC-M1 ✅ 2026-09-06 | site |
| the 19 no-build ES modules + `public/index.html` as the shipped client (`static/js/`) | the Vite build from `index.html` + `app/`, served from `dist/` (`assets.directory`) | CRC-M1 ✅ 2026-09-06 | site (c-ground-code refs → CRX) |
| `tests/support/dashboard.mjs` (the concatenated-source import shim) | nothing — Vitest imports the modules | CRC-M1 ✅ 2026-09-06 | site |
| `app.py` as the dev server (`python app.py`, `requirements.txt`, the `clean-plate-va` launch entry on :5001) | `npm run dev` (Vite; the `cleanplateva-vite` launch entry on :5620) | CRC-M1 ✅ 2026-09-06 | site, c-ground-code launch entries |
| `node --test` as the app-suite runner (`test:node`, `tests/*.test.mjs`) | `vitest run` — one runner, one CI step; the two survivors ported as `tests/vitest/{retired-vocabulary,worker-cache-contract}.spec.ts` | CRC-M1 ✅ 2026-09-06 | site |
| `wrangler.preview.jsonc` / `cleanplateva-preview` / `next.cleanplateva.com` as the pre-flip preview (D-CR-STAGE-1 re-resolution 2026-08-30) | production serves `dist/` | CRC-M1 (config deleted 2026-09-06) → **CRC-M2 (Worker + domain torn down; the regex lands then)** | site, c-ground-code refs/memory |
| *kept, not retired:* `?tier=lite` dev override; `tenant`-routed VDH links; one document per permit in Couch | — | — | (do not flag) |

**Pre-V4 retirements worth catching while the sweep is running** (opportunistic in CPX-M0 — fix on hit, do not hunt): `public/data/facilities.json` (public monolith, retired 2026-08-05); `cf_couch_check.py` → `cf_couch_probe.py --mode transport`; `cf_scrape_recent.py` (deleted); `cf_migrate_location_v3.py` → `cf_apply_location_projection.py`; `cf_facility_location_{candidates,adjudicate,promote}.py` → `cf_location.py <stage>`; `cf_facility_location_google.py` → `cannon_food/location/google.py`; `cf_geocode_repair.py` (folded into `cf_geocode_backfill.py`); `cf_statewide_runner.py` + `cf_targetzips_commit.py` (deleted 2026-08-05); Contract V1/V2 language.

## 15. Cross-references

| Path | Why |
|---|---|
| [`README.md`](../README.md) | The shipped contract (V4 since CPD-M3) — authoritative for what is live, incl. the transport + request budget after CPH |
| `app/data/client.ts` | The V4 client (TypeScript strict since CRF-M1; the old `dataClient.js` until CRC): manifest-led loads, the static-first finder with R2 fallback (CPH-M3), `decodeChecklist`, detail-contract validation on the click path, Full→Lite fallback |
| `app/App.tsx` + the view modules (`MapView` · `ListView` · `AboutView` · `DetailPanel` · `HoverCard` · `Toolbar` · `AckDialog`; `mapLayers.ts` · `useMapPopup.ts` · `useGeolocate.ts` · `useAppRouter.ts`) | The React client (CR program — `docs/frontend-redesign.md` §4, §6); replaced the per-concern ES modules at CRC |
| `src/worker.js` · `wrangler.jsonc` · `public/_headers` | Transport: the R2 full channel (public, Cache API) is the Worker's only job since CPH-M3; `run_worker_first: ["/data-full/*"]`; `_headers` (publisher-written, by shard name) carries the static channel's cache-control |
| `tests/vitest/lite-roster-contract.spec.ts` · `worker-cache-contract.spec.ts` · `data-client-v4.spec.ts` · `ack.spec.ts` · `visits-parity.spec.ts` · `retired-vocabulary.spec.ts` | The contract tripwires CPD/CPF/CPH left behind — under Vitest since CRF-M1 (the ports) and CRC (the two survivors) |
| `cannon-food/scripts/cf_export_site.py` | Deterministic full-rebuild authority; owns the V4 contract constants and the readers for its own output (`shard_bucket`, `bucketize_v4`, `write_v4_family`, `read_site_baseline`) |
| `cannon-food/scripts/cf_publish_cleanplate.py` · `cf_publish_full.py` · `cf_publication_change.py` | Orchestrator, R2 inventory guard, change-set domains (`location`/`inspection`/`assessment` incremental; `facility`/`site`/`standards`/`contract` full-rebuild) |
| `cannon-food/tests/test_export_contract_v4.py` · `test_export_incremental.py` · `test_publication_change.py` · `test_publish_full.py` · `test_publish_cleanplate.py` | The cannon-food suites CPD-M1 rewrote |
| `cannon-food/tools/developer/cf_merge_diff.py` | Merge-partition comparison for D-DATA-1 |
| `cannon-food/tools/developer/cf_v4_prototype.py` · `tools/visits-crosscheck.mjs` (this repo) | The V4 / `visits` measurement study and the renderer-vs-exporter parity check (CPD-M0, CPH-M0; re-run at CPH-M1 with `--view public/data-full`) |
| `CannonAI/cannonai/gui/server.py` (`cleanplate_site`) | The embed passthrough D-URL-6 touches |
| `c-ground-code/memory/project_clean_plate_va.md` · `project_cannon_food.md` | Identity cards; update when milestones land |
| `c-ground-code/runway/cannonai-tab-separation.md` | CTS — CannonAI's own routes (`/food`), the embed's parent |
