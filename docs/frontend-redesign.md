# CleanPlateVA front-end redesign — the CR program design reference

**React + Vite + Tailwind, redesigned as it is rebuilt, on a frozen Contract V4.**

| | |
|---|---|
| Status | **Living design reference.** Governs the CR program — **CRD** (design) → **CRF** (foundation) → **CRV-a/b** (views) → **CRC** (cutover) → **CRX** (close-out). Each arc's runway briefing cites this doc by section instead of restating it. |
| Owner | Cannon decides; the active agent (lead dev) maintains. |
| Written | 2026-08-29 (CRD-M0), against the shipped Contract V4 site (V4 program closed 2026-08-18) and the production asset baseline measured the same day (§8). Measured numbers are dated receipts, not constants. |
| Update rule | When a milestone resolves an entry in **§10 Open decisions**, flip its status **in this doc, same PR**. When a principle changes, change it here first, then the briefings. Superseded content is struck or moved to **§11 History**, never silently deleted. |
| Relationship to [`architecture-v4.md`](architecture-v4.md) | That doc stays **authoritative for data + transport** — the families, the tier boundary, the request budget's mechanics, the publisher. This doc is **authoritative for the front-end** — stack, build, components, styling, tests, and the cutover. This doc *cites* the constitution, never restates it; where the two could ever disagree, architecture-v4.md wins on data/transport and this doc wins on the client that consumes them. |
| Runways | `c-ground-code/runway/cleanplate-react-redesign.md` (CRD, the program opener) · CRF/CRV/CRC/CRX briefings are teed up one at a time as the prior arc closes. |

---

## 1. Why + the program frame

The V4 program left the site on a settled floor: Contract V4 on both tiers, public Full behind the acknowledgement, a request-dieted transport, residue tripwires green. What remains old is the **front-end fabric**: ~6.7k lines of no-build ES modules across 19 files, a 705-line `index.html`, a ~2,800-line `style.css` over **Bootstrap 5.3.3 + bootstrap-icons from the jsDelivr CDN**, and MapLibre GL 5.19.0 CDN-loaded as a classic script. <!-- retired-ok: §1 describes the pre-CR fabric as it stood 2026-08-29 -->

**Ratified 2026-08-29 (Cannon, the CR tee-up session) — the four program decisions:**

1. **Stack:** React + Vite, styled with Tailwind.
2. **Scope:** **redesign as we rewrite** — each view is built to its new design, never a pixel-port of its old one.
3. **Shape:** a series of arcs + this one shared design reference (the V4 pattern).
4. **Data contract:** **Contract V4 is frozen for the whole program.** cannon-food, the published families, and `src/worker.js` are untouched end to end. A redesigned view that genuinely needs data the families do not carry is an out-of-program escalation to Cannon, never a quiet PR.

Because the scope is redesign-not-port, the existing suites stop refereeing presentation. The referee becomes **per-view acceptance specs** (§6): CRD writes them with the mockups, CRV builds against them, CRC verifies them at the flip.

### The arcs

**Status: the CR program closed 2026-09-06.** Every arc below shipped; this document is the front-end constitution from here on — §2 the invariants every change keeps, §6 the ratified per-view specs, §7 the test strategy, §8 the measured budgets, §9 the cutover and close-out receipts, §10 the decision registry, §11 the history. The CRP parity passes (M0–M3, M5, M6; §11) ran between CRV-b and CRC on Cannon's re-sequencing.

| Arc | Lands | Gate | Closed |
|---|---|---|---|
| **CRD** — design | This reference merged, every D-CR-* fork resolved, the wire baseline measured (§8), visual direction (style tile + per-view mockups), per-view acceptance specs (§6) | none — fires first | ✅ 2026-08-29 |
| **CRF** — foundation | Vite + React + Tailwind scaffold, build + Workers Builds pipeline proven, the Contract V4 data client and ack gate on the new stack, contract tripwires ported and green — boots to a working gray basic map + acked overlay merge, zero UI beyond proof | CRD | ✅ 2026-08-30 (#163–#166) |
| **CRV-a** — shell + Map + hover + detail panel | The redesigned map surface built to its acceptance specs | CRF | ✅ 2026-08-30 (#167–#169) |
| **CRV-b** — List + About/terms + ack chrome | The remaining views | CRV-a | ✅ 2026-08-30 (#170–#172) |
| **CRC** — cutover | Production flips to the new client in one PR (new build live, old modules deleted), embed re-verified, budgets re-measured on the host, retirements appended to architecture-v4.md §14.1 | CRV | ✅ 2026-09-06 (#191–#193; the flip #192) |
| **CRX** — close-out | Stale-prose sweep across repos, retired-vocabulary tripwires extended, docs/memory re-based; the two follow-on calls (D-CRX-1/2) | CRC | ✅ 2026-09-06 (#194–#196 + cground-skills #1870/#1871; §9.3–9.5) |

One PR per milestone, pause between milestones, later arcs got their own briefings teed up as the prior closed.

## 2. Parity constitution — what survives the rewrite

The redesign may move **everything except these**. Every arc inherits this table; CRC verifies it on the host. Sources: [`architecture-v4.md`](architecture-v4.md) (§ cited per row), [`README.md`](../README.md), [`wrangler.jsonc`](../wrangler.jsonc). *(Moved here from the CRD runway briefing at M0; the briefing now cites this section.)*

| # | Invariant | Source |
|---|---|---|
| **C1** | **Contract V4 client semantics.** Manifest-led boot; finder + overlay fetched in parallel, per-bucket `finder_sha256` + row-count check, position merge; closed lazy on first toggle; standards with the first click; detail on click only (P5), LRU-cached; detail contract validated on the click path; **an HTML-200 answer on any data path is a miss, never JSON**; Full → basic-map degrade on any contract failure (P4); static-first finder from `/data/` by content-addressed name with R2 fallback on miss; the grade letter is never shipped — always `gradeForScore(score)`. | v4 §5–§6, README |
| **C2** | **The ack gate.** Blocking first-load dialog over the empty basemap; **zero data fetches until answered**; Agree → Full, Decline → basic map; versioned `localStorage['cleanplateva.ack.v1']`; Escape = unpersisted decline; `?tier=lite` forces the basic map, asks no terms, hides the control; accepted chrome carries no terms control — About §06 owns the switch back; the terms are ONE document (Cannon's words, formal register), single-sourced in About and cloned into the dialog. | v4 §3, D-ACK-1..3 |
| **C3** | **The request budget is a hard gate.** Basic map / decline **0** Worker requests; acked boot **17** Worker (+16 static finder); hover **0** (the card renders wholly from the overlay's `visits` column); first click +2 (detail + standards), each further new facility +1. New UI adds no per-interaction fetches; new app assets are static (free). Never enable the opt-in "Workers Caching" (it bills static requests). | v4 §9, README |
| **C4** | **Transport config.** `run_worker_first` stays exactly `["/data-full/*"]`; `not_found_handling: "single-page-application"`; the Worker serves the R2 full channel and nothing else; `workers_dev` / `preview_urls` stay off unless D-CR-STAGE-1 consciously flips one. | wrangler.jsonc, v4 §4 |
| **C5** | **Publisher-written files keep their paths and reach the deploy verbatim.** `public/data/**` (manifest + 16 finder shards) and `public/_headers` (by-name immutable rules, rewritten and committed by cannon-food with every Lite publish) are cannon-food's write targets — the frozen contract means the publisher never learns the front-end changed, so the build must carry both into the served assets dir untouched. **Amended at CRX (D-CRX-1, 2026-09-06):** `_headers` reaches `dist/` as the publisher's block byte-identical *followed by* a build-appended block — one `immutable` rule per content-hashed asset the build emitted, by name, never a splat, none for the un-hashed maplibre worker pair; `public/_headers` itself is never edited (`vite.config.ts` `buildAssetHeaders`, pinned by `dist-contract.spec.ts`). | v4 D-TRANSPORT-4, cground-skills #1684; D-CRX-1 (§10) |
| **C6** | **URL scheme + search semantics.** `/` canonical (`/map` normalizes) · `/list` · `/about`; query keys `q` `grade` `restaurants` `closed` `new` `mobile` `permit` + List `sort` `dir` `page` (load-more, 50/chunk); URL wins when present, non-default-only writes measured against the *persisted* toggle value; pushState on view/facility change, replaceState for filter churn; inside the CannonAI frame everything is replaceState. Search: whitespace-split terms, every term must match, per-term name/address/city substring + ZIP **prefix**, quoted phrase whole-within-one-field, legacy `?zip=` folds into `q` once. The redesign may *add* routes/keys; it may not re-mean these. | v4 §4, README Routes |
| **C7** | **The embed contract.** CannonAI's Food tab iframes the site at `/cleanplate/` (`?tier=lite` for the basic-map toggle); the served `<base href>` is rewritten by the passthrough and the router reads `document.baseURI` — one build serves any mount depth; the frame tracks the view across reloads; ack is once per origin. The Vite build must keep base-relative assets for this to keep working (D-CR-EMBED-1). | v4 D-URL-6 |
| **C8** | **Copy register.** Public copy is the formal register; the public word is **"basic map"**, never "Lite" to a visitor; every displayed score/grade is CleanPlateVA's own derivation and is never described as official VDH scoring; footer keeps the VDH line + "Terms & attribution"; attribution inventory (D-ACK-2) rides wherever About's content lands. | memory cards, v4 D-ACK-2 |
| **C9** | **Vocabulary + derivation rules.** `loc` / `pt` / `scope` decode via the manifest's `vocab` block and are **append-only** (codes are positional identity); `is_restaurant` is a boolean, never derived from `pt`; both tiers qualify all three approximate-location classes (one label switch); a ZIP-centroid pin is a failed site, never dressed up. | v4 §6.2, 2026-08-19 row |
| **C10** | **Both themes first-class; the device's appearance is the visitor default** (amended 2026-09-06 twice, Cannon's calls — dark was the default through the CR program, light for part of that day, and since the settings dialog the choice is three-way: light · dark · system, with system the default). The redesign restyles freely; the un-prefixed CSS stays the dark design and `.theme-light` is applied whenever the RESOLVED theme is light (Tailwind strategy is D-CR-STYLE-1). | memory card |
| **C11** | **Tripwires stay green throughout.** `tests/vitest/retired-vocabulary.spec.ts` (site; the `.test.mjs` original until CRC) and `cannon-food/tests/test_retired_vocabulary.py` (scans c-ground-code too) run on every PR of every arc — the rewrite *appended* its own retirements at CRC, it never resurrects V4's. `worker-cache-contract` passed untouched through every arc and ported to Vitest at CRC (the Worker doesn't change). | v4 §14.1 |
| **C12** | **P1–P9 in full** — especially P2: the site never computes at request time. React + Vite here means a **static SPA build**; no SSR, no server functions, no query API, ever. | v4 §2 |

### Explicitly redesignable

Layout, chrome, information hierarchy, component structure, styling system (Bootstrap + `style.css` retire wholesale), interaction patterns (hover card presentation, panel anatomy, list density, filter affordances), iconography, typography, the About page's shape (content + single-source mechanism survive per C2/C8), responsive posture, and the module graph itself. The 19 ES modules, `index.html`, and `app.py`'s SPA-fallback role all retire at CRC (each retirement → v4 §14.1, same PR). List columns may change presentation freely but can only *draw* on what the frozen overlay carries (nine fields + `visits`) — a column needing new data is out of program (§1.4).

## 3. Stack + build pipeline

*Resolutions from D-CR-LANG-1 and D-CR-BUILD-1/2/3 (§10). This section records the end state; CRF proves it.*

- **Language: TypeScript, `strict`.** The data client's decode/merge/fallback paths — overlay `columns` decoding, `visits` mark kinds, checklist flag bits, the Full → basic-map degrade ladder — are exactly where types pay. Contract V4's row shapes become checked types (`FinderRow`, `OverlayRow`, `VisitMark`, `FacilityDetail`), written from v4 §6.2 and pinned by the ported contract tests.
- **Repo layout: Vite at the repo root, the app under `app/`, output `dist/`.** `public/` remains the publisher's channel and rides into the build verbatim as Vite's `publicDir` (with `public/data-full/**` — 400+ MB, gitignored, local-only — excluded from the build copy). `src/worker.js` and wrangler's `main` pointer never move. The old `public/index.html` + `public/static/**` remained the *served* site until CRC (2026-09-06), which deleted them. The build also ships maplibre-gl's runtime-resolved worker pair (`maplibre-gl-worker.mjs` + its one import `maplibre-gl-shared.mjs`) beside the main chunk (`vite.config.ts` `maplibreWorkerCopy`) — maplibre computes those URLs at runtime against its chunk's URL, so no bundler emits them, and without them the SPA fallback answers the worker request with HTML and the map never loads (found on the preview host 2026-08-30 — dev never hits it, `optimizeDeps.exclude` serves native ESM from `node_modules`; pinned by `dist-contract.spec.ts`).
- **Chunks (CRP-M5, 2026-09-05, [#190](https://github.com/CannonWest/CleanPlateVA/pull/190)):** the build splits what changes from what does not — Rolldown `codeSplitting` groups in `vite.config.ts` put maplibre in its own chunk (`maplibre-*.js`, its CSS beside it), React in its own (`react-*.js`), the rest of node_modules in `vendor-*.js`, and the app in the entry; List, About and the report-card modal load on demand (`React.lazy` + `Suspense`). The shell preloads the static vendor chunks and never the lazy ones. Why: content-addressed names mean an app-only deploy re-downloads ~40 KB (entry + its CSS) instead of ~377 KB, for every returning browser and every edge colo; the request budget is untouched — a chunk is a static asset, zero Worker requests (C3). Pinned by `dist-contract.spec.ts`. Measured in §8.
- **Deploy: the existing Workers Builds git integration gains a build command** (`npm ci && npx vite build`). Sequencing so production never flips early: CRF adds the build command while `assets.directory` still points at `./public` — builds run and are proven green, but what is served does not change. CRC's one-PR flip is `assets.directory` → `./dist` (+ deleting the old client). **Metered-unit check — recorded CRF-M0 (2026-08-29):** the unit Workers Builds meters is the **build minute**; the Free plan carries **3,000 build minutes/month, 1 concurrent build, 20-minute timeout** (docs, retrieved 2026-08-29). Push rate measured from `main`'s last 30 days: **~89 builds/month in an active dev month** (24 cannon-food data publishes + 65 PR merges; a quiet month floors near 30) — **plus PR-branch builds**: Workers Builds checks every branch push too (observed live on CleanPlateVA #163, pre-build-command), so an active month runs ~150–200 builds all-in. At a conservative 3 min/build that is ~450–600 min ≈ **15–20% of quota** (quiet month ~3%) — the pipeline is safe to rely on with wide margin. The build command went live in the dashboard 2026-08-29 (build `npm ci && npx vite build`, deploy `npx wrangler deploy`, version `npx wrangler versions upload`, root `/`, builds-for-non-production-branches on). **First real builds measured 2026-08-30 (#164):** branch build push→green **~34 s** wall; production build push→deployed **~47 s** (deployment record 01:52:28Z vs merge push 01:51:42Z; the pre-command baseline was ~40 s, so the build itself rides in well under a metered minute) — call it **≤1 build-minute per push**, making the earlier 3-min/build estimate a ~3× overcount: even a 200-build month spends ~200 of 3,000 minutes (~7%). Production verified byte-identical after both merges (shell, `style.css`, `app.js`, `/data/manifest.json` sha256-matched the pre-M0 baseline). Re-check the math if the plan or the push rate changes an order of magnitude. **Corrected at the flip (2026-09-06, [#192](https://github.com/CannonWest/CleanPlateVA/pull/192)): the "branch build" above never built.** Measured in the dashboard: a push to `main` runs Initializing → Cloning → Installing → **Building (17 s)** → Deploying with the build command (build `2a3b8dfb`, the #191 merge); a push to a non-production branch runs Installing → `npx wrangler versions upload` with **Build command: None** and no Building stage (builds `b5dc271d` on #190's branch and `15a45a78` on #192's) — one Build configuration, "Builds for non-production branches" on, no per-branch field. Every earlier branch build "passed" because `./public` needed no build; the flip's branch check went red the moment `assets.directory` pointed at an un-built `./dist`. A branch push therefore proves wrangler's view of the committed tree, never a built `dist/`; the pre-merge proof of the build is GitHub Actions (`npm ci · npx vite build · npx vitest run`, the same command on Node 22) plus a local `npx wrangler deploy --dry-run` against `dist/`, and Cannon shipped #192 on that. The ~34 s branch figure above was install + upload. **Standing consequence:** with `assets.directory` at `./dist`, every PR's Workers Builds check read red for the same reason — read GitHub Actions instead — until D-CRX-2 resolved it. **Resolved CRX-M2 (D-CRX-2, 2026-09-06, Cannon):** "Builds for non-production branches" is **OFF** in the dashboard (Build configuration → Branch control; production branch `main`; flipped in-session through Cannon's browser at his instruction and confirmed after a reload). A branch push now produces no Workers Build at all — no red check, no build minutes — and the PR proof is GitHub Actions (`npm ci · npx vite build · npx vitest run`) plus a local `npx wrangler deploy --dry-run` against `dist/`. Pushes to `main` still build and deploy as before. The wrangler `build.command` alternative was not taken (it would double-build `main` until the dashboard command was removed, and it touches `wrangler.jsonc`).
- **Local dev: the Vite dev server** (serving `public/` passthrough including a local `data-full/` for full-tier work). **`app.py` survived untouched until CRC** (2026-09-06), then retired → v4 §14.1.
- **`wrangler.jsonc` was untouched until CRC** (C4): same `run_worker_first`, same SPA fallback, same Worker. The only CRC change to it was the assets directory (`./dist`, 2026-09-06).
- **The dual-track preview Worker (stood up 2026-08-30; D-CR-STAGE-1 re-resolved, §10):** `cleanplateva-preview` serves the built `dist/` at **`next.cleanplateva.com`** from `wrangler.preview.jsonc` — same Worker code, same R2 bucket (GET/HEAD only), same `run_worker_first` + SPA fallback. Deployed manually from the checkout (`npx vite build && npx wrangler deploy -c wrangler.preview.jsonc`), never by Workers Builds; its `dist/` data snapshot goes stale between deploys. Production and `wrangler.jsonc` stay untouched. Retires at CRC (§9). **Torn down 2026-09-06 at CRC-M2** — `npx wrangler delete --name cleanplateva-preview` from a config-free directory (so the only Worker name wrangler could see was the argument); the origin-worker-owned AAAA record it had created for `next.cleanplateva.com` went with it (the host answers 530, the resolver NXDOMAIN); production untouched. <!-- retired-ok: the dated record of the dual track -->

## 4. Component architecture + state

*Resolutions from D-CR-ROUTE-1, D-CR-MAP-1, D-CR-STATE-1, D-CR-STYLE-1 (§10). The component tree below is a sketch — CRV builds to the acceptance specs, not to this diagram; this section constrains the seams, not the pixels.*

- **Router: hand-rolled hook** porting `router.js`'s semantics — C6 is the spec, and route libraries fight exactly its load-bearing parts (URL-wins-when-present, non-default-only writes measured against *persisted* toggle values, replaceState-in-frame, mount from `document.baseURI`). The pure functions (`parseUrlState`, `serializeUrlState`, `viewFromPath`, `mountFromBaseURI`, `revealCount`, `storedFlagDefaults`) port nearly verbatim to TS with their tests. Revisit a library only if the redesign grows real route depth.
- **MapLibre: direct instance in a ref'd container** (a React island). The map is imperative on purpose — data-driven circle layers over ~25k markers, same-point stack spiderfy, theme swap via `setStyle` + re-add — and a wrapper would fight all of it. The npm `maplibre-gl` dep replaces the CDN classic script; React owns the container, the toolbar, and every overlay *around* the canvas.
- **State: React context + reducers first.** Roster, filters, selection, ack state, and URL sync live in one app-level store shape; adopt an external store (zustand or similar) only if the URL ↔ map ↔ list sync measures painful, with the measurement written down in §11.
- **Styling: Tailwind v4** (CSS-first config), **class-based variant, the dark design as the CSS base** (C10 — light became the visitor default 2026-09-06, without flipping the base), theme tokens as CSS variables so both themes are first-class and the style tile (§6) is the single vocabulary. Bootstrap and `style.css` do not survive; bootstrap-icons is replaced by **Lucide** (ISC, vendored via npm, tree-shaken inline SVGs — never an icon font, never emoji; ratified CRD-M1; the retired font alone is 130 KB of today's baseline, §8). <!-- retired-ok: the ratified styling decision names what it replaced -->

Component sketch (seams, not pixels): `App` (providers: ack, data, router, theme) → `AckDialog` · `Toolbar` (search, grade chips, toggles) · `MapView` (MapLibre island + `HoverCard` + stack fan-out) · `DetailPanel` (history, checklist, grade receipt) · `ListView` (load-more rows) · `AboutView` (terms §06 single-source, live cards) · `Footer`.

## 5. Data layer — the Contract V4 client in React terms

C1 restated as module responsibilities. The three modules whose *semantics* survive verbatim were the old client's `dataClient.js`, `router.js`, and `ack.js` (deleted at CRC, 2026-09-06; in git history) — they ported as TypeScript modules with the same observable behavior, pinned by the ported contract tests (§7).

- **`data/client.ts`** — `createFoodApi` ported whole: manifest-led `loadFullV4`/`loadLite`, static-first finder with R2 fallback, `finder_sha256` + row-count verification, position merge by the shard's own `columns`, closed/standards/detail lazy paths, the detail LRU (200), contract validation on the click path, HTML-200-is-a-miss, and the P4 degrade. The ack gate stays a call-time predicate (`isAcknowledged`), not a module-level flag.
- **`data/presentation.ts`** — the pure derivation layer (`gradeForScore`, `visitsOf`, scope/trend/date math) ports from today's `presentation.js`/`receipt.js`; every displayed judgment keeps deriving client-side from the overlay/detail (C1, C9).
- **React bindings** — one provider owns the loaded roster + manifest and exposes it; hooks select filtered/sorted views. The provider re-runs the load when the ack answer changes (C2's re-load semantics). No fetch happens in any component body: every network read goes through the client module, so the request budget (C3) stays auditable in one file.
- **Vocabulary** — `loc`/`pt`/`scope` decode only via the manifest's `vocab` block (C9); types encode the append-only discipline by never enumerating the codes as a closed union.

## 6. Per-view specs — ratified CRD-M1 (2026-08-29)

The mockups under [`docs/mockups/`](mockups/) are the ratified visual direction (reviewed view by view with Cannon on a throwaway port, 2026-08-29); the specs below are what CRV builds to and CRC verifies on the host. `mockups/tokens.css` is the token vocabulary the style tile ([`style-tile.html`](mockups/style-tile.html)) documents — it becomes the Tailwind v4 `@theme` at CRF. Mockups are direction, not pixel contracts: the spec rows are binding, the mockup settles taste questions the prose can't.

### 6.0 Shared vocabulary — cross-view rules the tile ratified

- **Direction: "slate & signal."** Neutral slate chrome; color is reserved for judgment (the grade ramp) and action. Both themes first-class — the visitor's choice is light · dark · system, with system (the device's appearance) the default since 2026-09-06, the un-prefixed CSS the dark design (C10); dark elevates by border + surface tint (no shadows), light by soft shadow. System font stack; `tabular-nums` on every date, score, ratio, and count.
- **Action color language:** blue = the way to the grades, red = the way to the basic map — dialog buttons, About's status panel, and any future tier affordance all speak it. Filled for the dialog's fork, outlined for standing controls.
- **Grade ramp (mode-invariant):** A `#2f9e44` · B `#94be1b` · C `#f59f00` · D `#e8590c` · F `#a61e1e` · no-grade `#868e96` · NEW `#1c7ed6` · closed `#9aa0a6` · basic-map `#8d939c`. **A color-blind friendly ramp is the visitor's alternative since 2026-09-06 (Cannon's palette; the settings dialog, §6.5): A `#045a8d` · B `#5aa9d6` · C `#f4c245` · D `#f08c3c` · F `#9c4a0c` — a blue → gold → orange → umber diverging scale. Under it NEW is near-black `#212529` (this ramp's A and B are blues, so blue cannot be NEW's) and the declining ring is `#212529` too (red on the umber F is ~1.4:1, and red-vs-orange is the hue difference the mode exists to avoid — the cue becomes "no bright halo," a luminance cue every kind of color vision keeps); the grays are unchanged. Mechanics: `.palette-colorblind` on `<html>` re-points the `--cp-grade-*` / `--cp-new` tokens, and every DOM surface asks for the TOKEN (`gradeColor` returns `var(--cp-grade-x)`); the map's paint and the donut canvas cannot read a token and take the hex from `GRADE_PALETTES` / `NEW_COLORS` / `DECLINE_RINGS` by palette name (the donut id names the palette). Since 2026-09-07 the dots' fill is a paint EXPRESSION over the bucket each feature already carries (`mapLayers.pointFillExpr` — one table with the donut's arcs, `bucketFills`), so a palette switch re-points three layer properties in place and the map data is palette-free: no GeoJSON rebuild, no `setData` — the principle the cluster switch and the declining ring already held. Known, accepted for now: the white grade letter on this ramp's gold C reads at ~1.7:1 (the standard B/C sit at ~2.1); a per-grade ink token is the fix if it matters.** Only F moves from the live site — **darkened, which fixes its worst normal-vision failure** (D↔F ΔE 7.6 → ~15). B stays the lime family by Cannon's call (a teal B validated stronger — deutan 7.0, normal 15.7 — but read as blue-adjacent, and **blue belongs to NEW alone**; decision record: [`mockups/ramp-candidates.html`](mockups/ramp-candidates.html)). Final numbers (dataviz six-checks, all pairs, both surfaces, 2026-08-29): deutan worst ΔE 3.8 (B↔C) · normal worst 14.4 (A↔B, a hair under the 15 bar — accepted). The compensating control is mandatory **wherever a grade is NAMED**: **the letter rides the color** on chips/badges/hover/panel/list rows, everywhere. **The map dots are the exception (Cannon's call 2026-09-06, amending this row):** they wear the color and the ring alone — the letters that rode them past z13.5 are retired, so at deep zoom the B↔C pair (deutan ΔE 3.8) is separable only by hovering the dot, which names the grade in the card. The glance-cost is accepted; the letter is one hover away and every other surface still carries it.
- **Icons: Lucide only, inlined/tree-shaken (npm at CRF), never an icon font, never emoji.** bootstrap-icons retired wholesale at CRC (2026-09-06). <!-- retired-ok: the ratified icon decision names what it replaced -->
- **Badges are a sometimes thing.** Pills are reserved for conditional notes (`≈ approximate location`, NEW). Permanent facts render structurally: kind as icon + word, status as dot + word, canonical actions as real buttons.
- **The external grammar:** a blue-outlined control carrying the box-and-arrow (Lucide `external-link`) means "leaves CleanPlateVA" — the facility-level **Source** button and every per-inspection link-out share it, and nothing else uses it.
- **The trend instrument** (one spec, one component — the panel's framed trend section, which the hover card mounts wholesale via the shared `TrendSection` since 2026-08-30; the compact `card` variant stays specified and tested for future compact surfaces):
  - *Scale:* y is **absolute over the grade domain**, resting at 100→55 so the F threshold is always in frame; when a real score falls below the floor the domain **extends to include it** (thresholds keep their absolute positions and compress upward). A real value is never clipped; the range is never normalized to the data's own min/max. The panel geometry reserves top headroom (bandTop 26, 2026-08-30) so a 100-height mark's label and its hover enlargement stay in frame. Reference renderings: [`mockups/trend-compare.html`](mockups/trend-compare.html) (its pixel pins now carry the +18 headroom offset).
  - *Threshold furniture:* hairlines at 90/80/70/60 with the 60-line dashed in the F color; band labels **A · C · F only**; endpoint dates inside the plot edges (start-anchored left, end-anchored right).
  - *Mark grammar* (ports `sparkline.js` semantics): line + dots = broad scores only, dot filled with the score's grade color; **diamonds** = focused re-checks at their outcome height, outcome-toned, never joined to the line; **filled diamond at the verdict's height** = adjudicated follow-up (all-corrected rides the 100 line); **baseline tick below the band** = scope-unknown. **Every claiming mark prints its label ABOVE itself, always** (Cannon's 2026-08-30 call — the old sparkline's grammar in the new style): the score, the bare X/Y ratio (the claim's " OUT" suffix dropped), or the ✓-glyph; the below-floor outlier's beside-the-mark side label retired with it. Legend: ○ broad score · ◇ re-check · | scope unknown.
  - *Hover:* the hovered mark scales ~1.7× while every other element (marks, line, grid, labels) dims to ~30%, ~150 ms ease both ways; the labels ride their mark's group and dim in the same sweep (the hover-only readout retired 2026-08-30 — the always-on label is the readout).
  - *Panel sizing:* the trend is a labeled, framed section ("Trend · N visits"), full content width, ≥13px axis text — never a decorative micro-spark.

### 6.1 Ack dialog — ratified (mockup: [`ack-dialog.html`](mockups/ack-dialog.html))

**Draws on:** nothing — renders before any data fetch (C2).
**Must show:** the title alone ("Terms of Use and Data Acknowledgment", no kicker/lede); the terms as ONE verbatim document (cloned from About §06's single source) in an inset, recessed scroll box with a **persistent** scrollbar; actions centered, **Decline left (filled red) / Agree right (filled blue)** with the shipped labels; the empty basemap + ghosted brand/search shell behind a scrim.
**Must never show:** any fetched content; a second copy of the terms; a dismiss affordance that reads as a third choice.
**Interaction contract:** blocking on first load, zero data requests until answered (C2/C3) — and nothing else asks the visitor for anything while it is up: the map's auto-locate waits (§6.2, 2026-09-07); Agree → Full, Decline → basic map; Escape = unpersisted decline; `?tier=lite` bypasses entirely. Mobile (<~560px): bottom sheet, stacked full-width actions, Agree on top.
**C-invariants touched:** C2, C3, C8 (formal register, verbatim document).
**Theme:** both, scrim-over-basemap in each.

### 6.2 Map + hover + detail — ratified, 11 passes (mockup: [`map-view.html`](mockups/map-view.html))

**Draws on:** finder + overlay at boot; closed on toggle; detail + standards on click (C1).
**Must show:**
- *Chrome:* ONE floating band — brand · Map/List/About segmented switcher (navigation sits brand-adjacent) · search pill · A–F grade chips · Filters pill · Show closed · counts — that **reflows into the space left of an open panel; the panel never obscures chrome**. A **Settings** pill under the band's left edge (map view only) opens the settings dialog (§6.5) — theme · Group nearby places · text size (2026-09-06). A **layers control** in the map's bottom-right lane, above the zoom and "Find me" buttons — a 29px icon button that expands upward into the basemap choices and collapses on a pick (2026-09-07, Cannon's ask). Footer attribution line per C8.
- *Basemap* (2026-09-07): **Map** — the theme's CARTO style, the shipped default and what a first-time visitor sees — or **Aerial**, the Commonwealth's own VBMP orthoimagery (VGIN's `MostRecentImagery_WGS` cached tile service; Spring 2022/2023/2025, 1-ft GSD with 6- and 3-inch urban upgrades, free of charge and public by the program's terms, CORS open, `max-age=86400`, tiles to z19). **It is one raster layer INSIDE the basemap, never a second style:** inserted under the style's label block, so the photograph replaces the drawn ground while the theme's own labels ride over it with their halos — "aerial with labels", and the theme keeps owning every label, so nothing here has to be kept in step with it. The seam is the layer after the LAST non-symbol one, **not the first symbol one**: dark-matter draws all 66 of its fills and lines and then writes, so the two coincide there, but positron writes `waterway_label` at index 13 and then draws 53 more layers of roads, buildings and boundaries over it — inserting at its first symbol left the photograph under most of the cartography (a white map with imagery in the gaps; caught live 2026-09-07, and the reason the e2e case runs in BOTH themes). Accepted cost of taking the seam late: positron loses exactly one of its 27 label layers under the photo (`waterway_label`, river names, which the imagery shows anyway); dark-matter loses none, and every place / road / POI / house-number label rides over in both. The markers never move — they sit above the label block, so a flip re-stacks nothing, rebuilds nothing and re-sets no source; the choice is the third twin of the theme (`basemap.ts`, `cleanplateva.basemap`, per visitor, never the URL — C6). The imagery's credit rides the SOURCE, so MapLibre shows it exactly while the imagery is drawn and drops it with the layer; the terms document carries the standing formal row.
- *Markers:* grade-colored dots with a ring in every mode (letters rode them past z13.5 until 2026-09-06, when Cannon retired the glyphs — §6.0 amended); gray = basic map/unscored; same-point stacks as neutral count bubbles. **No proximity clusters by default; production's clustering is a visitor SWITCH** (CRP-M6, 2026-09-05) — the CRD-M1 mockup rule was re-ratified the hard way 2026-08-30 (revived at Cannon's ask on the dual track, #174; withdrawn on his live review the same day, #175), and since the cutover deletes the old client, the production look survives as a choice: a switch in the settings dialog (§6.5; it stood bottom-left as its own control until 2026-09-06), public words "Group nearby places," **off** for a first-time visitor, persisted per visitor like the theme and NEVER in the URL or AppState (C6). On, the map draws production's grammar — MapLibre source clustering at radius 40 / tile zoom 12 (bubbles dissolve at camera zoom 13), bubbles SIZED by the places inside (12/16/22px at <10/<50/50+), under dots and stacks, hover = cursor only, click = expansion zoom + 0.5, an absorbed stack's popover closing on the way OUT only — with **Cannon's form for the bubble: a DONUT whose arcs are the grade breakdown of the places it hides** (A–F on the ramp, NEW blue, unscored gray, closed dimmed; every place in the count gets an arc, so the ring totals the number in the hole), around a hole in the stacks' own surface carrying the count in ink, no halo, no outer ring. The basic map's donut is one neutral arc (P6). Flipping re-clusters the live source in place (`setClusterOptions`); the donuts are on-demand images from the missing-image resolver, one per (theme · size · bucket tuple). **Declining = a red ring on the dot** (CRP-M2 2026-09-05, Cannon's pick; retires the CRP-M1 ↓ suffix and its image layer): `#e03131` (`--cp-danger-solid`) at 2.5px in place of the white ring, GRADED dots only ("letter-carrying" when the letters existed), at EVERY zoom the dot is drawn — it never had their zoom gate. Production's form with the color moved off the ramp: production rings in grade-F red, which is invisible on an F fill. **Banded + grade-to-grade since M1b (same day)**: the flag fires only past 5 points of drop, and the delta compares the current adjusted grade to the PREVIOUS era's (second-newest scored broad + its follow-ups) — never raw broad-to-broad.
- *Hover card* (fine pointers only; touch = click): the OLD client's defined zones (re-laid 2026-08-30, preview review; the anchor boxes cut the same day, pass 3 — the hero's broad date and the trend's endpoints already say it) — bordered identity head; the grade hero (circle beside score/100 + "Broad inspection · date", with the old NEW/no-grade variants); then **the panel's §6.0 trend section borrowed wholesale** (shared `TrendSection` — furniture, endpoint dates, mark-grammar legend; its hover interactivity is inert in the mouse-transparent popup) — rendered wholly from the overlay row, zero fetches (C3).
- *Detail panel* (right sheet from `sm` up; **full-screen on phones** — Cannon's call 2026-09-06, replacing the ratified 62vh bottom sheet: any width that would put the panel at the bottom gets the whole screen, the close ✕ at its top-right): header = name/address + **Source** button (external grammar) + close; fact line (kind icon + word · status dot + word) with `≈ approximate location` as the one badge; the **grade hero as a single centered tappable box** — grade circle → score + trend chip (↘ Declining etc.) → anchor line ("Broad inspection · date") → divider → blue "Tap to see Grade breakdown" — the whole box opens the grade breakdown (the report-card modal, which carries the formula story; no separate wordy button); the trend section per §6.0; inspection history as expandable rows.
- *Inspection rows* (the full `inspection.js` inventory): collapsed = outcome badge (score pill / OUT-ratio / adjudicated ✓-chip) · date · purpose · scope badge (suppressed when an adjudication chip shows) · count chips (filled red/green violations box + risk-factor / retail-practice split) · boxed link-out · caret. Expanded = per-violation blocks (severity left-rail, red for risk factors; disposition + `#item` + fixed/repeat flags; **the ↳ per-violation corrective/comment line**); the grouped checklist ("N distinct applicable code items · N published rows · N OUT") with per-category pass-rate bars; the **Inspector comments** block (general + Additional text); Temperatures & sanitizer as its own collapsible.
**Must never show:** grade letters on individual inspections (scores only — the letter is the facility's, C1/C9); judgment content on the basic map (P6); per-hover or per-interaction fetches (C3); "official VDH" framing on any derived number (C8).
**Interaction contract:** hover fetches nothing; click fetches detail (+standards once); URL semantics per C6 (permit in the URL, pushState on select); panel close = Back; band controls write filter state per C6. **The unsolicited auto-locate waits for the acknowledgement** (2026-09-07): the "Find me" control stands from the mount and asks nothing on its own; the one request that can raise the browser's location prompt fires once, the first time the page is ready — the terms answered, or never asked (`?tier=lite`, a returning visitor) — so a first-time visitor never meets a location prompt under the terms dialog (it did, from the mount, since the old client: two consent asks at once, one unasked-for, and Chrome's permission embargo counts the dismissals). Pinned by `use-geolocate.spec.tsx` (the request's timing) and `tests/e2e/ack-then-locate.spec.ts` (the page, end to end). **A selection the map did not make brings the place into view** (2026-09-07; `app/mapCamera.ts`): a shared link, a List row, Back/Forward ease the camera to the facility at neighborhood radius (camera zoom 13, the geolocate landing) — unless it is already in view at that radius or closer, in which case the camera stays; off-screen at a closer zoom, it is eased to at the visitor's zoom. The map's OWN picks (a dot clicked, a stack member picked) never move the camera: the visitor is looking at the place, and a lurch under the click is the one thing this must not do. A `?permit=` link at boot also stands the auto-locate down for that page load (App `arrivedAtPlace`): the visitor came for a place, and a fix would take it away. Before this, a link opened the panel over whatever the camera held — on a cold load, the whole state and 27k dots (C6 read "says what you look at"; it said what you read). Pinned by `map-camera.spec.ts` (the rule) and `tests/e2e/deep-link.spec.ts` (the page: the ease, the panel, its ✕, and the visitor's own click leaving the camera alone). **The layers control** (2026-09-07) opens on click, closes on a pick, on Escape, and on a click anywhere outside it (the map's own dismissal rule, the stack popover's); its entries are a `radiogroup` so the keyboard reaches both without a roving-focus contraption; picking writes `localStorage` and nothing else. Pinned by `basemap.spec.ts` (the preference, including a value a later version might write), `map-layers.spec.ts` (the insertion seam against both CARTO shapes, idempotence, and that a flip touches no marker paint, layout, image or source) and `tests/e2e/aerial.spec.ts` (the page: a real VGIN tile, the seam in both live styles, the theme swap, the credit, and the attribution lane).
**C-invariants touched:** C1, C3, C6, C8, C9 (approximate-location note both tiers), C10.
**Theme:** both; markers re-validated against both basemaps.

### 6.3 List — ratified (mockup: [`list-view.html`](mockups/list-view.html))

**Draws on:** finder + overlay only (the nine fields + `visits`); no new data (frozen contract).
**Must show:** the same band chrome (List active); a dense sortable table — grade chip + score · name over address/city/zip · compliance % · trend (colored arrow + signed delta, em-dash when null) · last visit (date + scope/outcome); sticky headers with the active sort in accent + direction arrow; closed rows muted with a CLOSED word-badge (only when Show closed is on); NEW as the blue-dot word-badge; load-more ("Show 50 more · N match") per D-DATA-11.
**Must never show:** judgment columns on the basic map (name/address/identity only, P6); a pagination scheme other than load-more; columns needing data the overlay doesn't carry (out-of-program).
**Interaction contract:** header click sorts (URL `sort`/`dir` per C6); row click opens the shared detail panel (`permit` in URL); load-more appends 50 and writes `page=N`; narrow widths drop Compliance/Trend columns.
**C-invariants touched:** C1, C6 (sort/dir/page semantics, default sort worst-first score on Full / name on the basic map), C8, C9.
**Theme:** both.

### 6.4 About — ratified (mockup: [`about-view.html`](mockups/about-view.html))

**Draws on:** `manifest.freshness` + `counts` for the live cards; everything else is static content.
**Must show (content parity with the live page, restyled):** the hero with the three-badge **provenance legend** (Official source · Archived snapshot · CleanPlateVA-derived — a color+icon vocabulary reused by every section) + independent-presentation lede + four live cards (snapshot · newest report · places · coverage); **§01 Score anatomy** — weight board with point-block visuals (hollow blocks = repeat), both worked receipts (inspection 100−6−9−3=82 with score ring + banded-grade pill; grade 70→83·B with the not-laundered note), scope cards (20+/1–19/0), the grade scale bar in the ramp with a score marker, the four adjustment cards, the scoring edge cases disclosure; **§02 signals** — four cards (compliance formula · trend demo with unconnected diamond · red flags marked *Heuristic* · open repeats) + the red-flag weight disclosure; **§03 pipeline** — six steps with code chips + the GUID note; **§04 lineage** — official-vs-derived card pair + tier cards with the acknowledged-route line + current-access live card; **§05 limits** — all eight + the Verify-at-the-source card (external grammar); **§06 Terms** — the verbatim single-source body (`#aboutTermsBody` equivalent — the dialog clones it, C2) + the status panel OUTSIDE the cloned body, owning the one tier switch (red outline out / blue outline in; `?tier=lite` states the override, no button).
**Must never show:** a second terms copy; "official VDH rating" framing for any derived number; a terms control in the accepted chrome outside §06 (C2).
**Interaction contract:** `/about#aboutTerms` cold-loads to §06 (footer link target); disclosures expand in place; the status-panel button switches tier per C2's re-load semantics.
**C-invariants touched:** C2, C8 (register + attribution inventory), C9.
**Theme:** both.

### 6.5 Settings dialog — shipped 2026-09-06 (Cannon's call; no mockup — the tokens and §6.1's dialog chrome)

**Draws on:** nothing fetched — four per-visitor preferences in `localStorage` (`cleanplateva.theme` · `cleanplateva.gradePalette` · `cleanplateva.clusters` · `cleanplateva.textSize`) plus `cleanplateva.settingsSeen`.
**Must show:** the title "Settings" ALONE — the one-line lede and the hint under the theme group were cut on Cannon's first look (2026-09-06); **Theme** as a three-way segmented group — Light · Dark · System (Lucide sun / moon / monitor), the band's view-switcher idiom, System the default; **Grade colors** as a two-ramp choice — "Standard" · "Color-blind friendly" — each option showing its own five lettered chips in ITS ramp's literal hex (the band's chip idiom), the current one lit and the other faded like the pictures below (2026-09-06, Cannon's ask; §6.0 carries the ramp and its NEW / declining-ring consequences); **Group nearby places** (heading; hint "Nearby places share one bubble when zoomed out.") as a **two-picture choice** — "Every place" · "Grouped" — each a small drawing of that map state in the map's own vocabulary on the basemap tint (nine grade dots with rings; the same nine with the seven near ones folded into one donut whose arcs are their grade breakdown around a hole carrying "7", beside the two that stay dots), the current state lit with the accent border and the other faded to half (Cannon's ask on the first look, replacing a plain switch; off by default, CRP-M6's behavior); **Text size** as a slider over the body size in px, 12–20 by 1, 14 the default, a live readout ("14 px · default") and a "Reset to 14 px" action off the default; a Done button (filled blue), ✕ top-right. Below `sm` a bottom sheet, as §6.1.
**Must never show:** a decision — no Save / Cancel (every change applies as it is made and persists at once); the acknowledgement's fork; any control that reshapes the roster (those are the band's, URL-carried, C6).
**The hint** (2026-09-07, Cannon's ask): a closeable note hanging under the pill — a tip pointing up at it and the one line "Theme and accessibility options are in Settings." — because the dialog's one self-opening teaches what the choices are and then disappears. **It is the pointer AT the accessibility choices, so it is the one piece of floating chrome that does not whisper** (Cannon's two calls on it the same day): 15px ink at weight 500 — above the 14px body, where every other floating control sits at 11.5–13 and scaling with the visitor's text size like all of them — inside a **2px accent outline the tip carries too** (`border-cp-accent`, the band's own "look here" signal on an active filter pill and a chosen option in the dialog), the tip's fill covering the outline behind it so the whole shape reads as one object over a busy map. It is HELP, not a notification: nothing times it out and nothing animates. It renders behind the dialog on a first visit, is still there when the scrim goes, and stands on every map view (never the documents, which carry no pill) until the visitor closes it OR opens Settings from the pill — either answers the same question, and both persist as `cleanplateva.settingsHintDismissed`.
**Interaction contract:** opens on its own ONCE, on a visitor's first map view after the acknowledgement is answered (never over the blocking §6.1 dialog; `settingsSeen` is written on that opening, so a reload mid-dialog does not ask again); re-opened from the **Settings** pill under the band's left edge, map view only (the List / About documents carry no button; the choices hold on every view); ✕ · backdrop · Escape · Done all close. The theme resolves `system` against `prefers-color-scheme` and re-resolves live on the query's `change`; `index.html` applies the stored theme and text size pre-paint by the same rules (pinned by `theme.spec.ts` / `settings.spec.ts`, which run the script). Text size rides `--cp-text-scale` on `<html>`: every HTML text size in the app is `text-cp-<px>` (a Tailwind functional utility — the ratified px × the scale; `app/theme.css`) and the body is `14px × scale`, so the slider moves the site's text and nothing else — the chrome's px geometry, the map's canvas labels and the trend instrument's SVG are drawings and stand. Radix primitives (`radix-ui`: Dialog · ToggleGroup · Switch · Slider) do the mechanics — portal, focus trap and return, scroll lock, `aria-hidden` on the page behind, roving focus, keyboard stepping — dressed in the theme's tokens.
**C-invariants touched:** C6 (never URL state), C8 (formal register), C10 (the three-way theme).
**Theme:** both; the dialog is the theme's own chrome, so it re-skins live as the choice changes under it.

### 6.6 Edit modes (admin) — acceptance spec (ratified CPE-M0, 2026-09-07; M1 + M2 + M3 shipped 2026-09-08; M4 + M5 — the cannon-food half — 2026-09-09; the arc complete)

*The CPE arc (`c-ground-code/runway/cleanplate-map-editor.md`): `/admin` becomes a session page, and the two public views gain admin-only edit modes. This section is the acceptance spec the milestones ship against — the arc plan, the probes' method and the pull/compose side (cannon-food) live in the briefing. Milestone tags mark what each PR lands: **M1 session + About edit ✅ 2026-09-08** (`src/worker.js` `session`, `app/admin/{session.ts,AdminApp.tsx,AboutEditor.tsx,EditButton.tsx}`, `run_worker_first` widened, D-CPE-1 resolved) · **M2 Map edit ✅ 2026-09-08** (`app/admin/{mapDraft.ts,mapEditController.ts,MapEditor.tsx}`, the Edit pill beside Settings, D-CPE-3 resolved) · **M3 Submit + store ✅ 2026-09-08** (`src/worker.js` `proposals`, `app/admin/mapSubmit.ts`, the `PROPOSALS` binding to the bucket `cleanplateva-proposals`, `PROPOSAL_OPERATOR`; D-CPE-2 and D-CPE-5 resolved, D-CPE-4 opened for M4).*

**Draws on:** nothing new from the frozen contract. The Map edit mode reads the finder row's `permit_id` · `lat` · `lon` · `loc` and, on a drag, the Full-tier detail's `location` block (`lat` · `lon` at 9 dp · `precision` · `source` · `site_lat` · `site_lon` · `site_source` · `site_group_id` · `site_count`) plus `manifest.snapshot_id`; the About edit mode reads the rendered §6.4 document exactly as `/admin` does today. Session state comes from the Access application `CleanPlateVA admin` (`b421f71e-d025-48d2-96be-c64620d8dbbb`, both hosts, the `/admin` prefix) through one Worker route.

**Must show:**
- *The session page (M1)* at `/admin` — still branched in `main.tsx` before the router, still its own lazy chunk: "Signed in as ‹email›" from `GET /admin/api/session`, a **Sign out** control (`/cdn-cgi/access/logout`), links to the Map and About. On a 200 it writes the device flag `cleanplateva.admin.session` (email + expiry). A load that reaches the page IS a session — Access let it through — and the flag is what lets the public views show their controls without asking.
- *The Edit control (About at M1, Map at M2)* — a second pill in the map's top column, right of **Settings** (OQ-A), and on About the same pill at the document's head; rendered ONLY when the device flag is present. In edit mode it reads **Exit edit**. A visitor's page never renders it, never probes, and never loads an editor chunk.
- *About edit (M1)* — today's editor mounted around the LIVE §6.4 document (`app/admin/AboutEditor.tsx`, lazy): pick an element, rewrite its own words, hide it, per-element undo, Reset all, Export. `app/admin/ops.ts` and the export's shape are unchanged; paths re-anchor on AboutView's own root (today they hang off the editor's host div, so every export begins `0/`) and the storage key becomes `cleanplateva.admin.about.v2`, so an old draft cannot mis-apply; the fold recipe in `project_clean_plate_va.md` follows.
- *Map edit (M2)* — entering: proximity clusters forced off for the mode (a cluster cannot be dragged; the visitor's own switch is restored on exit), the visitor's filters kept — the band's "Show mobile food units" flag stays in charge (OQ-E) — the canvas cursor a crosshair, and a banner in the formal register: **"Edit mode. Drag a place to where it is. Nothing is published."** A drag on a dot leaves the dot where it is and draws an orange proposal pin under the pointer with a dashed tether back to the dot (`#ff922b`); a row dragged out of a stack's popover (§6.2 M2) becomes the same pin tethered to the stack point, the popover staying open for the next row — no spiderfy (Cannon, 2026-09-07). A pin on a `loc: 2` place carries the badge **"Moves N permits at ‹address›"** (N = roster rows sharing the row's normalized address) and is a site fix; every other pin is a refinement. A drawer lists the pending pins — name, distance moved (haversine, metres), an optional note, Undo — with **Reset all** and a count badge; pins persist per device as `cleanplateva.admin.map.v1` and survive a reload (OQ-B). Above the list, **Go to a coordinate** (2026-09-09) — a box that takes a pasted decimal pair, latitude first, flies the camera to it **at the zoom already chosen** and MARKS the point; the marked line reads the coordinate back at 6 dp with **Clear**. It proposes nothing, fetches nothing, and holds nothing across a reload: it is somewhere for a drag to aim, so a coordinate that arrived from outside the map can be acted on inside it. **Submit (M3)** — a button in the drawer's foot, "Submit N pins", the draft count beside it ("2 drafts — one per place or stack"), held while any pin still waits for its detail; it posts one draft per stack or lone point (OQ-C — and per snapshot: a pin drafted before a publish goes in a draft that names the older snapshot) and shows each result under the button: **Stored** `‹compact-saved_at›-‹sha8›` · N pins (the names), or **Refused** with the Worker's reason naming the field, or the line that this build cannot store drafts. A stored draft's pins leave the device; a refused draft's stay. A **Submitted drafts** disclosure above the foot lists the bucket's drafts — name · saved time · pins · "Awaiting pull" / "Pulled" — asked once on entry and again after a store; a build without the Worker says so there too. From `sm` up the drawer stops above the map's bottom-right control lane and the lane is lifted over it while the mode is on, so the layers list opens over the drawer's foot rather than under it (OQ-F, D-CPE-5).
- *Session expiry* — an Edit click or a Submit that meets Access's redirect (`opaqueredirect`) clears the flag and says "Sign in at /admin to continue." beside the control, which stays for this page load so the click has a visible answer (the next load renders nothing, as for a visitor). The draft is kept.

**Must never show:** any of it to a visitor (no control, no probe, no chunk — the e2e smoke asserts a visitor's page load makes zero `/admin/*` requests); a moved dot (the record never moves; the pin is the proposal); a decoded `loc` label in a draft (codes are positional identity, §5); the CannonAI editor's vocabulary — no provider candidates, no Google ranking, no accept/reject (those stay in CannonAI's Refinement Editor, which remains the review surface); the visitor word for the tier is "basic map" (C8) — edit mode needs the acknowledged tier and says so if the device declined.

**Interaction contract:**
- The Edit click probes `fetch('/admin/api/session', { redirect: 'manual', credentials: 'same-origin' })` once: 200 → edit mode; `opaqueredirect` → the flag is cleared and the sign-in line shows. `redirect: 'manual'` is load-bearing — `follow` would chase Access's 302 cross-origin and throw. (Both states measured 2026-09-07 — § Ground truth below.)
- Drag: `mousedown` on a point feature (mapHit's pick, §6.2) disables `dragPan`; `mousemove` writes the pin into the `cp-proposals` GeoJSON source (pin + tether); `mouseup` commits. Re-dragging a pin moves it; dropping it back inside its dot's hit slop discards it. A stack row rides window pointer events: nothing happens until the pointer has travelled 8 px (a click on the row stays a click), then the pin is born under the pointer, tethered to the stack point, and `map.unproject` of the canvas-relative position tracks it. The drag fetches the detail — the ONE fetch the mode adds (C3), on the pin's creation, through the click-path client — and attaches its 9-dp `location` block when it arrives; a pin whose detail could not be fetched STANDS and says so in the drawer (D-CPE-3, amended at M2 from "refused": the composer resolves `before` from the archive either way, and a build without the Full tier — CI, the dev server — still edits). In edit mode a dot click opens no panel and a popover row click picks nothing: the mode owns the pointer.
- Go to a coordinate (2026-09-09, `app/admin/mapGoto.ts`): the box parses a decimal pair — latitude, then longitude — tolerating only the wrapping a copy picks up (surrounding parentheses or brackets, extra whitespace, a comma or whitespace separator, a degree sign, an explicit `+`) and refusing everything else BY NAME (`Not a decimal number: 37.5N`; `Paste two decimal numbers — latitude, then longitude.`; the −90..90 and −180..180 bounds). The one shape it could plausibly repair it must not: a pair that is outside the Virginia box but INSIDE it reversed is refused with "That pair reads longitude first" rather than reordered — silently transposing an operator's own numbers in a tool whose output becomes published coordinates is the failure this refusal exists to prevent. On a good parse the camera `flyTo`s the point with a CENTRE and no zoom (`flyTo`, not `easeTo`: it ends where it started on the zoom axis, which is the rule, and it arcs over a cross-state move rather than smearing the map sideways at z19), and the point is drawn on its own source `cp-goto` — a white-cased ring with a hole and a bead on the coordinate, `#ae3ec9`, the map's one free hue (the grades own green→red and, on the color-blind ramp, blue→umber; NEW owns blue; `#e03131` is the declining ring; `#ff922b` is reserved for unsaved judgment). The mark's three circle layers install UNDER the proposal layers on every `style.load`, so a pin dragged onto the target draws inside the ring. Nothing about it enters a hit test, the draft, or storage.
- Submit (as built, M3): `POST /admin/api/proposals`, `redirect: 'manual'`, same-origin, `no-store`, the draft as `application/json`. The Worker verifies the Access JWT's SIGNATURE first — the same check as `session` (RS256 via WebCrypto against `https://cannonwest.cloudflareaccess.com/cdn-cgi/access/certs`, the `kid` matched, `iss` the team domain, `aud` the application's, `exp` in the future; certs cached ~1 h) — and refuses anything else with 401 before the body is read; then the envelope (415 unless `application/json`; 413 over 64 KB; 400 `$: not JSON`); then the shape, every refusal naming its field the contract owner's way (`$.pins[3].after: …`): exactly `contract` · `schema_version` · `snapshot_id` · `tier` · `basemap` · `batch` · `pins` — a draft carrying `operator`, `instrument`, `saved_at` or `submitted_by` is refused as server-owned; `tier` full or lite; `basemap.style` positron · dark-matter · aerial, `zoom` 0..24; `batch.stack_key` the 6-dp key of `group_lat,group_lon` (inside the Virginia box `36.3..39.7 / −83.9..−75.0` — `manual_proposals.VA_LAT/VA_LON`, mirrored), `facility_count` ≥ 1, `site_group_id` 12 hex or null; 1..200 pins, each exactly `permit_id` · `kind` · `name` · `address` · `covers` · `published` · `after` · `note` — the permit id as the roster carries it (VDH's uppercase UUID, or Fairfax's `HFOOD-000009485` / `HFOOD-2020-00039` — both shapes on the 2026-09-08 roster; D-CPE-4), no permit twice, `kind: site` exactly when `published.loc` is 2, `published` standing on the batch point, `after` inside the box and different from `published` at 7 dp, `covers` non-empty and holding the pin's own permit (exactly that for a refinement; no permit covered twice across pins — one permit has one address), `note` null or non-empty ≤ 2,000 chars, `name` ≤ 200. Then it injects the four server-owned fields — `operator` from `PROPOSAL_OPERATOR` (`human:cannon`, a `wrangler.jsonc` var: the contract's handle for the one email the application admits), `instrument: cleanplateva:map-editor@1`, whole-second UTC `saved_at`, and `submitted_by` = the verified `email` claim — and writes canonical bytes (sorted keys at every depth, compact separators, trailing newline, UTF-8 — **measured byte-identical to `manual_proposals._canonical_bytes` on 2026-09-08**, non-ASCII, a backslash and a tab included, so the puller verifies the sha over the bytes it fetched and never re-serializes) to the `PROPOSALS` binding at `drafts/<compact-saved_at>-<sha8>.json` (`filename_for`'s own shape), with `saved_at` · `pins` · `sha256` · `stack_key` · `snapshot_id` · `submitted_by` as the object's custom metadata; **never overwriting** — the same bytes in the same second answer 200 `existing: true` and write nothing; a new draft answers 201 `{ ok, key, sha256, pins, saved_at, existing: false }`. `GET /admin/api/proposals` (HEAD for the headers) lists the `drafts/` prefix newest first — `key` · `name` · the metadata · `size` · `uploaded` · `pulled` — where `pulled` is true when the bucket holds a `pulled/<name>` marker, which `cf_location.py manual pull` writes after composing (the panel's one word for "done"); capped at a thousand (`truncated`). No CORS — same-origin only. The client (`mapSubmit.ts`) reads both answers as the session probe does: an opaque redirect or a 401 is "signed out" (the flag goes, the mode closes, the sign-in line shows, the draft is kept), the SPA shell is "unavailable" (never a store), a 5xx a transient.
- Nothing here writes CouchDB, the location ledger, `/data/*`, `/data-full/*` or the data bucket — the Worker's write surface is the proposals bucket and nothing else. A draft leaves the bucket only through `cf_location.py manual pull` on Cannon's machine (OQ-D: run by hand in a review session), which composes it into `cannon-food.manual-refinement-proposal.v1` / `manual-site-proposal.v1` with `before` resolved from Couch — the browser cannot compose those itself, because the published detail carries no `decision_event_id` / `accepted_at` / `accepted_by`.
- Sign out: `/cdn-cgi/access/logout` on the host, then the flag is cleared.

**The draft — `cleanplateva.map-draft.v1` (FROZEN at M3, 2026-09-08; `app/admin/mapSubmit.ts` builds it, `src/worker.js` checks and completes it):** `{ contract, schema_version: 1, operator, instrument, saved_at, submitted_by, snapshot_id, tier: "full" | "lite", basemap: { style: "positron" | "dark-matter" | "aerial", zoom }, batch: { stack_key, group_lat, group_lon, facility_count, site_group_id | null }, pins: [ { permit_id, kind: "refinement" | "site", name, address | null, covers: [permit_id…], published: { lat, lon, loc, location | null }, after: { lat, lon }, note | null } ] }`. The browser sends everything but the four server-owned fields, which the Worker adds. Against the M0 sketch: `submitted_by` (the identity the signature proved, beside the contract's `operator` handle), `tier` may be `lite` (the basic map still drags; the composer resolves `before` from Couch regardless — D-CPE-3), `name` and `address` (so a draft reads on its own in `data/review/map-drafts/`), and `covers` — the permits the BROWSER counted as moving (the one permit for a refinement, the permits at the address key for a site fix): the composer counts again from Couch, and a disagreement is a reason to stop, not a value to trust. `snapshot_id` is the pin's own (the roster it was dragged against), the map's when a pin has none, and the word `unknown` when neither exists. `batch` is provenance — the stack as judged (`facility_count` the rows shown at the point, never fewer than the pins); the composer recomputes the contract batch from Couch's SITE coordinate and may split one draft into several files (a public stack keys on the EFFECTIVE point, `refine.py` on the site point). `published.location` is the detail's 9-dp block as attached, or null where the build had none.

**Transport (an amendment to §9's diet, M1):** `/admin/api/*` joins `run_worker_first` — the array becomes exactly `["/data-full/*", "/admin/api/*"]`, re-pinned by `worker-cache-contract.spec.ts`. Every such request is a Worker invocation, and every one is gated by Access before the Worker sees it (measured, § Ground truth); the traffic is one operator's, so the visitor's request diet (D-TRANSPORT-4) is untouched.

**C-invariants touched:** C3 (one added fetch, on a drag, admin-only — the visitor waterfall is identical), C6 (edit state is never in the URL; drafts are per device), C8 (formal register on every control and banner), C9 (codes, never labels, in a draft), C10 (both themes).

**Theme:** both. The pill is the band's own chrome; the banner and the drawer use the dialog's surface tokens; the proposal pin and its tether are `#ff922b` in both themes — a color reserved for "unsaved judgment", neither a grade nor an action color.

**Ground truth — probed 2026-09-07 at M0** (don't re-derive; M1's first deploy on the host is the next measurement):
1. *The cookie carries.* From a page at `https://cleanplateva.com/` in a signed-in Chrome, `fetch('/admin/api/session', { redirect: 'manual', credentials: 'same-origin' })` → `type: basic`, 200 `text/html` (the SPA fallback, THROUGH Access), and `/cdn-cgi/access/get-identity` on the same origin → 200 naming the signed-in email. In a cookie-less browser the same fetch → `type: opaqueredirect`, status 0 (Access's 302), while `/about` → 200. On `www.cleanplateva.com`, signed in there, the same two answers — each hostname carries its own application cookie, so the operator signs in on the host being edited. Documented cause: the application cookie is scoped to the domain unless the Cookie Path Attribute is enabled; this application has it off (`http_only_cookie_attribute: true`, `enable_binding_cookie: false`, no path attribute).
2. *Access before the Worker.* An unauthenticated `curl` of `/admin/api/anything` → 302 to `cannonwest.cloudflareaccess.com` on BOTH hosts; `/administrator` → 200 (outside the application, as `route.ts` says).
3. *The header.* Cloudflare: a request that passes Access carries the application token as the request header `Cf-Access-Jwt-Assertion` and, from a browser, as the `CF_Authorization` cookie; validate the header, not the cookie. Keys rotate every 6 weeks and the previous key stays valid 7 days. (The name left the vocabulary tripwire's list at M1 — D-CPE-1; the tier boundary stays the acknowledgement.)
4. *The certs.* `https://cannonwest.cloudflareaccess.com/cdn-cgi/access/certs` answers anonymously: two RS256 keys (`d22f9bba…`, `e17645ca…`) in `keys` and `public_certs`.
5. *The bucket plan.* `cf_publish_full.py` lists and deletes in `cleanplateva-data` alone (`BUCKET`; `list_objects_v2(Bucket=BUCKET)`); a sibling bucket is invisible to it. `wrangler.jsonc` carries one binding today (`DATA_FULL`).
6. *The data* (snapshot `2026-09-05T13:10:35Z`, 29,209 rows): `loc` 28,468 rooftop · 626 street · 70 zip_centroid · 45 venue; 2,670 mobile. Same-point stacks ≥ 2: 1,132 (4,141 members; the largest 84). ZIP-centroid places: 38 in 14 all-centroid stacks, 32 lone — **no stack mixes `loc: 2` with another class**, so a stack is a centroid pile or it is not. M5 candidates: a 6–8-place Richmond stack (1250 E Marshall St · 403 N 3rd St · Richmond International Concourse B) and a centroid pile (Radford 24141 ×5 · Norfolk 23529 ×4).
7. *The canon* (M3, 2026-09-08): a draft stored through the Worker's own code path (a real RS256 token, a Map-backed bucket) came out **byte-identical to `manual_proposals._canonical_bytes`** of its parsed body — 1,017 bytes with `Café Ünïcode “quoted” — dash`, a backslash and a tab in it — and `filename_for(saved_at, sha256)` named the same file the Worker did; `operator`, `instrument` and `saved_at` pass the contract owner's own regexes. The puller can trust the sha over the fetched bytes.
8. *The permit ids* (M3): the 2026-09-08 roster (29,572 rows) carries 25,683 VDH permits as uppercase UUIDs and **3,889 Fairfax permits as `HFOOD-…` in two shapes** (`HFOOD-000009485` ×2,142, `HFOOD-2020-00039` ×1,747). The contract owner's `_permit_id` accepts a canonical UUID alone, so a Fairfax pin cannot be composed under today's contracts — **D-CPE-4**, M4's to resolve; the Worker accepts both shapes so the judgment is at least recorded.
9. *The bucket* (M3): `cleanplateva-proposals` did not exist at the milestone's start (`wrangler r2 bucket list`: `cleanplateva-data` and one unrelated bucket) — created at M3 on Cannon's authorization; `wrangler deploy --dry-run` binds `DATA_FULL`, `PROPOSALS`, `ASSETS` and the three vars.
10. *The route on the host* (M3, deployed 2026-09-09 00:03Z — production's entry chunk byte-identical to the local build, `index-BDYO2nKq.js`; `MapEditor-BiyAVhKJ.js` served at its exact local size, 20,419 B, under the by-name `immutable` rule): unauthenticated `GET` and `POST /admin/api/proposals` → 302 to Access on BOTH hosts, a forged `Cf-Access-Jwt-Assertion` on the POST → 302 too (Access answers before the Worker); `/data-full/manifest.json` still 200 `public, max-age=60`; signed in on the apex, `GET /admin/api/proposals` through Access reached the real Worker with the real binding and answered the empty bucket. **Then the first real POST — Cannon's own drag and Submit, 2026-09-09 00:30:03Z, on the aerial at zoom 19:** ONE draft for a two-place stack at `38.828751,-77.188966` (site group `18e7d867ee58`), 2 refinement pins — TOSOKCHON moved 93.2 m and GJ2 4U 9.5 m, both with the 9-dp detail attached (`fairfax_mar`, `site_count` 2), no notes — stored as `drafts/20260909T003003Z-84bab82b.json`, 1,403 bytes, `operator human:cannon`, `submitted_by` the verified email, `tier full`, snapshot `2026-09-08T05:27:20Z`; the signed-in list showed it (`pulled: false`), and the object read back with `wrangler r2 object get … --remote` (v4 defaults to the LOCAL bucket without `--remote`; `wrangler r2 bucket info`'s `object_count` lagged at 0 for half an hour — the Worker's `list()` is the real-time count) was **byte-identical to `manual_proposals._canonical_bytes`, `filename_for` naming the same file**. And the first real draft was D-CPE-4 made concrete: **both permits are Fairfax `HFOOD-2026-00131` / `HFOOD-2025-00355`, which `_permit_id` refused until M4** widened the rule spine-wide (resolved by chip; Ground truth 11).
11. *The spine, end to end* (M4 + M5, 2026-09-09 — the cannon-food half in cground-skills #1914, #1916, #1917; this site changed only by the publisher's Lite commit `5f5399c`): `cf_location.py manual pull` fetched the draft with Cannon's own bucket-scoped token (12:18Z), kept it byte-identical, and composed it into ONE `cannon-food.manual-refinement-proposal.v1` (`co_located`, `facility_count` 2, `before` from Couch, `after` at `poi`) that `validate` accepted and `show` disposed as two fresh `accepted` pins (93.2 m, 9.4 m); the `pulled/<name>` marker went back to the bucket and **this drawer's Submitted panel read "Pulled"** (Cannon's own Chrome, 12:5xZ). A second draft — Burger King # 9731, a ZIP-centroid place dragged as a SITE fix (12:42Z, aerial at z18.85) — composed into a `manual-site-proposal.v1` and went through the RFE-M5 intake. Bridged (`build` → `promote --apply`, the boilerplate rationale recording instrument · basemap · zoom · save time), merged into one change set (three permits, domain `location`), and published: the site change forced the full rebuild (11m37s), R2 took 9 data + 1 manifest (28 s), the Lite commit `5f5399c` deployed the snapshot `2026-09-09T12:53:05Z` within a minute. **Measured on the host:** the Full tier's three details carry `manual_pin` (the two refinements at `poi` with the MAR site retained beneath; the Burger King's site itself), and the Lite tier's shards by the manifest's names serve both Fairfax rows and the Burger King at the new points on both hosts under the by-name `immutable` rule — one shard answered as the shell at `max-age=0` for a single request seconds after the deploy (edge propagation), the next request served it, which is the window the HTML-is-a-miss rule exists for. Pin to published point: 24 minutes for the second draft. The arc is complete; retro `cannon-food/references/retros/cpe-map-editor-2026-09-09.md`.

### The acceptance-spec template (future views fill this)

```markdown
### <View> — acceptance spec (ratified <date>)
**Draws on:** <which finder/overlay/closed/detail/manifest fields — frozen-contract fields only>
**Must show:** <the content and states the view is required to render, incl. empty/degraded/lite states>
**Must never show:** <C8/C9/P6 guards — e.g. no judgment content on the basic map, no "official VDH score" framing, no shipped grade letters>
**Interaction contract:** <clicks/hovers/keys and what they do; which are URL-writing (C6) and which fetch (C3 — hover fetches nothing, detail on click only)>
**C-invariants touched:** <the §2 rows this view is responsible for upholding>
**Theme:** <light-default + dark parity notes (C10)>
```

## 7. Test strategy

*Resolution from D-CR-TEST-1 (§10).* **Vitest** replaces `node --test` as the runner on the new stack; `tests/support/dashboard.mjs` (the concatenated-source import shim) dies with the module graph it shims. The repo's CI check stays green-defined through every arc — at no point does `main` carry a red or empty suite. <!-- retired-ok: D-CR-TEST-1's resolution names the runner it replaced -->

**Port order — contract tripwires first** (these referee the rewrite itself):

| Today's suite | Disposition |
|---|---|
| `data-client-v4.test.mjs` | **Port first** — pins C1 whole (manifest/shard validation, sha binding, merge, fallbacks, LRU) |
| `lite-roster-contract.test.mjs` | **Port** — pins the committed Lite artifact contract + C5/C9 |
| `routes.test.mjs` · `search.test.mjs` | **Port** — pin C6's URL + search semantics (the pure functions port with their tests) |
| `ack.test.mjs` · `lite-override.test.mjs` | **Port** — pin C2 (versioned key, blocking first load, Escape, `?tier=lite`) |
| `permit-link-tenant.test.mjs` · `mobile-food-units.test.mjs` | **Port** — pin P8 tenant-routed VDH links + `is_restaurant`/`mobile` semantics (C9) |
| `retired-vocabulary.test.mjs` | **Stayed as-is** (already runner-agnostic prose scan) until CRC, then PORTED — `tests/vitest/retired-vocabulary.spec.ts`, the rewrite's retirements appended |
| `worker-cache-contract.test.mjs` | **Untouched until CRC** — the Worker doesn't change; then PORTED — `tests/vitest/worker-cache-contract.spec.ts`, a direct import of `src/worker.js` |
| `grade-receipt.test.mjs` · `narrative-verdicts.test.mjs` · `scope-presentation.test.mjs` · `inspection-counts.test.mjs` · `hover-card.test.mjs` (the visits↔detail parity half) | **Port the pure-derivation halves** — they pin C1/C9 judgment math (`gradeForScore`, verdict presentation, visits parity), independent of markup |
| `dark-basemap-contrast` · `list-layout` · `toolbar-layout` · `marker-hit-slop` · `splitter` · `stack-panel` · `stack-spiderfy` · `about-transparency` · `hover-card` (markup half) | **Superseded by acceptance specs** — presentation pins of the old design; CRV re-pins the new design per §6, and these retire with the old client at CRC |

**Port status (CRF-M1, 2026-08-30):** the six contract rows above are PORTED — `tests/vitest/{data-client-v4, lite-roster-contract, routes, search, ack, lite-override, permit-link-tenant, mobile-food-units}.spec.ts` run against the `app/` modules (behavioral halves; the .mjs originals keep refereeing the served client until CRC, so both runners pin the same contracts). **CRVa-M2 (2026-08-30): the pure-derivation rows are PORTED too** — `tests/vitest/grade-receipt.spec.ts` (the engine-certified fixture suite whole, against `app/data/receipt.ts`), `scope-presentation.spec.ts` (absorbing the narrative-verdicts + inspection-counts behavioral halves), and `visits-parity.spec.ts` (the hover-card suite's visits↔detail half — the old byte-identical-SVG pin re-expressed as trend-LAYOUT equality). The old suites' markup/CSS/source-scan halves are superseded by the CRV component suites (`router-hook`, `map-data`, `toolbar`, `trend-instrument`, `hover-card`, `detail-panel`, `map-hit`) and retire with the old client at CRC.

**The paint proof (2026-09-07):** the component suites above pin the map's pieces against fakes — the layer set against a recording host, the hit test against numbers, the donut codec against strings — and none of them can say whether the map *draws*. `tests/e2e/map-paints.spec.ts` (Playwright, `npm run test:e2e`; third referee in `tests.yml`) runs the PRODUCTION build under `vite preview` in a real Chromium on a software GL and asks the running map, through the `window.__cpMap` handle `MapView` leaves for it (no longer DEV-gated for exactly this reason), whether `food-points` has rendered features, whether the GL canvas holds more than one color (read back inside a `render` frame), in each theme over its own basemap with the resolved theme class in place, and — grouped — whether cluster bubbles render and the missing-image resolver has painted donuts (the canvas path jsdom cannot exercise). It runs on the basic map (`?tier=lite`: no acknowledgement, the committed `public/data/` shards) — the Full families live on R2 behind the Worker, which `vite preview` does not run — and against CARTO live, so a CARTO outage fails it, as it fails production. It is the one test that exercises the maplibre worker file the build copies beside the main chunk (§3; `dist-contract.spec.ts` checks the file exists, this checks it starts). Screenshots are attached to the report for eyes, never pixel-compared: a software rasterizer on a Linux runner does not match a laptop, and a threshold that survives that proves nothing. Not covered, by design: the Full tier's grades on the map, the detail panel, any interaction — those remain Cannon's live review.

**`tests/e2e/aerial.spec.ts` (2026-09-07)** joins it, and earns its place the hard way: the aerial basemap's whole correctness is *where one layer is inserted in someone else's style*, and no fake can answer that. It asserts, against the live CARTO styles in **both themes**, that every layer the basemap DRAWS is under the photograph and every wayfinding label is over it — the case that caught the first cut, which used "the first symbol layer" as the seam and so was right in dark-matter and wrong in positron. It also fetches a real VBMP tile from `vginmaps.vdem.virginia.gov` (a live state ArcGIS server: an outage there fails this suite, exactly as a CARTO outage does), checks a theme swap re-applies the raster on the new style, checks the credit arrives with the imagery and leaves with the layer, and measures the attribution lane at 1024 / 1200 / 1440 so the imagery's credit and the footer chip can never overlap again. That last one is a REGRESSION pin: the longer first-cut credit ran under the chip and lost its own opening word.

## 8. Budgets

*Resolution from D-CR-PERF-1 (§10, Cannon 2026-08-29): the request budget is the only hard gate; there is no wire budget.*

1. **The request budget is identical and hard (C3).** The rewrite adds no per-interaction fetches and no Worker-invoking asset paths. Vendored assets (MapLibre, fonts→SVG icons, all JS/CSS) are first-party static assets — free requests, zero Worker invocations, same as today's `/static/**`. The CDN's 5 requests leave the waterfall entirely.
2. **Asset wire bytes carry no pass/fail.** The baseline below stays as the dated reference point, and CRC still re-measures and *reports* the new build's asset table on the host with the same method — observed and compared, not gated.

**Production asset baseline — measured 2026-08-29** (`curl`, `accept-encoding: br` wire / `identity` raw, fresh visitor, app shell only — Contract V4 data payloads excluded, they don't change):

| Asset group | Requests | Wire (br) | Raw |
|---|---|---|---|
| Shell `/` (index.html) | 1 | 15.0 KB | 60.4 KB |
| `static/css/style.css` | 1 | 27.3 KB | 106.0 KB | <!-- retired-ok: the dated 2026-08-29 baseline -->
| 19 ES modules `static/js/*.js` | 19 | 118.6 KB | 350.5 KB | <!-- retired-ok: the dated 2026-08-29 baseline -->
| Bootstrap 5.3.3 CSS (jsDelivr) | 1 | 33.2 KB | 232.8 KB |
| bootstrap-icons 1.11.3 CSS + woff2 (jsDelivr) | 2 | 143.8 KB | 216.3 KB | <!-- retired-ok: the dated 2026-08-29 baseline -->
| MapLibre GL 5.19.0 CSS + JS (jsDelivr) | 2 | 276.3 KB | 1,093.4 KB |
| **Total** | **26** | **614.2 KB** | **2,053.3 KB** |

Notes for the CRC re-measure: the icons **font** (130.4 KB, incompressible) is the largest non-MapLibre line — inline tree-shaken SVGs replace it outright. Bootstrap's 33.2 KB retires into Tailwind's purged output. MapLibre (~267 KB wire) is the floor no stack choice moves; it merely changes host (CDN → first-party bundle). A same-order React + Tailwind build is *expected* to land under today's numbers; per D-CR-PERF-1 that expectation is reported against, not enforced.

**Re-measured 2026-09-05 — CRP-M5 build on the PREVIEW host** (next.cleanplateva.com; production still serves the old client, so the production re-measure stays CRC-M2's). Same method: `curl`, `accept-encoding: br` wire / `identity` raw, fresh visitor, app shell only — the maplibre worker pair and the brand mark counted because the browser fetches them at boot. No pass/fail (D-CR-PERF-1); reported against the baseline above.

| Asset (M5 build) | Requests | Wire (br) | Raw |
|---|---|---|---|
| Shell `/` | 1 | 1.0 KB | 2.0 KB |
| `index-*.js` (the app) | 1 | 33.0 KB | 104.3 KB |
| `react-*.js` | 1 | 59.2 KB | 189.6 KB |
| `vendor-*.js` (lucide) | 1 | 2.9 KB | 7.3 KB |
| `maplibre-*.js` | 1 | 252.1 KB | 979.2 KB |
| `maplibre-gl-worker.mjs` + `maplibre-gl-shared.mjs` | 2 | 140.4 KB | 508.2 KB |
| `maplibre-*.css` + `index-*.css` | 2 | 16.9 KB | 115.9 KB |
| brand mark `.png` | 1 | 61.7 KB | 61.7 KB |
| **Total, fresh visitor on the map view** | **10** | **567.2 KB** | **1,968.1 KB** |
| *on demand:* `ListView-*.js` · `AboutView-*.js` · `ReceiptModal-*.js` | +1 each, first use | 2.2 · 10.8 · 3.8 KB | 6.2 · 38.4 · 11.7 KB |

Read against the baseline: 26 → 10 requests (all static, 0 Worker — the CDN's five and the icon font are gone); 614.2 → 567.2 KB wire, with maplibre (392.5 KB incl. its worker pair) the floor. The number that changed most is not in the table: before this split every deploy re-shipped the whole 360 KB chunk; after it an app-only deploy re-ships the entry + its CSS, ~40 KB, because `maplibre-*`, `react-*` and `vendor-*` keep their content-addressed URLs and a returning browser's conditional GET for each is a **304 with no body** (measured on the preview host: `If-None-Match` → `304`, 0 bytes downloaded). The host serves hashed assets at `public, max-age=0, must-revalidate` — the platform default, and exactly what production serves the old client's `/static/**` today — so the saving is bytes, not round trips. A long-lived rule for the hashed assets is a follow-on for CRX: **by name, never a splat** (the CPH-M3 lesson — a `/assets/*` splat would stamp `immutable` on the SPA shell a missing chunk name returns; measured 2026-09-05, that miss comes back `200 text/html` at `max-age=0`), and it would move C5's `_headers` pin from "byte-identical" to "publisher rules verbatim + build rules appended". **Done at CRX (D-CRX-1, 2026-09-06, Cannon's call on the measured cost: ~9 bodiless 304s per returning visit, ~75 ms per wave, unmetered):** `vite.config.ts` `buildAssetHeaders` appends one `immutable` rule per emitted hashed asset to the `dist/` copy of `_headers` (10 today; the worker pair stays at `max-age=0`); C5 re-worded in §2; pinned by `dist-contract.spec.ts`; production re-measured below.

**Re-measured 2026-09-06 — the flip, ON PRODUCTION** (cleanplateva.com, deployment `b2c2171f`, [#192](https://github.com/CannonWest/CleanPlateVA/pull/192)). Same method: `curl`, `accept-encoding: br` wire / `identity` raw, fresh visitor, app shell only, the maplibre worker pair and the brand mark counted because the browser fetches them at boot. No pass/fail (D-CR-PERF-1).

| Asset (production, #192) | Requests | Wire (br) | Raw |
|---|---|---|---|
| Shell `/` | 1 | 0.9 KB | 1.9 KB |
| `index-CD3J57gt.js` (the app) | 1 | 33.0 KB | 104.3 KB |
| `react-*.js` | 1 | 59.1 KB | 189.6 KB |
| `vendor-*.js` (lucide) | 1 | 2.9 KB | 7.3 KB |
| `maplibre-*.js` | 1 | 251.9 KB | 979.2 KB |
| `maplibre-gl-worker.mjs` + `maplibre-gl-shared.mjs` | 2 | 140.4 KB | 508.2 KB |
| `maplibre-*.css` + `index-*.css` | 2 | 16.9 KB | 115.6 KB |
| brand mark `.png` | 1 | 61.7 KB | 61.7 KB |
| **Total, fresh visitor on the map view** | **10** | **566.9 KB** | **1,967.9 KB** |
| *on demand:* `ListView-*.js` · `AboutView-*.js` · `ReceiptModal-*.js` | +1 each, first use | 2.2 · 10.8 · 3.8 KB | 6.2 · 38.4 · 11.7 KB |

Read against the 2026-08-29 baseline: 26 → 10 requests (all static, 0 Worker), 614.2 → 566.9 KB wire. The preview-host numbers of 2026-09-05 reproduced line for line — same build, same content hashes (the production shell's entry `index-CD3J57gt.js` is the local build's, and every asset the shell names exists by the same name in a local `dist/`): the flip changed the host, not the bytes. Every hashed asset still answered `public, max-age=0, must-revalidate` (the platform default) at the flip; **since D-CRX-1 ([#196](https://github.com/CannonWest/CleanPlateVA/pull/196), deployed 2026-09-06, measured on production the same hour): every hashed asset answers `public, max-age=31536000, immutable` (`index-*.js`, `maplibre-*.js` checked by name), the shell `/` and the un-hashed worker pair still `max-age=0, must-revalidate`, a missing `/assets/nope-00000000.js` still the shell at `max-age=0` (the splat trap avoided), and the publisher's own rules untouched (shard `immutable`, manifest `max-age=60`)** — a returning visit now re-validates the shell and the two worker files, not ~9 assets. The browser's waterfall also carries Cloudflare's zone-level Web Analytics beacon (`static.cloudflareinsights.com/beacon.min.js` + `/cdn-cgi/rum`) — platform-injected, present for the old client too, not an app asset and not a Worker invocation; excluded above.

## 9. Cutover sketch — what CRC inherits

- **One PR flips production:** `assets.directory` → `./dist`; delete `public/index.html`, `public/static/**`, `tests/support/dashboard.mjs`, and the superseded presentation suites (§7); `app.py` retires. Nothing else in `wrangler.jsonc` moves (C4). **Shipped 2026-09-06 — [#192](https://github.com/CannonWest/CleanPlateVA/pull/192).** <!-- retired-ok: the cutover checklist names what it deleted -->
- **Verified on the host after deploy** (`feedback_measure_host_behavior_not_the_stand_in`): the §2 constitution row by row; the §8 budgets re-measured (fresh-visitor Worker request counts + asset wire table, same method, stamped); the miss paths (`/data/finder/<missing>` → shell at `max-age=0`, HTML-200 = client miss).
- **Embed smoke:** the CannonAI Food tab against the new build — mount depth via `<base href>` rewrite, view tracked across reload, ack once per origin, `?tier=lite` (C7).
- **Retirements appended to architecture-v4.md §14.1, same PR:** Bootstrap 5.3.3 + bootstrap-icons + the jsDelivr CDN as a host; `static/css/style.css`; the 19 no-build ES modules and `public/index.html` as the shipped client; `tests/support/dashboard.mjs`; `app.py` as dev server; `node --test` as the app-suite runner. The site tripwire gains the rows CRC owns. <!-- retired-ok: the cutover checklist names what it deleted -->
- **The dual track retires with the flip:** delete `wrangler.preview.jsonc`, the `cleanplateva-preview` Worker, and its `next.cleanplateva.com` custom domain + DNS (the pre-flip preview, D-CR-STAGE-1 re-resolution 2026-08-30) — production serving `./dist` makes it redundant. **Done 2026-09-06 (CRC-M2).** <!-- retired-ok: the cutover checklist names what it deleted -->

### 9.1 CRC-M0 — pre-flip readiness (2026-09-05, docs only; production untouched)

The remainder of M0 after the 2026-08-30 dual-track detour absorbed the base, the host proof and the worker pin: the list, exactly; the runner; the §2 dry run against the preview; the rollback. Written on the tree after CRP-M3 (#189) and CRP-M5 (#190), the two passes Cannon moved ahead of the flip.

**The flip PR (CRC-M1), file by file.** One PR; merge = deploy (Workers Builds runs `npm ci && npx vite build` then `wrangler deploy`).

- `wrangler.jsonc`: `assets.directory` `./public` → `./dist`. The ONLY change to that file (C4 — `run_worker_first`, the SPA fallback, the Worker, the routes all stay; `worker-cache-contract` keeps pinning the list).
- **Delete — the shipped client:** `public/index.html` · `public/static/css/style.css` · `public/static/img/clean-plate-va-logo.png` (the React client imports its own copy since #187) · the 19 ES modules `public/static/js/{about, ack, app, constants, dataClient, detail, filters, foodDashboard, hover, inspection, list, map, markers, presentation, receipt, router, sparkline, splitter, stacks}.js`. Nothing else under `public/` moves — `public/data/**` and `public/_headers` are the publisher's (C5), `public/data-full/` is gitignored. <!-- retired-ok: the cutover checklist names what it deleted -->
- **Delete — the dev server:** `app.py` · `requirements.txt` (Flask was its only line). Dev is `npm run dev` (Vite, :5620 via the workspace's `cleanplateva-vite` launch entry); the workspace's `clean-plate-va` launch entry (app.py on :5001) goes the same day on the c-ground-code side.
- **Delete — the dual track's config:** `wrangler.preview.jsonc`. The `cleanplateva-preview` Worker and its `next.cleanplateva.com` custom domain stay UP through M2's host verification (the compare target and the safety net), then go by name — `npx wrangler delete --name cleanplateva-preview` needs no config file; deleting the Worker removes the custom-domain route and the DNS record it created. <!-- retired-ok: the cutover checklist names what it deleted -->
- **Delete — the node runner, whole:** `tests/support/dashboard.mjs` and all 23 `tests/*.test.mjs`. Twenty referee the old client and are superseded by the CRV component suites (§7): `about-transparency` · `ack` · `dark-basemap-contrast` · `data-client-v4` · `grade-receipt` · `hover-card` · `inspection-counts` · `list-layout` · `lite-override` · `marker-hit-slop` · `mobile-food-units` · `narrative-verdicts` · `permit-link-tenant` · `routes` · `scope-presentation` · `search` · `splitter` · `stack-panel` · `stack-spiderfy` · `toolbar-layout`. `lite-roster-contract.test.mjs` goes because its Vitest twin (`tests/vitest/lite-roster-contract.spec.ts`, assertion for assertion) already pins the committed artifact. The two survivors PORT to Vitest and their `.mjs` files go with the runner: `retired-vocabulary.test.mjs` → `tests/vitest/retired-vocabulary.spec.ts` (SCAN_FILES drops `app.py`, gains `index.html` · `vite.config.ts` · `package.json` · `tsconfig.json`; SCAN_DIRS gains `app`; the text filter gains `ts`/`tsx`; the CRC rows below appended) and `worker-cache-contract.test.mjs` → `tests/vitest/worker-cache-contract.spec.ts` (a direct `import worker from '../../src/worker.js'` replaces the data-URL import; the passthrough sample path `/static/js/app.js` becomes a hashed `/assets/…` name). `package.json` loses `test:node` and its description's "keeps serving until CRC"; `.github/workflows/tests.yml` loses the `node --test` step (CI = `npm ci` · `npx vite build` · `npx vitest run`); `tsconfig.json`'s "NOT in the program" note goes. <!-- retired-ok: the cutover checklist names what it deleted -->
- **Trim:** `vite.config.ts` `PUBLIC_COPY_EXCLUDED` → `['data-full']` (the `index.html` and `static` entries are moot once the files are gone; the dist-contract "static/** is not copied" test stays as the tripwire against a resurrected `public/static`). The header comments in `vite.config.ts`, `index.html`, `main.tsx`, `README.md` that say "not served until CRC" flip to present tense.
- **`README.md`:** the Architecture / Hosting and local use / `tools/` paragraphs rewritten to the built client (Vite dev server, `npm test`, `dist/` served) — the minimum so the README stops describing a deleted `app.py` and a "no build step"; the deeper prose sweep stays CRX's.
- **`architecture-v4.md` §14.1 rows (tripwire owner: site)** — appended in the same PR, then mirrored as regexes in the ported `retired-vocabulary.spec.ts` (dead-outright strings only; `retired-ok` on the deliberate historical mentions, of which this document's §1 and §8 baseline carry several): Bootstrap 5.3.3 + bootstrap-icons + the jsDelivr CDN as a host (`cdn.jsdelivr.net` · `bootstrap-icons` · `data-bs-theme`) → Tailwind v4 + Lucide, first-party bundle; `static/css/style.css` → `app/theme.css`; the 19 no-build ES modules + `public/index.html` as the shipped client (`static/js/`) → the Vite build from `index.html` + `app/`; `tests/support/dashboard.mjs` → nothing (Vitest imports the modules); `app.py` as dev server (`python app.py`) → `npm run dev`; `node --test` as the app-suite runner → `vitest run`; `wrangler.preview.jsonc` / `cleanplateva-preview` / `next.cleanplateva.com` as the pre-flip preview → production serves `dist/` (retired at M2).

**The runner, decided: Vitest only.** D-CR-TEST-1 resolved "Vitest replaces `node --test`" at CRD-M0; the flip PR consolidates rather than keeping a trimmed `test:node` — the two survivors are runner-agnostic (`node:assert/strict` under a `test` import, exactly how `lite-roster-contract.spec.ts` already runs), so the port is the import line plus paths, and one runner means one CI step and one place a new contract goes. <!-- retired-ok: the cutover checklist names what it deleted -->

**The §2 dry run — walked against next.cleanplateva.com (the M5 build, 2026-09-05).** What M2 repeats on cleanplateva.com the hour of the flip.

| # | Receipt on the preview host |
|---|---|
| C1 | Manifest-led boot: the acked boot fetched `data-full/manifest.json` + 16 `data/finder/*` + 16 `data-full/overlay/*`, nothing else; a facility click fetched its detail + `standards.json` once (+2), a second facility +1, a cached one 0. **HTML-200-is-a-miss:** `/data/finder/zz-000000000000.json` (a name the tree lacks) comes back `200 text/html` at `max-age=0, must-revalidate` — the client's miss path, pinned by the data-client suite. The grade letter is derived (`gradeForScore`) — the overlay carries scores only. |
| C2 | A cleared first load: dialog over the empty basemap, **4 assets and zero data requests** (M3 build; 8 assets on the M5 build), `localStorage['cleanplateva.ack.v1']` unset; Agree → `agreed` and the full boot; Decline → the basic map; Escape = unpersisted decline; `?tier=lite` → no dialog, 17 static, **0** `data-full`, the grade chips gone, the terms control present; the terms are `TermsBody`, one component mounted by About §06 and the dialog. |
| C3 | Basic map **0** Worker requests (17 static) · acked boot **17** Worker (+16 static finder) · hover **0** (the card rendered VCU – Bleecker Street Cafe, A 98/100 + trend, request count unchanged) · first click +2, further +1, LRU 0 · the M5 chunks are static assets (0 Worker). |
| C4 | `/data-full/manifest.json` → `application/json`, `Cache-Control: public, max-age=60, must-revalidate`, `X-Cache: MISS` — the Worker answered, so `run_worker_first: ["/data-full/*"]` is in force; `wrangler.jsonc` last changed at CPH-M3 (`6761d1c`); `workers_dev` / `preview_urls` off. The preview Worker (`wrangler.preview.jsonc`) is the same code, config and R2 binding with a different `assets.directory` — which is the whole flip. | <!-- retired-ok: the dated dry-run receipt -->
| C5 | `/data/manifest.json` on the host sha256-matches `git show origin/main:public/data/manifest.json` (`11e18d0f…`); `_headers` is consumed by the platform, not served (requesting it returns the shell), so its proof is its effect: a finder shard by name → `public, max-age=31536000, immutable`, the manifest → `max-age=60` — and `dist-contract.spec.ts` pins the byte-identical copy of both into `dist/`. |
| C6 | `/list?sort=score&dir=asc`, `/about`, `/?tier=lite` deep links serve the shell and the client lands on the view (titles "List · …" / "About · …"); a list row → `/list?permit=<id>`; Back closes the panel; the router suites re-pin the key set and the search semantics under Vitest. |
| C7 | The CannonAI Food tab serves this checkout's `dist/` over the live `public/` data channels since [CannonAI #338](https://github.com/CannonWest/CannonAI/pull/338) (2026-09-05): `<base href="/cleanplate/">` rewritten, the maplibre worker pair as `text/javascript`, acked boot 1 + 16 + 16 from the two live roots, `?tier=lite` 0 `data-full`, ack once per origin, `/cleanplate/list` deep link, the frame keeps its view across leaving the tab. The flip changes nothing the mount reads. |
| C8 | Footer line on every view: "Inspection records: VDH and Fairfax County Health Department · archived snapshot · Sep 5, 2026 · scores and grades calculated by CleanPlateVA · Terms & attribution"; the basic map's copy never says "Lite"; About §06 carries the terms verbatim. |
| C9 | `loc` / `pt` / `scope` decode via the manifest's `vocab` (pinned: `lite-roster-contract.spec.ts` asserts the frozen `loc` prefix and the sorted `permit_type`); `≈ approximate location` rendered as the panel's one badge on a zip-centroid row (detail-panel suite). |
| C10 | Dark is the document default (`theme-light` is the opt-in class); the theme switch swapped to Positron and back to Dark Matter with the data layers reinstalled both ways and the road-label fix re-applied on dark. |
| C11 | Both tripwires green on every PR of the session (`node-test` job: build · vitest · `node --test`); the ported `retired-vocabulary.spec.ts` inherits the rows and gains CRC's. | <!-- retired-ok: the dated dry-run receipt -->
| C12 | A static SPA build — `dist/` is HTML + JS + CSS + the publisher's data; the only compute at request time is the Worker's R2 read for `/data-full/*` (P2 holds). |

**Rollback, rehearsed.** Because the flip PR deletes the old client, `assets.directory` alone cannot roll back — the files it would point at are gone from the tree. The rollback is `git revert` of the flip's squash commit, as its own PR (the repo never takes a direct push to `main`): `git checkout -b crc-rollback origin/main && git revert --no-edit <flip-sha> && git push -u origin crc-rollback && gh pr create --fill && gh pr merge --squash` — CI ≈ 1 min, Workers Builds ≈ 47 s to serve `./public` again (the old client's files come back with the revert). Until M2 tears it down, `next.cleanplateva.com` keeps serving the new build for compare, and cleanplateva.com is what visitors see either way.

### 9.2 CRC-M1 + CRC-M2 — the flip, shipped and verified (2026-09-06)

**CRC-M1 — [#192](https://github.com/CannonWest/CleanPlateVA/pull/192), squash `7124791`, merged 12:11:23Z.** §9.1 applied file by file: `assets.directory` → `./dist` (the ONLY `wrangler.jsonc` change; three comment pointers in that file now name `app/ack.ts` / `app/router.ts` and the build command), 48 files deleted (the shipped client's 22 · `app.py` + `requirements.txt` · the dual track's config · `tests/support/dashboard.mjs` + 23 `.test.mjs`), the two survivors ported (`tests/vitest/retired-vocabulary.spec.ts` with the CRC rows, `frontend-redesign.md` §11 joining architecture-v4 §13/§14.1 as an exempt range, a missing heading failing loudly · `worker-cache-contract.spec.ts` on a direct `import` of `src/worker.js`, `tsconfig` `allowJs`), CI = `npm ci · npx vite build · npx vitest run` (job `vitest`; the repo has no branch-protection rule keyed on a job name), `PUBLIC_COPY_EXCLUDED` → `['data-full']`, README's hosting/architecture minimum, §14.1's seven rows. **249 Vitest** green (235 + the tripwire + the Worker's 13); the tripwire caught exactly one thing on its first run — the ported Worker spec's own header naming the old runner. The site's `.claude/launch.json` entry became `cleanplateva-vite` (:5620); the three `app.py`-based entries in the workspace launch file went the same hour. <!-- retired-ok: the shipped list names what it deleted -->

**The pre-merge proof, corrected.** The PR-branch Workers Builds check went RED — a non-production branch runs NO build command (§3, measured on this branch's build and on #190's); GitHub Actions (build + 249) and a local `npx wrangler deploy --dry-run` against `dist/` (34 files) were the proof, and Cannon shipped on that. Production: merge 12:11:23Z → deployment `b2c2171f` (version `73369ecc`) at 12:12:07Z — **44 s** — on the main-push path that does build (Building 17 s). The shell's entry `index-CD3J57gt.js` is the same content hash the local build produced.

**CRC-M2 — the §2 constitution ON cleanplateva.com, the hour of the flip** (curl for the headers; Playwright in a fresh browser context per flow, `performance.getEntriesByType('resource')` for the counts; receipts under the flip session):

| # | Receipt on production |
|---|---|
| C1 | The acked boot fetched `data-full/manifest.json` + 16 `data/finder/*` + 16 `data-full/overlay/*`, nothing else; a List row click +2 (`facility/<id>.json` + `standards.json`), a second facility +1, the same facility again 0. **HTML-200-is-a-miss:** `/data/finder/zz-000000000000.json` and `/assets/nope-00000000.js` both answer `200 text/html` at `public, max-age=0, must-revalidate`. |
| C2 | A cleared first load: one dialog (Agree + Decline) over the empty basemap, **8 static assets, 0 `/data/`, 0 `/data-full/`**, `cleanplateva.ack.v1` unset; Agree → `agreed`, the dialog gone, the full boot; Decline → `declined`, the basic map (17 static, 0 Worker); Escape → no dialog, nothing persisted, the basic map; `?tier=lite` → no dialog, 17 static, **0** `data-full`, a terms control present. `/\bLite\b/` against `body.innerText` on the basic map: false. |
| C3 | Basic map **0** Worker · acked boot **17** Worker (+16 static finder) · click +2 · further +1 · repeat 0. Hover 0 is pinned by the component suites and was measured on the preview host at M0 (production markers are canvas-drawn and the DEV-only `__cpMap` hook is absent, so nothing to project against). The only non-app requests in the waterfall are Cloudflare's zone-level Web Analytics beacon (§8) — not a Worker invocation. |
| C4 | `/data-full/manifest.json` → `application/json`, `public, max-age=60, must-revalidate`, `X-Cache: MISS` then `HIT` with the contract Cache-Control re-asserted; `/data/manifest.json` carries no `X-Cache` (static, no Worker). `run_worker_first` still `["/data-full/*"]`, pinned by the ported spec. |
| C5 | `/data/manifest.json` and `/data/finder/00-3c3935925b48.json` sha256-match `git show origin/main:public/data/…` (`11e18d0f…`, `3c393592…`); `_headers`' effect: the shard `public, max-age=31536000, immutable`, the manifest `max-age=60`; `/_headers` itself returns the shell. |
| C6 | `/list?sort=score&dir=asc` → "List · …", 50 rows; a row → `/list?permit=63D42897-…`; Back → `/list`; `/about` → "About · …"; `/map`, `/?tier=lite`, `/list?permit=nope` serve the shell; `www.cleanplateva.com` 200. |
| C7 | The CannonAI Food tab's mount (`127.0.0.1:8080/cleanplate/`, this checkout's `dist/` over the live `public/` data): `<base href="/cleanplate/">`, `document.baseURI` at the mount, the maplibre worker pair `text/javascript`; a fresh visitor → dialog, 0 data; Agree → 16 `/cleanplate/data/` + 17 `/cleanplate/data-full/`; the ack per origin (a reload: no dialog, straight to full); `/cleanplate/list` → "List · …", 50 rows; `?tier=lite` → 0 `data-full`. |
| C8 | The footer on map, List and About: "Inspection records: VDH and Fairfax County Health Department · archived snapshot · Sep 5, 2026 · scores and grades calculated by CleanPlateVA · Terms & attribution"; About carries the terms heading. |
| C9 | Unchanged by the flip — the vocab pins ride in `lite-roster-contract.spec.ts`, green in CI on the flip commit. |
| C10 | `theme-light` absent by default; the bottom-left switch → `theme-light` + `cleanplateva.theme=light`, again → off + `dark`. |
| C11 | CI on #192: `vitest` green (build + 249). The ported tripwire now scans `app/`, `tests/`, the Vite entry and the configs. |
| C12 | `dist/` is HTML + JS + CSS + the publisher's data; **0 console errors, 0 page errors** across every flow above. |

**§8 re-measured on production:** 10 requests / 566.9 KB br / 1,967.9 KB raw — the M5 preview numbers reproduced (the table in §8).

**The dual track, torn down (CRC-M2, Cannon's call at the receipts):** `npx wrangler delete --name cleanplateva-preview` from a config-free directory; the Worker is gone from the account, the AAAA record it owned for `next.cleanplateva.com` went with it (530 from the edge, NXDOMAIN from the resolver), production untouched (shell 200 with the built entry, the Worker channel 200). §14.1's row closes; `wrangler.preview.jsonc|cleanplateva-preview` joins the tripwire (the hostname stays in dated receipts). <!-- retired-ok: the dated record of the teardown -->

**Rollback, now:** `git revert 7124791` as its own PR — the old client's 22 files come back with it; `assets.directory` alone cannot.

**What CRX inherits from here:** the c-ground-code refs/mcps and cannon-food prose that still say `static/js/`, `app.py`, `node --test`, "no build step" or "until CRC" (the cannon-food Tier-0 tripwire's scope — its CRC rows); the deeper README prose sweep; a long-lived `_headers` rule for the hashed `/assets/*` **by name, never a splat** (a design call — it moves C5's `_headers` pin from byte-identical to "publisher rules verbatim + build rules appended"); `project_clean_plate_va.md` re-based to an identity card; this document's §1 status re-based when the program closes; **D-CRX-2** — every PR's Workers Builds check reads red from here on (non-production branches never build, and `./dist` needs a build): turn non-production builds off in the dashboard, or add a repo `build.command` (double-builds on main) — Cannon's call, GitHub Actions is the check to read meanwhile. <!-- retired-ok: the hand-off names the strings CRX must sweep -->

### 9.3 CRX-M0 — the sweep, measured (2026-09-06; docs only)

The full record is [`crx-sweep.md`](crx-sweep.md): method, the pattern × scope counts, every hit classified from its line, and the M1 list. The nine CRC rows (verbatim from the site tripwire) and twelve candidate phrases were run with `git grep -nIP` over the 20,057 tracked files of the cground-skills checkout, this README, and the workspace launch file, with the Tier-0 tripwire's own exemptions applied — 446 records, 216 live, 230 exempt (the memory card, the runway archive, HISTORY, the retros).

**What came back is smaller and sharper than the hand-off expected.** The feared present-tense sentences have **zero live instances** outside the CRX charter (the runway row and briefing name what they sweep — markers at M1, gone at M2); `mcps/**` carries nothing; `references/**` carries one CannonAI-side sentence. The residue proper: **eight stale lines in cannon-food** (the deleted Lite suite named in `operations.md` and the routine; the old client's `presentation.js`, `foodDashboard.js` and `stacks.js` named as the mirror of `TREND_DECLINE_BAND`, `distinctOutItems`, `permitUrl` and `STACK_DP` — all of which now live in `app/data/presentation.ts` and `app/mapData.ts`), **two README sentences** the regexes could not see (the "branch push builds too" claim §3 corrected at the flip; "CPX is the remaining V4 arc"), and the memory card's 19 (M2's). **Two rows must be narrowed** before the Tier-0 mirror can take them: 29 live c-ground-code lines are CannonAI's own `cannonai/gui/static/js/**` and `static/css/style.css`, so the paths are pinned as `public/static/…` plus the 18 deleted modules by name (`app.js` excluded — CannonAI's entry). Two rows are promoted: `lite-roster-contract\.test\.mjs` (exact — cannonwest-me's `links.test.mjs` is live) and `foodDashboard\.js` (dead outright in both projects since CannonAI #300 and the flip). The candidate phrases for bare `Bootstrap` and `requirements.txt` measured to pure noise and are declined; the unbounded `no[- ]build` matched "no building" once — the word bound is load-bearing. <!-- retired-ok: the receipt names the strings the sweep measured -->

### 9.4 CRX-M1 — tripwires extended, hits fixed (2026-09-06)

**The two calls, first.** Cannon's go on both at the M0 boundary: the publisher hotfix landed as its own PR ([cground-skills #1870](https://github.com/CannonWest/cground-skills/pull/1870) — the Lite contract gate now runs `tests/vitest/lite-roster-contract.spec.ts` through the checkout's `node_modules`, proven on the committed `public/data` before the merge: 9 tests, exit 0; the nightly's first post-flip publish is the real proof), and the dead crosscheck tool is deleted in this PR.

**cground-skills half — [#1871](https://github.com/CannonWest/cground-skills/pull/1871).** `cannon-food/tests/test_retired_vocabulary.py` gained the rows as `crx-sweep.md` §4.1 measured them: seven bare CRC rows, the two path rows **narrowed** to `public/static/…` plus the eighteen deleted modules by name (a planted-line check proved each row fires on its own string and not on CannonAI's `cannonai/gui/static/js/app.js` or its stylesheet), two promoted (the deleted Lite suite by exact name; the old orchestrator's file name, dead in both projects), and `visits-crosscheck.mjs`; its trailer records the phrases measured to zero and declined, dated. The eight stale lines were rewritten (the exporter's mirror comment and docstrings → `app/data/presentation.ts` / `app/mapData.ts` / the parity spec; the skill's `permitUrl` pointer; the script-surface companion; the stack census's key pointer; `cannonai-project.md`'s Food-tab sentence — CannonAI #300 had deleted the legacy tab 2026-07-18 while the reference still said "retired-in-place"), nine dated lines took `retired-ok` markers. Tier-0 green in CI. <!-- retired-ok: the receipt names the deleted tool -->

**Site half — this PR.** README: the design-reference callout names both constitutions and the closed programs (no more "CPX is the remaining arc"); the hosting section says what §3 measured — the build command runs on `main` pushes only, a branch's Workers Builds check reads red until D-CRX-2, the PR proof is GitHub Actions plus `wrangler deploy --dry-run`; the parity sentence and the `tools/` paragraph point at `visits-parity.spec.ts`. `tools/visits-crosscheck.mjs` deleted (§14.1 row appended; its regex in the tripwire — it fired on three inventory lines in `crx-sweep.md`, marked); §15's cross-reference row re-pointed. Vitest green (31 files); nothing under `app/`, `src/`, `public/` or `wrangler.jsonc` touched. **M2 is what remains:** the identity card, this document's §1 status, D-CRX-1 and D-CRX-2 to Cannon, the briefing archived. <!-- retired-ok: the receipt names the deleted tool -->

### 9.5 CRX-M2 — the two calls, the re-base, the close (2026-09-06)

**The calls, one chip round, both Cannon's.** D-CRX-1 went the other way from the recommendation on the measured cost (a returning visitor re-validated ~9 assets per visit — 304, 0 body bytes, 72–103 ms each in one wave — unmetered but a whole round-trip wave before the module graph runs): **implemented** in [#196](https://github.com/CannonWest/CleanPlateVA/pull/196) — `vite.config.ts` `buildAssetHeaders`, one `immutable` rule per emitted hashed asset appended to the `dist/` copy of `_headers`, by name, never a splat, the un-hashed worker pair excluded, a hash-less bundle asset failing the build; C5 amended (§2), the pin in `dist-contract.spec.ts`; **measured on production after the deploy** (§8): hashed chunks `public, max-age=31536000, immutable`, the shell and the worker pair `max-age=0, must-revalidate`, a missing name still the shell at `max-age=0`, the publisher's shard/manifest rules untouched. D-CRX-2 as recommended: **"Builds for non-production branches" OFF** in the dashboard, flipped in-session through Cannon's browser at his instruction and confirmed after a reload; #196 was the first PR with no Workers Builds branch check (§3). Both rows in §10.

**The re-base — this PR.** §1 carries the program's status line and the arcs table's Closed column; the `project_clean_plate_va.md` identity card is rewritten to the ≤1.2 KB shape in cground-skills (what / where / status / invariants + pointer; the 15 KB chronology it carried lives here in §11, in architecture-v4 §13, and in git), its MEMORY.md line trimmed, and the runway briefing archived with its HISTORY line. **The CR program is closed.** What this document is from here on: the front-end constitution (§2, §6, §7, §8, §10) that every later change to the site keeps; new decisions get D-CR-* rows, new retirements go to architecture-v4 §14.1 and the tripwire, and a new arc gets its own briefing.

**Two live breaks no tripwire could see (both outside CRX's stated scope; calls for Cannon at the M0 boundary):** `cannon-food/scripts/cf_publish_cleanplate.py:618` still runs `node --test tests/lite-roster-contract.test.mjs` as the Lite contract gate — a file #192 deleted — and `_stream` raises on the non-zero exit, so the first post-flip publish (the nightly at 20:00 local; the last publish was 2026-09-05 09:26) halts at phase `lite_contract` until it names `tests/vitest/lite-roster-contract.spec.ts`: a hotfix on its own PR, not CRX. And this repo's `tools/visits-crosscheck.mjs` imports `public/static/js/presentation.js` and `dataClient.js` by `path.join` — dead since the flip, outside the tripwire's directories, and described as live by the README (lines 246, 342): delete it and point at `visits-parity.spec.ts` (recommended), or port it. <!-- retired-ok: the receipt names the deleted files the two breaks invoke -->

## 10. Open decisions registry — D-CR-*

Every fork the CR runways inherit. **Resolver** = the milestone that must close it; **Status** flips in this doc, same PR. Recommendations are the lead dev's; Cannon decides (via `AskUserQuestion` at the resolving milestone).

| ID | Question | Options | Recommendation | Resolver | Status |
|---|---|---|---|---|---|
| **D-CR-DOC-1** | Design reference name + home | `docs/frontend-redesign.md` (site repo, beside architecture-v4.md) · other name · runway-side doc | `docs/frontend-redesign.md`; site repo; architecture-v4.md's update rule | CRD-M0 | **resolved CRD-M0 2026-08-29 — `docs/frontend-redesign.md`, site repo** (this doc; the runway briefing now cites it) |
| **D-CR-LANG-1** | TypeScript or JS | TS strict · TS loose · JS + JSDoc | TS strict — the data client's decode/merge/fallback paths are exactly where types pay | CRD-M0 | **resolved CRD-M0 2026-08-29 — TypeScript strict** |
| **D-CR-BUILD-1** | Repo + build layout | Vite at repo root, app in `app/`, `publicDir: 'public'` (`data-full/**` excluded), output `dist/`, wrangler `assets.directory` → `./dist` at CRC · build into `public/` in place · separate package dir | First option — publisher write targets (C5) ride publicDir verbatim into `dist/`; `src/worker.js` and its `main` pointer never move | CRD-M0 | **resolved CRD-M0 2026-08-29 — root Vite, app in `app/`, output `dist/`**; `assets.directory` flips to `./dist` only at CRC (§3) |
| **D-CR-BUILD-2** | Workers Builds pipeline | add build command (`npm ci && npx vite build`) to the existing git integration · GitHub Action builds + commits `dist/` | Build command; **count builds/month against the Workers Builds quota at CRF-M0** — every daily data commit becomes a real build | CRD-M0 | **resolved CRD-M0 2026-08-29 — build command on the existing Workers Builds integration**; builds/month counted against the quota at CRF-M0 before relying (§3) |
| **D-CR-BUILD-3** | Local dev + `app.py`'s fate | Vite dev server; `app.py` retires at CRC · keep `app.py` as a parallel fallback indefinitely | Vite dev; `app.py` survives untouched until CRC, then retires → v4 §14.1 | CRD-M0 | **resolved CRD-M0 2026-08-29 — Vite dev server; `app.py` untouched until CRC, then retires** |
| **D-CR-ROUTE-1** | Router implementation | hand-rolled hook porting `router.js` semantics · react-router (library mode) · TanStack Router | Hand-rolled — C6's semantics are the spec, and libraries fight exactly those; revisit only if the redesign grows real route depth | CRD-M0 | **resolved CRD-M0 2026-08-29 — hand-rolled hook**; the pure functions port with their tests (§4) |
| **D-CR-MAP-1** | MapLibre binding | direct MapLibre instance in a ref'd container (React island) · react-map-gl wrapper | Direct — full control over markers/layers/events at 25k markers; the vendored npm dep replaces the CDN classic script | CRD-M0 | **resolved CRD-M0 2026-08-29 — direct MapLibre instance in a ref'd container (React island)** |
| **D-CR-STYLE-1** | Tailwind version + dark strategy | v4 (CSS-first config), class-based dark variant, dark default, tokens as CSS variables · v3.4 config-file | v4; class-dark; both themes first-class per C10 | CRD-M0 | **resolved CRD-M0 2026-08-29 — Tailwind v4, class-based dark variant, dark default**; **the visitor default flipped to light 2026-09-06** (Cannon's call; the CSS base stays dark — `.theme-light` applied by `index.html` + `theme.ts` unless the visitor chose dark; no component uses the `light:` variant, so nothing else moved) |
| **D-CR-STATE-1** | App state | React context + reducers · zustand (or similar) from day one | Context first; adopt an external store only if the URL ↔ map ↔ list sync measures painful, with the measurement written down | CRD-M0 | **resolved CRD-M0 2026-08-29 — context + reducers first**; an external store needs a written measurement (§4) |
| **D-CR-TEST-1** | Test strategy | Vitest; port contract tripwires first, presentation re-pinned per acceptance specs; `worker-cache-contract` untouched till CRC · keep `node --test` | Vitest; port order per §7; CI stays green-defined through every arc | CRD-M0 | **resolved CRD-M0 2026-08-29 — Vitest; port order per §7**; `worker-cache-contract` + `retired-vocabulary` untouched till CRC | <!-- retired-ok: the decision registry quotes the option it rejected -->
| **D-CR-EMBED-1** | CannonAI embed | preserve the iframe contract exactly (Vite `base` relative, router reads `baseURI`, replaceState-in-frame; CRC smokes it) · revisit the embed (out-of-program CannonAI arc) | Preserve; any Food-tab redesign is CannonAI's own arc, not CR's | CRD-M0 | **resolved CRD-M0 2026-08-29 — preserve the iframe contract exactly**; CRC smokes it (C7) |
| **D-CR-STAGE-1** | Where WIP is reviewed | local-only — Vite dev + throwaway-port builds with Cannon; production untouched until CRC · enable preview URLs · second Worker (`cleanplateva-next`) | Local-only — the other two publish WIP to the internet for a single-reviewer program; C4 stays undisturbed | CRD-M0 | **resolved CRD-M0 2026-08-29 — local-only**; production untouched until CRC's one-PR flip. **Re-resolved 2026-08-30 (Cannon) — dual-track preview Worker**: with CRV complete, Cannon called for both clients live side by side to see/compare/tinker pre-flip; `cleanplateva-preview` serves `dist/` at `next.cleanplateva.com` (`wrangler.preview.jsonc`, manual deploys). Production and `wrangler.jsonc` untouched (C4 holds); the preview retires at CRC (§9) | <!-- retired-ok: the decision registry records the option taken -->
| **D-CR-PERF-1** | Budgets | request budget identical (hard, C3) + wire ceiling = §8's measured 614.2 KB baseline, negotiable with evidence at CRC · no wire budget | First — measured, not assumed | CRD-M0 | **resolved CRD-M0 2026-08-29 (Cannon) — NO wire budget** (not the recommendation): the request budget (C3) is the only hard gate; §8's baseline stays a dated reference point, and CRC re-measures and *reports* the wire on the host without a byte gate |
| **D-CR-ARC-1** | CRV single or split | split CRV-a (shell/Map/hover/detail) + CRV-b (List/About/terms/ack chrome) · one CRV arc | Split — matches pace-yourself; each half is one reviewable surface | CRD-M0 | **resolved CRD-M0 2026-08-29 — split CRV-a + CRV-b** (Cannon: two arcs for finer review) |
| **D-CRX-1** | A long-lived cache-control rule for the build's content-hashed assets (§8) | decline — keep the platform default (`max-age=0, must-revalidate`; a returning visit re-validates ~9 assets: 304, no body, ~75 ms per wave, unmetered) · implement — one `immutable` rule per emitted hashed asset appended to the `dist/` copy of `_headers` **by name, never a splat**, C5 re-worded · defer | Decline — the saving is one round-trip wave per returning visit; the cost a permanent second writer to a publisher-owned file | CRX-M2 | **resolved CRX-M2 2026-09-06 (Cannon) — implement** (not the recommendation, on the measured numbers): `vite.config.ts` `buildAssetHeaders` (10 rules today; the un-hashed maplibre worker pair keeps `max-age=0`; a bundle asset without a hash fails the build), `dist-contract.spec.ts` pins the publisher block byte-identical + the appended block = exactly the hashed files, C5 amended (§2), README + §8 updated |
| **D-CRX-2** | Every PR's Workers Builds check reads red since the flip (a non-production branch runs no build command; `./dist` needs one — §3) | turn "Builds for non-production branches" off in the dashboard (Cannon's account setting) · a repo-side wrangler `build.command` (branch uploads build first; `main` double-builds until the dashboard command goes) · leave it red, read GitHub Actions | Dashboard off — branch builds prove nothing and spend build minutes; GitHub Actions is the PR proof | CRX-M2 | **resolved CRX-M2 2026-09-06 (Cannon) — dashboard: builds for non-production branches OFF** (flipped in the dashboard on Cannon's instruction, in-session; §3 records it; the first PR after it is the check) |
| **D-CR-UX-1** | Per-view redesign directions | resolved at **CRD-M1** with mockups in hand, not in prose at M0 | Constraints pinned in advance: C2 ack semantics + Cannon's terms document verbatim · C8 register + "basic map" · C9 labels · red = the way to the basic map, blue = the way to the grades (ratified dialog color language) | CRD-M1 | **resolved CRD-M1 2026-08-29 — all four views ratified in-session** (ack dialog · Map/hover/detail after 11 passes · List · About at full content parity), specs in §6, mockups under `docs/mockups/`. The session also ratified the style tile ("slate & signal"), the grade-ramp change (F darkened; B lime by Cannon's call, §6.0), Lucide-only icons, the badges-are-a-sometimes-thing rule, the external grammar, and the trend instrument (absolute rest-and-extend scale, full mark grammar, hover enlarge+dim). |
| **D-CPE-1** | The Access JWT header's NAME is a retired term (`retired-vocabulary.spec.ts`, the CPF-M2 row — "the tier boundary is the acknowledgement, not Access"), and CPE-M1's Worker must read that header by name for the admin write path (§6.6) | delete the row at M1 with a §14.1 amendment (the header returns for a purpose the CPF retirement never covered; the tier boundary stays the acknowledgement) · keep the row and assemble the name at runtime · mark every live line `retired-ok` | Delete the row. The neighbours stay retired — the hyphenated "gated" phrasing for `/data-full/*`, the `private` cache-control of the Access era, the Worker's old sign-in route — none of them come back | CPE-M1 | **resolved CPE-M1 2026-09-08 — the row deleted** (a comment in its place says why); `docs/architecture-v4.md` §14.1's CPF-M2 row amended; `worker-cache-contract.spec.ts` now pins that the full channel's code never mentions the header and ignores one if sent, while `worker-admin-session.spec.ts` pins the admin route's use of it |
| **D-CPE-2** | Which credential the pull uses for the proposals bucket | re-scope the publisher's R2 token (`~/.r2-cleanplateva.env`) to both buckets · a second token and env file for `cleanplateva-proposals` alone | Second token — the publisher's credential keeps its one-bucket blast radius | CPE-M3 | **resolved CPE-M3 2026-09-08 (Cannon) — a second token**, scoped to `cleanplateva-proposals` alone in an env file of its own, created at M4 when `pull` first needs it; the publisher's credential keeps its one-bucket blast radius |
| **D-CPE-3** | A map drag's detail fetch (the 9-dp `location` block): M0's spec refused the pin when the fetch failed | refuse the pin · attach the detail when it arrives, the drawer saying when it did not | Attach — the composer (M4) resolves the contract's `before` from the archive regardless, so the block is provenance, not authority; CI and the dev server have no Full tier, so a refusal would make the mode untestable there and unusable off the host | CPE-M2 | **resolved CPE-M2 2026-09-08 — attach**; §6.6 amended, the drawer's line is "Site detail was not available in this build; the archive supplies it on review." |
| **D-CPE-4** | Fairfax permit ids are not UUIDs — `HFOOD-000009485` / `HFOOD-2020-00039` (3,889 rows on the 2026-09-08 roster, §6.6 Ground truth 8) — and `manual_proposals._permit_id` admits a canonical uppercase UUID alone, so a Fairfax drag cannot be composed into either manual-pin contract as they stand | widen `_permit_id` to the roster's shapes (a cannon-food contract amendment, its own tests; the bridges then need to resolve a Fairfax permit in Couch under `jurisdiction_source: fairfax`) · `pull` fails closed on a Fairfax pin with the reason, until the county's location path is designed · the Worker refuses Fairfax pins at Submit | Fail closed at `pull`, with the reason — the Worker records the judgment (accepted at M3), the contract stays honest, and the county's location ladder is a design of its own | CPE-M4 | **resolved CPE-M4 2026-09-09 (Cannon, by chip) — widened spine-wide**: `cannon_food/location/permit_ids.py` is the one rule (VDH's uppercase UUID or Fairfax's `HFOOD-…`), imported by the ledger's event and record validators, the proposal contract, the bridge and the provider pipeline; schema versions unchanged. The first real draft's two Fairfax pins composed at M4 and were promoted (`a14ec8af…`, `bec37fa2…`) and published at M5 — Ground truth 11 |
| **D-CPE-5** | The edit mode's drawer (`fixed top-3 right-3 bottom-3`) covered the map's bottom-right control lane — the layers button (#220), Find me, the zoom pair — so in edit mode they were pointer-unreachable (keyboard only; the #220 e2e flips the basemap by focus + Enter for that reason). Raised as OQ-F at the #220 merge | stop the drawer above the lane from `sm` up and lift the lane over the drawer while the mode is on · lift the lane's controls over the drawer alone · accept keyboard-only | The first — measured on the production build, the lane is 190 px tall at 1024, 1280 and 1440 wide, so the drawer ends 202 px above the bottom (the lane + its 12 px gutter); with the lane at z 40 the layers list, which opens upward, draws over the drawer's foot instead of under it | CPE-M3 | **resolved CPE-M3 2026-09-08 (Cannon) — as built**: `MapEditor.DRAWER_BOTTOM` (`sm:bottom-[202px]`) + `theme.css` `.cp-map-editing .maplibregl-ctrl-bottom-right { z-index: 40 }` (MapView sets the class in edit mode); measured after the change at the three widths: a 12 px gap, every lane control and the open layers list hit-testing as the lane |

## 11. History

| Date | Change |
|---|---|
| 2026-09-09 | **Go to a coordinate — the edit mode's navigation aid (this PR; Cannon's ask, three calls by chip: mark the point · keep the zoom · decimal pairs only).** The CPE arc stays closed; this is a dated addition to a shipped mode, not an M6. `app/admin/mapGoto.ts` (pure): a parse that takes a decimal pair latitude-first, tolerating only the wrapping a copy picks up (parentheses, brackets, whitespace either side or as the separator, a degree sign, an explicit `+`) and refusing everything else **by name** — including the one shape it could plausibly repair, a Virginia pair pasted longitude-first, which is refused rather than transposed (silently reordering an operator's numbers in a tool whose output becomes published coordinates is the failure the refusal exists to prevent); plus the mark's source, its GeoJSON and its three circle layers. `MapEditor.tsx` gains a **Go to a coordinate** section above the pin list — the box, **Go** (held while empty), a refusal line that clears as the operator types, and a `Marked 37.538126, -77.566542 · Clear` line at the roster's own 6 dp. On a good parse the camera `flyTo`s a CENTRE and no zoom (`flyTo`, not `easeTo`: it ends where it started on the zoom axis, and it arcs over a cross-state move rather than smearing the map at z19). The mark is `#ae3ec9` — the map's one free hue, the grades owning green→red and blue→umber, NEW blue, `#e03131` the declining ring and `#ff922b` reserved for unsaved judgment — a white-cased ring with a HOLE plus a bead on the coordinate, installed on `cp-goto` UNDER the proposal layers on every `style.load`, so a pin dragged onto the target draws inside the ring. It proposes nothing, fetches nothing, enters no hit test and survives no reload. **Verified on the production build** (Playwright, aerial, z16): centre `37.53812556551331, -77.56654203147326` against a paste of Cannon's 14-dp pair, **zoom unchanged to six places**, one mark drawn, `Marked 37.538126, -77.566542`, pin count 0; the reversed pair refused with the camera standing; Clear taking the mark off the source. Also found and left alone: the stack-row drag (M2) works as specified — an agent screenshot taken before the paint had read as a miss. **441 Vitest / 48 files** (+15: the parse's accepted and refused shapes with the reversed-pair guard and the range edges, the 6-dp read-back, the source data, the three layers' order · paint · theme casing · the free hue; the controller's mark-under-proposals order, `setTarget` and the clean dispose of both sources; the drawer's fly-with-no-zoom, the refusal that does not move the camera, Clear, and Go held while empty), **29 Playwright** (+1 on the production build), `tsc` clean. Bundle: `MapEditor-*` 20.4 → 24.4 kB raw (lazy — a visitor loads none of it), `index-*` 122.1 kB unchanged. |
| 2026-09-09 | **CPE-M4 + M5 recorded — the arc complete (this PR; docs only).** The cannon-food half is cground-skills [#1914](https://github.com/CannonWest/cground-skills/pull/1914) (`cf_location.py manual pull`: drafts composed into the manual-pin contracts with `before` from Couch, fail-closed per pin, idempotent by `pulled.jsonl` + the bucket's `pulled/<name>` marker; D-CPE-4 widened the permit-id rule spine-wide), [#1916](https://github.com/CannonWest/cground-skills/pull/1916) (the live pull of Cannon's first draft) and [#1917](https://github.com/CannonWest/cground-skills/pull/1917) (the first batch promoted — two Fairfax refinement events, the Burger King site fix — and the bridge's boilerplate rationale for noteless pins); this site changed by the publisher's Lite commit `5f5399c` alone. § 6.6's header says M1–M5; Ground truth 10's last sentence is no longer stale and Ground truth 11 records the chain measured on the host — the drafts pulled and composed, the panel reading "Pulled", the publish (full rebuild forced by the site change; R2 9 data + 1 manifest; the Lite snapshot `2026-09-09T12:53:05Z` deployed within a minute), the three places at their new points on both tiers of both hosts. D-CPE-4 resolved. Retro: `cannon-food/references/retros/cpe-map-editor-2026-09-09.md`. |
| 2026-09-08 | **CPE-M3 — Submit + store (this PR; the bucket `cleanplateva-proposals` created in-session under Cannon's wrangler OAuth on his authorization).** `src/worker.js` gains `/admin/api/proposals`: the same signature check as `session` FIRST (401 before the body is read), then the envelope (415 unless `application/json`, 413 over 64 KB, 400 for non-JSON), then the shape — every refusal naming its field the contract owner's way (`$.pins[3].after: …`) — then the four server-owned fields injected (`operator` from the new `PROPOSAL_OPERATOR` var, `instrument: cleanplateva:map-editor@1`, whole-second `saved_at`, `submitted_by` from the verified claim) and canonical bytes put at `drafts/<compact-saved_at>-<sha8>.json` in the new `PROPOSALS` binding, NEVER overwriting (the same bytes in the same second → 200 `existing: true`, no write); `GET` lists the prefix newest first with the put's custom metadata and a `pulled` flag read from `pulled/<name>` markers (M4's `pull` writes one after composing). `wrangler.jsonc`: the second bucket (its own, because the publisher deletes strays from `cleanplateva-data`), the var. **The canon measured, not assumed:** a draft stored through the Worker's own code path came out byte-identical to `manual_proposals._canonical_bytes`, `filename_for` naming the same file (§6.6 Ground truth 7). `app/admin/mapSubmit.ts` (pure): `buildDrafts` — one draft per stack or lone point AND per snapshot, each pin in the contract's shape (`name` · `address` · `covers` added to the M0 sketch, the device's bookkeeping dropped, the batch's count never fewer than its pins), `submitDraft` / `listSubmitted` reading the route the way the session probe does (an opaque redirect or a 401 = signed out; the shell = unavailable, never a store; a 5xx = a transient). `MapEditor.tsx`: "Submit N pins" + the draft count in the foot, held while a pin waits for its detail; a result line per draft — Stored `name` · pins, Refused with the reason, or "this build cannot store drafts"; a stored draft's pins leave the device; Access's redirect on the way hands the session loss up (`App` `sessionLost`: the flag cleared, the mode closed, the sign-in line, the draft kept); a **Submitted drafts** disclosure above the foot listing the bucket (Awaiting pull / Pulled), asked on entry and after a store. **OQ-F → D-CPE-5, resolved as built (Cannon):** the drawer stops 202 px above the bottom from `sm` up (`DRAWER_BOTTOM`; the lane measured 190 px at 1024/1280/1440 + the 12 px gutter) and `.cp-map-editing` lifts the lane to z 40 so the layers list opens over the drawer's foot — measured on the production build after the change: a 12 px gap at all three widths, every control and the open list hit-testing as the lane. **D-CPE-2 resolved** (a second token, at M4). **D-CPE-4 opened:** 3,889 Fairfax permits are `HFOOD-…` in two shapes, not UUIDs, and `manual_proposals._permit_id` admits UUIDs alone (Ground truth 8) — the Worker accepts both; M4 decides. The M2 placeholder "Submit is not available in this build." is retired (§14.1 + the tripwire). **Tests:** `worker-admin-proposals.spec.ts` (a Map-backed bucket with a cursor, and the token helper now shared as `tests/vitest/support/access.ts` with the session spec: 401 before the body; the canonical put + its metadata; never overwriting; the server-owned refusal; 36 shape cases by field; both Fairfax shapes and a multi-permit site fix; 413 / 415 / 400 / the three 500s; a bad token on the write path; the list with markers, HEAD, an empty bucket, the cursor walk, 405; the write surface pinned at the source — no `PROPOSALS` in the full channel, no `DATA_FULL.put`, the two bindings and the var in `wrangler.jsonc`, the Virginia box), `map-submit.spec.ts` (the drafts' shape, the grouping by point and by snapshot, the site fix's covers, the basemap word, the zoom clamp, both transports against every answer), `map-editor.spec.tsx` (Submit held at zero and while a detail is pending, two drafts posted and cleared with the result lines and the panel, a refusal kept beside a store, the redirect handed up with the draft kept, the shell refused and the run stopped, `DRAWER_BOTTOM`); e2e `map-edit.spec.ts` +2 on the production build with real mouse events — a drag → Submit against Playwright's storing route: the POSTed draft asserted field by field (no server-owned field, the stack key, the published point and `loc`, `after` moved, `covers`, `tier: lite`, the basemap word, the zoom), the pin gone from the device, the result line, the Submitted panel's count and row; the SPA shell on the route → not stored, the pin and the draft stay. **424 Vitest / 47 files, 28 Playwright, `tsc` clean, `wrangler deploy --dry-run` binds `DATA_FULL` · `PROPOSALS` · `ASSETS` and the three vars** (Worker upload 20.2 KiB). Bundle: `MapEditor-*` 12.7 → 20.4 kB raw (lazy), `index-*` 122.1 kB raw. The host measurement follows the deploy (§6.6 Ground truth 10; Cannon's own drag and Submit). |
| 2026-09-08 | **A swappable aerial basemap behind a layers control (this PR; Cannon's ask 2026-09-07 after a read of VGIN's base-mapping / orthoimagery program and its ArcGIS services; parked as a draft the same day, pending VGIN's word on public use — Gerard Bernhardt, the VBMP program manager, answered 2026-09-08: public, with proper credit to the Virginia Base Mapping Program; merged with the map credit naming the program, and rebased over CPE-M1/M2 and the geolocate-note deletion).** `app/LayersControl.tsx` — a 29px icon button in the map's bottom-right lane that expands upward into **Map** ("Streets and place names", the shipped default) and **Aerial** ("Virginia aerial photography"), the chosen entry wearing the accent border the band uses for "this one", collapsing on a pick / Escape / an outside mousedown. It is a MapLibre `IControl` whose element React fills through a **portal**, so the state stays in React with no second root to unmount; it is added **last** in `bottom-right` and that is what puts it on top — MapLibre PREPENDS into a bottom corner (added first it sat under the zoom buttons; measured). It is the one presentation choice that did NOT go into the settings dialog: a basemap is what the map is drawn ON, flipped while looking at a place. **The aerial is one raster layer inside the basemap, never a second style** — `applyBasemap` (mapLayers.ts) adds VGIN's `MostRecentImagery_WGS` cached tile service (`/tile/{z}/{y}/{x}`, EPSG:3857, 256px, `maxzoom: 19`) under the style's label block, so the theme keeps owning the labels and the markers never restack; the same function runs on every `style.load` and on the visitor's flip, so there is no install/update pair to drift. `basemap.ts` is theme.ts's third twin (`cleanplateva.basemap`, unset or unrecognized = `map`, never the URL — C6). **The bug worth naming, caught live and not by a test:** the seam was first taken at the style's FIRST symbol layer, which is right for dark-matter (all 66 fills and lines, then all text) and wrong for positron (`waterway_label` at index 13, then 53 more drawn layers) — light theme rendered as a white map with imagery showing through the gaps. The seam is now the layer after the LAST non-symbol one; both styles end their drawing at `boundary_country_inner`, and the rule finds that edge without naming it. Accepted cost: positron loses one of its 27 label layers under the photo (`waterway_label`), dark-matter none. Our own layers are excluded from the search — the dots are CIRCLES, and counting them would push the seam to the top of the style and slide the photograph over the map. **Merging over CPE-M2 (2026-09-08) found the same failure from the other side:** the edit mode's tether (line) and pin (circle) layers sit ABOVE the markers, and a live flip that re-scanned the style took the pins for the basemap's last drawn layer — the photograph over the dots. `MapView` now measures the seam once per style at `style.load`, on the pristine style, and hands it to every flip (`applyBasemap(map, basemap, seam)`); the scan is the fallback for a seam the style no longer has. Pinned by three cases in `map-layers.spec.ts` (one keeps the defect as a test, so the reason survives) and, on the production build with the real mode, by a case in `tests/e2e/map-edit.spec.ts`. **The credit rides the source** so MapLibre shows it exactly while the imagery is drawn — `Imagery © Virginia Base Mapping Program (VGIN)`, the program by name, which is VGIN's condition for public use (a first cut said `© VGIN` alone, deliberately short, and was lengthened on their answer); the formal row — VBMP orthoimagery, © VGIN, Virginia Department of Emergency Management — joins the terms document's attribution list (`TermsBody.tsx`). The footer chip shares the bottom lane with that credit: the first cut moved the chip's stop 360 → 440px while the aerial was on, sized to the short credit, and the named credit outgrew it — so while the aerial is on the chip now **lifts one row above the strip** (`sm:bottom-[38px]`, the row the sub-`sm` rule already uses) with only the control lane as its stop, and no width of credit can reach it; the drawn map keeps its measured stop on the line. **Measured 2026-09-07:** the tile service reflects any `Origin` (`Vary: Origin`) and serves `Cache-Control: max-age=86400`, 12–23 KB a tile from z9 to z19 and 404 past 19 (its LODs claim 23 — hence the explicit `maxzoom`); the aerial lands at index 66 in Dark Matter (before `waterway_label`) and 67 in Positron (before `watername_ocean`), under `food-points` at 95; the attribution strip was 417px with the short credit and is 598px with the program named; the lifted chip sits 4px above it at 700, 1024, 1200 and 1440 (a 440 stop would have overlapped it by 168px at 1024 and 1200). **395 Vitest, 45 files** on the merged tree (+19 from this PR, the three seam-in-edit-mode cases included: `basemap.spec.ts` — the default, an unknown value degrading to it, private mode, and that no public word is "Satellite"; `map-layers.spec.ts` gains both CARTO shapes as fixtures, the seam, idempotence, the teardown order, and a pin that a flip touches no marker paint, layout, image or source). **16 Playwright, all green** (+10: `tests/e2e/aerial.spec.ts` — a real VGIN tile, the seam in both live styles, a theme swap re-applying the raster, the credit arriving and leaving, the lane at three widths, and the choice surviving a reload without touching the URL). `tsc` clean outside the pre-existing `tests/e2e` typings. Bundle: `index-*` 115.1 → 118.9 KB raw, CSS 38.0 → 38.1 KB; no vendor chunk moved (no new dependency — maplibre already ships raster sources). |
| 2026-09-08 | **The geolocate notes deleted (this PR; Cannon's call).** The card that hung under the band on a located fix is gone, both messages with it: the out-of-coverage note (“This map only covers Virginia, and you’re outside it.”) plus its **Back to Virginia** button, and the failure note (“check that location access is on”). `useGeolocate` loses the `geolocate` and `error` listeners, `note` / `dismissNote` / `backToVirginia`, and the coverage math the first was measured against — `coverageBounds` / `withinCoverage` / `COVERAGE_PAD`, the padded bounding box of the **filtered** roster, which is why the hook took a roster ref at all; it now takes nothing and holds no state, so `MapView` sheds `facilitiesRef` and the `X` import along with the card. Scope was Cannon's: asked whether the failure note should survive its sibling, the answer was the whole note UI. What stays: the control, the patient auto-locate, and the follow lock (`release`), which `mapCamera` still needs. Behavior now — a fix in Ohio and a fix the browser never returns read the same, the map holding its camera and saying nothing. `use-geolocate.spec.tsx` drops the coverage-math and note specs and gains one that pins the SILENCE (the registered handler set is exactly the four follow-lock events; `fitBounds` is never called from inside the hook), so a regrown listener is a red test; the strings join §14.1 and the tripwire. |
| 2026-09-08 | **CPE-M2 — the map's edit mode, no submit (this PR).** The Edit pill sits right of Settings in the map's top column (OQ-A; one pointer-transparent row so the hint still points at the gear), for a device holding the session flag; an Edit click probes once, closes an open panel (the mode owns the right side) and mounts `MapEditor` — its own lazy chunk — over the live map. Entering forces proximity clusters off for the mode (the visitor's switch untouched, back on exit) and keeps the visitor's filters (OQ-E). `app/admin/mapEditController.ts` owns the map side, typed structurally and driven against a recorder in `map-edit-controller.spec.ts`: one source `cp-proposals` and three layers — dashed tethers under `#ff922b` pins under site badges — reinstalled on every `style.load` after MapView's own; `mousedown` on a dot (mapHit's reach, the dot's radius) starts a pin on the published point and `preventDefault()`s the map event so MapLibre does not pan, `mousemove` moves it, `mouseup` drops it or DISCARDS it within the dot's reach (slop + dot radius + pin radius) — a click discards; a drafted pin is picked up again from its own layer; a release outside the canvas drops where the pin last was; a stack-popover row (`StackPopover` `onRowPointerDown`, wired by MapView in edit mode only) rides window pointer events with an 8 px click-stays-a-click threshold, then `unproject`s the canvas-relative position. `app/admin/mapDraft.ts` is the pure model: `loc: 2` → a SITE fix covering every permit at the address key (address folded + ZIP), else a refinement; the badge "Moves N permits at ‹address›"; haversine metres; the per-device draft `cleanplateva.admin.map.v1` (a `pending` detail reads back `unavailable`); the GeoJSON — tether then pin per proposal, badge on the pin, empty for a refinement. `MapEditor.tsx` renders the banner ("Edit mode. Drag a place to where it is. Nothing is published.") and the drawer — count, per pin the name · kind · distance · site badge · detail note · stale-snapshot note · a note field that persists as typed · Undo — with Reset all, Exit edit, and the line "Submit is not available in this build." The detail is fetched per new pin and attached when it arrives (**D-CPE-3** — attach, not refuse). In edit mode a dot click opens no panel and a row click picks nothing. **Tests:** `map-draft.spec.ts` (kind · address key · covered permits · the pin's shape · badge · haversine · discard reach · the list ops · the store's round trip, empty-clears, pending→unavailable, garbage, private mode · the drawing), `map-edit-controller.spec.ts` (layers + order + idempotence + reinstall + dispose; the dot drag start/move/drop/discard/refuse/right-button/re-pickup/window-release; the row drag threshold/birth/drop/discard/one-at-a-time), `map-editor.spec.tsx` (banner, empty drawer, a stored row, note persistence, Undo, Reset all, the stale note, the map wiring); `dist-contract` pins `MapEditor` lazy; e2e `tests/e2e/map-edit.spec.ts` on the production build with REAL mouse events: a visitor's map shows no pill and makes zero `/admin/*` requests · a dot dragged 90/60 px → 1 pin, its row named, metres > 0, source ≥ 2 features (pin + tether), the draft survives a reload, Undo clears it · a 3/2 px drop is discarded and a dot click opens no panel · a row dragged out of a stack's popover → the member's pin, the popover still open · a ZIP-centroid place → "Site fix" + "Moves N permits at". |
| 2026-09-08 | **CPE-M1 — the session model and About's edit mode (this PR).** `src/worker.js` gains the admin API: `GET /admin/api/session` reads the token Access forwards, verifies its RS256 signature against the team's published keys (`kid` → the JWK, keys edge-cached 1 h via `cf.cacheTtl`), then `iss`, `aud`, `exp`, and answers the operator's email `no-store` — 401 for no or a bad token, 503 when the keys are unreachable (a transient the client never reads as "signed out"), 404/405 for the rest; the application's team domain and audience ride as `wrangler.jsonc` `vars`. `run_worker_first` is exactly `["/data-full/*", "/admin/api/*"]` (`worker-cache-contract.spec.ts` re-pinned; the full channel's code is pinned to read no header and to ignore one if sent). `app/admin/session.ts`: the device flag `cleanplateva.admin.session` (`{ email, exp }`, expired flags read as absent) and the one probe (`redirect: 'manual'`, `credentials: 'same-origin'`, `no-store`; an opaque redirect or a 401 reads as signed out, the shell or a 503 as unavailable). `/admin` (`AdminApp.tsx`) is the session page — the probe on load, "Signed in as", Sign out via `/cdn-cgi/access/logout`, Map and About; it writes the flag on a verified answer and clears it on a refusal. The About view renders `EditButton` (Edit / Exit edit, the Settings pill's chrome) at the document's head for a device holding the flag; an Edit click probes once and mounts `AboutEditor` — the former `/admin` editor as an overlay around the LIVE `AboutView` element App already renders — with paths re-anchored on AboutView's own `<main>` (exports no longer begin `0/`; `ops.ts` storage key → `about.v2`; the export names the anchor); Exit edit reverts the document and keeps the draft in storage; leaving the view leaves the mode; edit mode needs the acknowledged tier and says so. The first editor's own Light/Dark switch is gone with its page (the overlay follows the visitor's settings on `<html>`). **D-CPE-1 resolved** (the header's name left the tripwire; §14.1 amended). **Tests:** `worker-admin-session.spec.ts` (a key pair of its own — current + previous key verify; a stranger's key, an unknown `kid`, the wrong `aud`/`iss`, expiry, `nbf`, no email, malformed and `alg: none` tokens all 401; keys unreachable 503; vars missing 500; HEAD/405/404), `admin-session.spec.ts` (the flag's round trip + expiry + private mode; every probe answer), `about-editor.spec.tsx` (the document-root anchor — `path 1`, never `0/1`; the document's controls inert; Exit reverts and keeps the draft; the v2 key; the session page's three states), `dist-contract.spec.ts` pins `AdminApp` and `AboutEditor` as lazy, never preloaded; e2e `tests/e2e/admin-session.spec.ts` on the production build: a visitor's About makes ZERO `/admin/*` requests and shows no control; a flagged device → Edit → one probe → the editor over the live document → Exit edit; Access's 302 on the probe (Playwright's route) → the flag cleared and the sign-in line; `/admin` names the operator and writes the flag. **The host measurement — the forwarded header reaching the Worker on the real hosts — is M1's first deploy, recorded in the runway briefing.** |
| 2026-09-07 | **CPE-M0 — the edit modes ratified against the live runtime (this PR; docs only).** §6.6 lands the acceptance spec for the CPE arc (`c-ground-code/runway/cleanplate-map-editor.md`): `/admin` → a session page; About and Map gain admin-only Edit / Exit edit; a map drag becomes an orange tethered proposal pin, stored as a content-addressed draft in a NEW R2 bucket for `cf_location.py manual pull` to compose into the existing manual-pin contracts — the browser proposes, the cannon-food spine disposes. Nine decisions by chip, every one the recommendation (four at the tee-up, five at M0): the new bucket · CannonAI's Refinement Editor stays the review surface · `loc: 2` drags as a SITE fix · briefing first · the Edit pill beside Settings · drafts survive a reload · one draft per stack · a manual pull · mobile units follow the visitor's flag. **Six probes** (§6.6 Ground truth): the application cookie rides a same-origin fetch from `/` (200 through Access signed in, `opaqueredirect` without — measured in Chrome and in a cookie-less browser), Access 302s `/admin/api/*` on both hosts before any Worker, the certs endpoint and the JWT rules per Cloudflare's guide, the publisher lists only its own bucket, and the roster census (70 `loc: 2` places; no mixed stacks). **One conflict found:** the vocabulary tripwire retires the Access header's name, which M1 must use — **D-CPE-1**; **D-CPE-2** registers the pull's credential. No code; the suite count is unchanged. || 2026-09-07 | **The hint stops whispering (this PR; Cannon's two calls on seeing [#214](https://github.com/CannonWest/CleanPlateVA/pull/214) live — "make it bigger", then "give the whole shape an outline and make the font BIGGER even on default settings").** It is the pointer AT the accessibility choices, so it is now the one piece of floating chrome that does not sit in a hairline: **a 2px `border-cp-accent` outline the tip carries too** (`border-t-2 border-l-2`, its `bg-cp-surface-1` fill covering the outline behind it, so outline and tip are one unbroken shape), **15px ink at weight 500** — above the 14px body, where every other floating control sits at 11.5–13 — on a 21rem card at `py-3 pr-11 pl-4`, tip 12px, ✕ 16px. Two steps in one arc: #214's 11.5px/16rem hairline card went to 13px/20rem/full ink first, then to this. **Verified on :5621 (Playwright, 1280×800 · 375×812, both themes, three text sizes):** 336×68 under the pill's 103×40 at the column's 10px gap; the tip 17×17 with its centre at x=37 — **exactly the pill's gear centre**; `font-size` 15px against the body's 14px, weight 500, ink `#1d2129` light / `#e9ecef` dark; the outline `rgb(25,113,194)` light / `rgb(77,171,247)` dark. At the text extremes the note follows the scale (12.86px/62 tall at the 12px setting, 21.43px/86 at 20px) and its right edge stays 348px — **27px inside a 375px phone's gutter at every size**. The ✕ still dismisses and persists. **A measurement trap worth naming:** the computed border reads `1.45455px`, not 2px — Chromium snaps border widths to whole DEVICE pixels and the runner's effective ratio is 1.375, so a bare inline `border-top: 2px` control probe computes to exactly the same value. The rule is right; the number is the browser's. **317 Vitest** (+1: the outline, the size, the weight, the full ink and the tip's matching 2px edges pinned as contract rather than styling incidentals); `tsc` clean. |
| 2026-09-07 | **A closeable hint under the Settings pill (this PR; Cannon's ask).** `app/SettingsHint.tsx`: a note (`role="note"`) hanging as the third child of the map's top column — a rotated-square tip pointing up at the pill's gear, the one line "Theme and accessibility options are in Settings.", a ✕ labelled "Dismiss" — because the dialog's one self-opening says what the choices ARE and then disappears, leaving a visitor who closed it no reason to look at a gear again. **Help, not a notification:** nothing times it out, nothing animates, and it does not race the dialog — it renders BEHIND the scrim on a first visit and is still there when the scrim goes. It stands on every map view (never the List / About documents, which carry no pill) until the visitor closes it **or** opens Settings from the pill; either is the same news and both persist as `cleanplateva.settingsHintDismissed`, a key of its own so the hint outlives `settingsSeen`. **Verified on :5621 (Playwright, 1280×800 · 375×812, both themes):** absent behind the blocking §6.1 dialog; present the moment the settings dialog opens over it (hint at (12,152) 256×51 under the pill at (12,102) 103×40 — the column's own 10px gap; the tip 11×11 at (33,146), `rotate: 45deg`, centred under the gear; `max-width` 256px, `font-size` 11.5px, background `#fff` light / `rgb(22,25,28)` dark); still there when the dialog closes and after a reload; gone on List and back on the Map; at 375px the pill sits at y=203 and the hint at y=253, inside the gutter; the ✕ removes it and stores `1`, and a reload keeps it gone; clearing the key brings it back, and opening Settings from the pill dismisses it just as the ✕ does. 0 console errors. **316 Vitest** (+4: `settings-hint.spec.tsx` — the note role, the copy, one control, the aria-hidden tip, and that it never dismisses itself; `settings.spec.ts` gains the flag's round trip, its independence from `settingsSeen`, and private mode); `tsc` clean. Bundle: `index-*` 113.4 → 114.4 KB raw. |
| 2026-09-07 | **A color-blind friendly grade ramp, as a settings choice (this PR; Cannon's palette).** `GRADE_PALETTES` in `constants.ts` holds the ratified ramp as `standard` and Cannon's A `#045a8d` · B `#5aa9d6` · C `#f4c245` · D `#f08c3c` · F `#9c4a0c` as `colorblind`; `GRADE_COLORS` stays as the standard ramp's old name. **Two channels, one table:** the DOM asks for the TOKEN — `gradeColor()` now returns `var(--cp-grade-x)` (every chip, badge, hero circle, list dot, receipt pill, inspection-row flag and the trend instrument's SVG follow the class with no plumbing; `InspectionRow` / `ReceiptModal` / `trend.ts` lose their direct `GRADE_COLORS` reads) and `.palette-colorblind` on `<html>` re-points the `--cp-grade-*` / `--cp-new` tokens (`theme.css`; `palette.spec.ts` pins the CSS block to the table hex for hex); the map's paint and the donut canvas cannot read a token and take HEX — `gradeHex(letter, palette)`, `NEW_COLORS[palette]`, `DECLINE_RINGS[palette]` — so `buildMapData` bakes fills in the palette (MapView rebuilds and re-sets the source on a switch), the donut id gains a palette segment (`donut:<theme>:<palette>:<size>:<tuple>`; the codec, `staleDonutIds` and `donutIconExpr` take it, defaults keeping the old call sites), and `applyPalette` re-points the ring and the icon-image on the LIVE style and evicts the other palette's images (no `setStyle`). **Two consequences of the ramp, decided here and easy to revisit:** under it NEW is near-black `#212529` (the ramp's A and B are blues, so blue cannot stay NEW's alone) and the declining ring is `#212529` too (red on the umber F is ~1.4:1; the cue becomes "no bright halo", a luminance cue). Flagged, not fixed: the white letter on this ramp's gold C reads at ~1.7:1 (§6.0). **The dialog:** a "Grade colors" row between Theme and Group nearby places — "Standard" · "Color-blind friendly", each option its own five lettered chips in ITS ramp's literal hex (the band's chip idiom, aria-hidden so the caption names the option), the picture-choice rules (accent border on, half opacity off). `palette.ts` is theme.ts's twin (`cleanplateva.gradePalette`, unset = standard); `index.html` applies the class pre-paint and `palette.spec.ts` runs the script. **Verified on :5621 (Playwright, 1280×800, both palettes):** the dialog's chips computed to each ramp's hex; picking Color-blind friendly → the class on, `--cp-grade-a` `#045a8d`, `--cp-new` `#212529`, the band's A chip `rgb(4,90,141)`, the source's baked fills re-pointed ONE FOR ONE (35,141 A · 9,930 B · 4,999 C · 2,255 D · 2,438 F · 609 NEW · 302 unscored — the same counts under both ramps), the ring expression `#212529`, the icon-image `donut:light:colorblind:…`, only `light:colorblind` donuts in the atlas with clusters on; a reload paints the ramp from the first frame; back to Standard → everything reverts and the color-blind donuts are evicted; a hover card and a detail panel on a deep-blue dot: hero `rgb(4,90,141)`, the trend's dashed 60 line (`var(--cp-grade-f)` as a presentation attribute) computed `rgb(156,74,12)`, 0 unresolved marks, the violations pill umber; 0 console errors. **312 Vitest green** (`palette.spec.ts` new; `donut.spec` re-pinned to the five-part id and palette fills; `map-layers.spec` gains the palette ring and the `applyPalette` recorder; `map-data.spec` the color-blind bake; `settings-dialog.spec` the two-ramp row; the trend pin moved to the token); `tsc` clean. Bundle: `index-*` 111.1 → 113.4 KB raw (gzip 35.0 → 35.6). |
| 2026-09-06 | **The settings dialog's first review (this PR; Cannon's calls on seeing [#211](https://github.com/CannonWest/CleanPlateVA/pull/211) live): the copy cut, and the cluster switch becomes two pictures.** Gone: the lede ("Applied as you change them and kept on this device.") and the theme group's hint ("System follows the device's appearance (dark now).") — `Dialog.Content` takes `aria-describedby={undefined}` so Radix stops asking for a description, and `SettingsDialog` no longer takes `systemDark`. The clustering row's hint is now the one sentence "Nearby places share one bubble when zoomed out." and its Radix `Switch` is replaced by a two-item `ToggleGroup` (a radiogroup, like the theme's) whose items are **pictures of the two map states**, drawn as inline SVG in the map's own vocabulary on the basemap tint (`fill-cp-basemap`, `fill-cp-grade-*`, `stroke-cp-marker-ring` — Tailwind's `fill-*` / `stroke-*` utilities over the theme's colors, measured in the built CSS): "Every place" is nine ringed grade dots (seven close on the left, two apart on the right); "Grouped" is the same nine with the seven folded into one donut — three arcs for 4 A · 2 B · 1 C clockwise from 12 o'clock via `pathLength`/`stroke-dasharray`, hairline gaps showing the hole's own `STACK_SURFACE`, the count "7" in `STACK_INK` — beside the two that stay dots. The current state wears the accent border; the other fades to half (`data-[state=off]:opacity-50`); pressing a picture asks for that state and the pictures never flip themselves. Captions are their own `<span>` (the donut's count is text too, and the picture is `aria-hidden`, so the caption alone names the option). **Verified on :5621 — Playwright at 1280×800 and 375×812, both themes (the Browser pane's window was minimized this session, so its JS viewport read 0×0 and its screenshots timed out; the DOM probes there agreed with Playwright's):** cleared visitor → Agree → the dialog with no `aria-describedby` and exactly one `<p>` (the hint); pictures 183×124 side by side (SVG 164×82), "Every place" on with the accent border `#1c7ed6` and "Grouped" at opacity 0.5; pick Grouped → states swap, `clusters=1`, the live source's `getClusterOptions().cluster` true; Light → the pictures' ground and ring follow the theme, the off item's hairline `rgba(16,24,32,.12)`; phone → the sheet at (0,345) 375×468 with the pictures 163×114 each. **301 Vitest green** (`settings-dialog.spec.tsx` re-pinned: no lede, no hint, the two-picture radiogroup with nine dots / three arcs / the count "7" / the fade class on both items, the caption span; the System-hint test deleted); `tsc` clean. Bundle: `vendor-*` 72.2 → 68.5 KB raw (the Switch primitive left), `index-*` 110.0 → 111.1 KB (the pictures). |
| 2026-09-06 | **The settings dialog — theme · Group nearby places · text size in one Radix dialog; SYSTEM the theme default (this PR; Cannon's call).** `app/SettingsDialog.tsx` + `app/SettingsButton.tsx` (§6.5 written for it): Radix primitives from the unified `radix-ui` package (Dialog · ToggleGroup · Switch · Slider — tree-shaken: the vendor chunk holds those four and lucide, nothing else) dressed in the theme's tokens; the two standalone switches (`ThemeSwitch.tsx`, `ClusterSwitch.tsx`, their suites) are DELETED — the dialog owns both choices, the **Settings** pill takes the theme switch's slot under the band's left edge (map view only), and the bottom-left corner keeps its column box with the attribution chip alone. **The theme is three-way** (`theme.ts`: `storedTheme` → `light \| dark \| system`, `resolveDark`, `systemPrefersDark`): unset reads as `system`, which follows `prefers-color-scheme` and re-resolves live on the query's `change` (App listens); `dark` / `light` round-trip under the old key, so an earlier choice survives; the CSS base still did not flip (C10 amended again — §2, §6.0). **Text size** (`settings.ts`): the body size in px, 12–20 by 1, 14 the ratified default = scale 1, stored as `cleanplateva.textSize` and reflected as `--cp-text-scale` on `<html>`; **every `text-[Npx]` in the app became `text-cp-N`** — a Tailwind v4 functional `@utility text-cp-*` whose rule is `calc(N * 1px * var(--cp-text-scale))` (`--value(number)` takes the half-px sizes: `.text-cp-12\.5` measured in the built CSS, the `text-cp-ink` color utilities still emitting beside it) — 20 sizes across 14 files, plus the body and the hover card's inline hero size; the map's canvas labels and the trend SVG are drawings and stand. `index.html`'s pre-paint script applies both rules (theme + size) and is RUN by `theme.spec.ts` / `settings.spec.ts` against every stored value, so the two entry points cannot disagree. The dialog opens on its own once — a visitor's first map view after the ack, `settingsSeen` written on that opening — and from the pill any later time; ✕ · backdrop · Escape · Done close; every change applies and persists as it is made (no Save / Cancel). The `/admin` editor keeps its private two-state flip over the resolved theme, writing an explicit word. **Verified on :5621 (Browser pane; Playwright for the media query):** cleared visitor → Agree → the dialog with System checked (the pane prefers dark: `theme-light` absent, body `#0f1214`), `settingsSeen=1`, focus on ✕; Light → class on, body `#f4f6f8`, `theme=light`; ArrowRight on the thumb → 15 px, `--cp-text-scale` 1.0714, body 15px, brand 15.54px (14.5 × 1.0714), dialog title 18.21px, readout "15 px" + "Reset to 14 px"; the switch → `clusters=1` and the live source's `getClusterOptions().cluster` true; Done → 0 dialogs, the page's `aria-hidden` cleared; reload → no dialog, class + scale present from the pre-paint script, 1 Settings pill, 0 switches on the page; the pill reopens it with Light / on / 15 intact; System under a light-emulated scheme → light on reload, under dark → dark; Escape and a backdrop click both close; phone 375×812 → a bottom sheet at (0,387) 375×425 with square bottom corners; 0 console errors. **Found: the Browser pane's scheme emulation dispatches NO `change` events** (a bare `matchMedia` listener logged nothing while `matches` flipped), so the live re-resolution was proven in Playwright instead — `emulateMedia` does dispatch (bare listener `[false, true]`); under System the class flipped on at light and off at dark without a reload, and an explicit Light stayed light under a dark device. **302 Vitest green** (`theme.spec.ts` rewritten for the three-way choice; `settings.spec.ts` + `settings-dialog.spec.tsx` new — the dialog's contract through Radix's rendered roles: a radio group of three, a labeled switch, a slider 12..20 asking by keyboard, the reset, every way out; the two retired switch suites gone); `tsc` clean. **Bundle, reported not gated (D-CR-PERF-1):** `vendor-*` 7.3 → 72.2 KB raw (Vite's gzip figure 24.0 KB; the §8 tables' 2.9 KB was br wire, not comparable) for the four primitives and their deps, `index-*` 104.3 → 110.0 KB raw (gzip 33.0 → 34.6 KB); nothing else moved. |
| 2026-09-06 | **The grade letters leave the map (this PR; Cannon's call).** `food-point-letters` — the symbol layer that drew a dot's grade letter past z13.5 — is deleted, with `LYR_POINT_LETTERS`, `LETTER_ZOOM`, `LETTER_TEXT_SIZE`, and the per-feature `letter` property that fed it (`mapData.ts` still DERIVES the letter: it picks the fill and gates `declining`, it is just no longer emitted). Four layers now, bottom to top: clusters · points · stacks · stack counts. **§6.0 amended, not silently broken:** the letter was that section's mandatory compensating control for a ramp whose worst deutan pair is B↔C at ΔE 3.8, so the row now says what is true — the letter rides the color wherever a grade is NAMED (chip, hover card, panel, list row), and the map dot is color + ring alone, with the letter one hover away. The glance-cost at deep zoom is stated and accepted. Nothing else moves: the radius curve, the hit test (which never queried the letters), the declining ring (never gated on them), and the stack/cluster grammar are untouched. |
| 2026-09-06 | **The attribution chip clears MapLibre's expanding attribution, and reads solid ([#206](https://github.com/CannonWest/CleanPlateVA/pull/206); Cannon's call).** The chip's own sentence was its only width constraint above `sm`, so at a merely-wide-not-huge desktop width it reached the map's bottom-right control lane and ran under the (i) control's hover/click expansion (~320px plus its own 10px margin). The bottom-left column gains the `sm:` half of the stop its `max-sm` half already had — `sm:right-[360px]`, so the chip wraps rather than disappearing under the expansion; the mobile rules are untouched. The chip also reads solid over the markers: `font-semibold` and a stronger scrim of its OWN (`--cp-scrim-2`, .85 dark / .78 light) — `--cp-scrim` stays where it is, being the AckDialog's and ReceiptModal's full-screen backdrop. **Measured at 750px:** chip right edge 390.5, expanded control left edge 421.4; phone unchanged (chip bottom 662.4, strip top 666.4). Verified again on production after the deploy. |
| 2026-09-06 | **The panel goes full-screen on phones (this PR; Cannon's call).** Below `sm` — every width that put the panel at the bottom rather than the right — `DetailPanel` was a 62vh bottom sheet; it now covers the whole screen (`max-sm:inset-0`, no radius, border, or shadow), so the header's close ✕ is the screen's top-right corner, with its hit box grown to 8px off both edges on phones. The right sheet from `sm` up is untouched; §6.2's panel line amended. The report-card modal and the ack dialog keep their own bottom-sheet rules. |
| 2026-09-06 | **Two color calls (this PR; Cannon): a switch track that lightens with the theme, and a theme-invariant stack bubble.** **(1) The theme switch's track is chrome now** — `bg-cp-surface-2` + `border-cp-hairline`, the surface and hairline the band and the cluster switch already wear — so it is near-white on the light theme instead of a black slab on a white map, and **byte-identical on the dark one** (surface-2 *is* the `#1d2126` it carried as a literal). The glyphs keep inks of their own: the sun stays yellow-5 in both themes, and the moon gains a light-theme ink (`#495057`, gray-7) because off-white would vanish on a near-white track — **the first real use of the `light:` variant** the theme.css header reserved for what tokens cannot carry, since a control's own ink is not a semantic surface. Both glyphs now take `fill="currentColor"` (stroke follows), so the cell's text color is the single place a glyph color is decided. **(2) The neutral count bubble is theme-invariant** (`STACK_SURFACE` / `STACK_INK` are plain strings, not per-theme records): a stack wears the dark theme's `#242a31` surface and `#e9ecef` ink on the light basemap too, so a stack looks like a stack wherever you meet it. **The donut's hole and count follow** — they are the same idiom and shared the same constants, and an isolated stack drawing beside a cluster bubble would otherwise read as two different objects on a light map. Side effect, accepted and not fixed: every donut image is now identical across themes, so the theme segment in the donut id is pure cache identity and a theme swap still evicts and repaints (0.23 ms a ring, measured at CRP-M6) — not worth a codec change. **Verified on :5621 (Playwright, computed styles + live paint properties):** light — track `rgb(248,249,250)`, border `rgba(16,24,32,.12)`, moon `rgb(73,80,87)` at 50% (so the `light:` rule wins the cascade — same specificity, variant sorts later), sun `#fcc419` on the `#1c7ed6` thumb; dark — track `rgb(29,33,38)` and border `rgba(255,255,255,.09)`, i.e. unchanged, moon `#f1f3f5` on the `#343a40` thumb; the map's own paint in BOTH themes — `food-stacks` `circle-color` `#242a31`, `food-stack-count` and `food-clusters` `text-color` `#e9ecef`; a 7-place stack in downtown Richmond screenshotted dark on the light basemap and again on the dark one. **269 Vitest** (+3 here: the glyph/ink split, the track-is-chrome pin, and a paint pin that collapses four stack paints across both themes to one surface and one ink), `tsc` clean, 0 console errors. |
| 2026-09-06 | **The theme switch is a MAP-VIEW control — off the List and About documents (this PR; Cannon's third call that day, after [#199](https://github.com/CannonWest/CleanPlateVA/pull/199) and [#200](https://github.com/CannonWest/CleanPlateVA/pull/200)).** The documents' top wrapper drops the switch and its column (`mx-3 mt-3` plain, the band alone); the map's fixed column is unchanged and keeps it under the band's left edge. **Both presentation switches are now map-view controls** — the theme switch under the band, the cluster switch in the bottom-left corner — and neither renders on a document, which is a page of records rather than a thing being presented. The theme itself is untouched: it lives on `<html>`, so a choice made on the map holds on every view, on navigation and on a cold deep link alike. **Verified on :5621 (Playwright; 1280×800 · 375×812):** map — 2 switches, the theme switch (12,102) desktop / (12,203) phone; go dark, then List and About — **0 switches**, `theme-light` absent, `body` `#0f1214`, band full-width in the flow (1241×57 desktop, 336×181 phone); back to the map — the switch present and `aria-checked="true"`; a cold `/list` deep link — dark honored, 0 switches; flip to light on the map, and List / About carry `theme-light` with `body` `#f4f6f8` and still no switch; 0 console errors. **Not test-pinned** (as the cluster switch's map-only placement never was — the component suites pin the controls, the browser pass pins where they render). **Cost, accepted:** a visitor who deep-links to `/list` or `/about` and wants the other theme goes to the map for it. |
| 2026-09-06 | **The theme switch UNDER the band, not beside it (this PR; Cannon's refinement of [#199](https://github.com/CannonWest/CleanPlateVA/pull/199) on seeing it live: "not the very top-left — underneath the filter / nav / search bar section").** The top-left row becomes a self-stacking COLUMN — the band first, the theme switch hanging 10px under its left edge (`self-start` keeps the switch its own width; the band stretches to the column) — fixed over the map, in-flow at the head of List / About; the same caps (`100vw-24px`; `100vw-448px` beside an open panel from `sm` up) and the same pointer-events idiom (none on the column, auto on its two children), so the map drags beside the switch. Nothing else moves: colors, size, the light default and the tests of #199 stand (258 green, `tsc` clean). **The band gets its full width back on every screen** — the +42px phone cost of #199 is gone — at the price of 57px under the band on the left (47 + the gap). **Verified on :5621 (Playwright; 1280×800 · 1536×864 · 375×812):** map at 1280 — band (12,12) 1005×80, production's own shape, the switch at (12,102) 83×47; dark → the same geometry, the moon disc on, `dark` stored and kept across a reload; List — the band full-width in the flow (1241×57, one row again) with the switch under it at y=79; panel open — the column capped at 832px, the switch under the reflowed band at y=120; 1536 — 1005×80 / switch at 102; phone — band 351×181 (production's exact phone shape), the switch at (12,203), the cluster switch (12,679) and the chip untouched bottom-left; phone List — band 336 wide, switch at 203; 0 console errors. |
| 2026-09-06 | **The theme switch to the top-left, 1.5×, in color; light the visitor default ([#199](https://github.com/CannonWest/CleanPlateVA/pull/199); Cannon's call, post-program).** Four asks, one PR. (1) **Placement:** the switch left the bottom-left corner column (which keeps the cluster switch over the attribution chip) for a top-left ROW beside the band — App's second self-sizing corner idiom: the row is as wide as switch + band, capped at the viewport's gutters (`100vw-24px`) and, beside an open panel from `sm` up, at `100vw-448px` (the band's own caps from the mobile fix, moved up a level); `pointer-events-none` on the row, `auto` on its two children, so the map still drags in the gap. The band no longer positions itself (`Toolbar` lost `panelOpen` + `docked`; it is a `relative min-w-0` flex item), and the List / About documents carry the same row in-flow (`mx-3 mt-3`). (2) **Size:** 1.5× the corner pill — 36px thumbs, 20px glyphs, 5px padding: 83×47 measured, against the corner column's 55×31. (3) **Color:** the glyphs' own, fixed in either theme — an off-white moon (`#f1f3f5`) and a yellow sun (`#fcc419`), filled as well as stroked, on a night-slate track (`#1d2126`, `border-white/10`); the thumb is a `#343a40` disc under the moon for dark and the accent-blue disc under the sun for light, the inactive glyph at 50%. A theme switch is a picture of its two options and must read the same in both — an off-white moon on the light theme's white surface would vanish, which is why the track is not a theme token. (4) **Default:** LIGHT — `storedDark` reads only a stored `dark` as dark; `index.html`'s pre-paint script adds `theme-light` unless `dark` is stored; the CSS base did NOT flip (the un-prefixed styles stay the dark design; no component uses the `light:` variant), so C10 is amended, not inverted — an existing choice survives under the old key, a never-touched visitor gets light. **Verified on :5621 (Playwright; 1280×800 · 1536×864 · 375×812):** a cleared visitor paints `theme-light` before the dialog (`body` `#f4f6f8`); Agree → the switch at (12,12) 83×47, the band at x=105, both themes' computed colors as designed; click → `dark` stored, class off, `body` `#0f1214`; reload keeps it; click → `light` stored, reload keeps it; List carries the row in-flow at (12,12); panel open at 1280 → the row capped at 832px; phone → the row at 351px, the cluster switch + chip still bottom-left, the zoom buttons clear; 0 console errors. **258 Vitest** (+9: `theme.spec.ts` — the default, the round trip, private mode, the class, and index.html's script RUN against the same storage so the two entry points cannot disagree; `theme-switch.spec.tsx` — role / words / click, the two solid Lucide glyphs in the pinned colors, the disc as the state). **Found, not fixed (pre-existing, measured on production the same hour):** the band wraps its count onto a second row at EVERY desktop width — 1005×80 at 1280 and at 1536 on cleanplateva.com; its shrink-to-fit width lands ~4px under its single-line need — and the switch changes nothing there. **Cost on a 375px phone, flagged:** beside the switch the band is 258 wide and 223 tall (six rows — the brand and nav no longer share one) against production's 351×181: +42px of header. |
| 2026-09-06 | **Mobile bits (this PR; post-program).** Two phone-width defects, found on the dev server at 375×812 and fixed as breakpoint rules — no data, contract, or copy change. (1) **The band collapsed to a 25px sliver whenever a facility was open:** its reflow beside the panel was an inline `min(100vw − 24px, 100vw − 448px)`, which goes negative below the right sheet's width, clamps to 0, and left every control spilling out of a card the width of its padding. The reflow is now the `sm:` rule it always meant (below `sm` the panel is a bottom sheet and the band keeps the full width); the two class strings are whole and whitespace-delimited, since Tailwind's scanner drops a candidate glued to a `${`. (2) **The attribution chip buried the theme switch and the map's zoom buttons:** the C8 line wraps to three lines at 375px (58px tall) while the switch sat a fixed 40px up. The bottom-left corner is now ONE self-stacking column — cluster switch · theme switch · chip (`Attribution` no longer positions itself; `ThemeSwitch` keeps its own floating placement for List/About via `floating`) — so nothing depends on the chip's height; below `sm` the column stops 54px short of the right edge (MapLibre's bottom-right control lane) and rises above the basemap's compact attribution strip, which opens EXPANDED on a map under 640px until the first drag. The column is pointer-transparent except for its children. Measured: phone — band 351px wide with the sheet open, chip 717–774, switch 679–711, cluster 642–673, zoom/locate at x ≥ 336, attribution strip 778–802 clear; desktop — chip bottom 10px, switch 43px (was 40), cluster 80px (was 76), band `100vw − 448px` beside the open sheet as before. Seen, not fixed: the band's four-row wrap on phones (35% of the viewport), and the chip crossing the expanded basemap attribution between 640 and ~920px. |
| 2026-09-06 | **CRX-M2 — the close; the CR program is closed (this PR).** §1 gains the status line and the arcs table's Closed column (CRD 08-29 · CRF/CRV-a/CRV-b 08-30 · CRC 09-06 · CRX 09-06); §8's production paragraph records the D-CRX-1 measurement (hashed chunks `immutable`, shell + worker pair `max-age=0`, a missing name still the shell, the publisher's rules untouched); §9.5 the M2 receipt. In cground-skills the same close rewrites `project_clean_plate_va.md` as a ≤1.2 KB identity card, trims its MEMORY.md line, archives `runway/cleanplate-close-out.md` with its HISTORY line, and records D-CRX-2's resolution on the Workers Builds memory card. From here this document is the front-end constitution: new decisions get D-CR-* rows, new retirements go to architecture-v4 §14.1 and the tripwire, a new arc gets its own briefing. |
| 2026-09-06 | **CRX-M2, the two calls ([#196](https://github.com/CannonWest/CleanPlateVA/pull/196)).** **D-CRX-1 implemented** (Cannon's call on the measured cost — ~9 bodiless 304s per returning visit, ~75 ms per wave, unmetered; not the lead-dev recommendation): `vite.config.ts` `buildAssetHeaders` appends one `immutable` rule per content-hashed asset the build emitted (10 today) to the `dist/` copy of `_headers`, by name, never a splat, after `cpPublicCopy` (a `sequential` + `order: 'post'` `closeBundle`), the un-hashed maplibre worker pair excluded, a bundle asset without a hash failing the build; `dist-contract.spec.ts` pins the publisher block byte-identical as the prefix and the appended block as exactly the hashed files with the one value; C5 amended (§2), §8 + README updated; typecheck clean, 249 Vitest incl. the enforced dist-contract. **D-CRX-2 resolved** — "Builds for non-production branches" OFF in the dashboard (flipped in-session through Cannon's browser at his instruction, confirmed after a reload; §3): this PR is the first with no Workers Builds branch check at all. §10 gains both rows. |
| 2026-09-06 | **CRX-M1 — tripwires extended, hits fixed (§9.4).** The publisher hotfix first, on Cannon's go ([cground-skills #1870](https://github.com/CannonWest/cground-skills/pull/1870): the Lite contract gate runs `tests/vitest/lite-roster-contract.spec.ts`, proven on the committed data before the merge). Then the cground half ([#1871](https://github.com/CannonWest/cground-skills/pull/1871)): the Tier-0 mirror gained twelve rows as M0 measured them — seven bare, two narrowed off CannonAI's own paths (planted-line check: fires on the site's strings, not on `cannonai/gui/static/js/app.js`), two promoted, the dead dev tool — eight stale lines rewritten, nine dated lines marked, Tier-0 green. The site half (this PR): README's design-reference callout, hosting section (main-push builds only; the branch check reads red until D-CRX-2; GitHub Actions + `wrangler deploy --dry-run` are the PR proof), parity sentence and `tools/` paragraph rewritten; `tools/visits-crosscheck.mjs` deleted with a §14.1 row and a tripwire regex; §15 re-pointed. Vitest green. M2 remains: the identity card, §1's status, D-CRX-1/2, archive. |
| 2026-09-06 | **CRX-M0 — the sweep, measured (docs only; this PR).** The nine CRC rows and twelve candidate phrases run over the 20,057 tracked files of the cground-skills checkout, this README and the workspace launch file with the Tier-0 tripwire's exemptions applied: 446 records, 216 live, 230 exempt — every hit classified in `docs/crx-sweep.md`, the summary in §9.3. The feared present-tense sentences have zero live instances outside the CRX charter; the residue is eight cannon-food lines, two README sentences, one CannonAI-side sentence, the memory card (M2). Two path rows must be narrowed (29 live c-ground-code lines are CannonAI's own paths), two rows promoted (the deleted Lite suite by exact name, the old orchestrator's file name — dead in both projects). **Found, not fixed:** cannon-food's publisher still runs the deleted Lite suite as its contract gate (the nightly halts at `lite_contract` from its first post-flip run — a hotfix, Cannon's go), and `tools/visits-crosscheck.mjs` imports two deleted modules (delete or port — Cannon's call). Nothing served changed. |
| 2026-09-06 | **CRC-M2 — verified on the host and closed (this PR).** The §2 constitution row by row on cleanplateva.com and at the CannonAI mount (§9.2), §8 re-measured on production (10 / 566.9 KB br — the preview numbers reproduced), the branch-build finding recorded in §3 (non-production branches run NO build command), the dual track torn down (`cleanplateva-preview` deleted by name, its `next.cleanplateva.com` AAAA gone), §14.1's preview row closed and its regex in the tripwire. CRX teed up in c-ground-code as its own briefing (`runway/cleanplate-close-out.md`). |
| 2026-09-06 | **CRC-M1 — THE FLIP ([#192](https://github.com/CannonWest/CleanPlateVA/pull/192)).** Production serves the Vite build: `assets.directory` → `./dist`; the no-build client (22 files), `app.py` + `requirements.txt`, `wrangler.preview.jsonc`, the node runner (`dashboard.mjs` + 23 suites) deleted; the two survivors ported to Vitest (249 green); CI one runner; §14.1's seven rows appended; README's minimum. Merge 12:11:23Z → deployed 12:12:07Z (44 s) on the main-push build path — the PR-branch Workers Builds check had gone red because non-production branches run no build command (§3, §9.2); GitHub Actions + a local `wrangler deploy --dry-run` were the pre-merge proof (Cannon's call). Rollback = `git revert 7124791` as its own PR. |
| 2026-09-05 | **CRC-M0 — pre-flip readiness, docs only ([#191](https://github.com/CannonWest/CleanPlateVA/pull/191)).** §9.1 is the flip's file-by-file list (the ONLY `wrangler.jsonc` change; the shipped client's 22 files; `app.py` + `requirements.txt`; `wrangler.preview.jsonc`, the Worker itself going at M2 by name; the node runner whole — `dashboard.mjs` + all 23 suites, twenty superseded, one already twinned, two PORTED to Vitest; the `package.json` / CI / `tsconfig` / `vite.config` trims; the README minimum; the §14.1 rows and the tripwire regexes CRC appends), the runner decision (Vitest only — D-CR-TEST-1 consolidated, not a trimmed `test:node`), the §2 constitution walked row by row against next.cleanplateva.com on the M5 build with this session's receipts (C1 miss path at `max-age=0`, C2 zero-fetch, C3 counts 17 / 33 / +2 / +1 / 0, C4 the Worker's `X-Cache` answer, C5 the manifest's sha vs git + `_headers` proven by its effect since the platform consumes the file, C6 deep links, C7 the CannonAI mount on `dist/`, C8 the footer line, C10 the theme swap), and the rollback rehearsed (`git revert` of the flip's squash commit as its own PR — the deletions mean `assets.directory` alone cannot roll back). Nothing served changed. **CRC-M1, the flip, fires in a FRESH session.** |
| 2026-09-05 | **CRP-M5 — the bundle diet ([#190](https://github.com/CannonWest/CleanPlateVA/pull/190); the second pre-flip pass of Cannon's re-sequencing).** One 1,334 KB / 362 KB-gzip chunk → Rolldown `codeSplitting` groups in `vite.config.ts`: `maplibre-*` 979 KB (its CSS beside it), `react-*` 190 KB, `vendor-*` 7 KB (lucide), the app's entry **104 KB / 33 KB gzip**; List, About and the report-card modal via `React.lazy` + `Suspense` (6 / 38 / 12 KB chunks that arrive on first use — the receipt MATH stays in the entry because it gates the hero's affordance); the shell preloads the three static vendor chunks and never the lazy ones; `chunkSizeWarningLimit` 1024 for the one chunk that is the floor. **Measured on the preview host** (§8 method): fresh visitor **10 static requests / 567.2 KB br** vs the M3 build's 6 / 580.0 and the old client's 26 / 614.2; an app-only deploy re-ships ~40 KB (entry + CSS) — the vendor chunks answer a **304 with no body** (hashed assets are served `max-age=0, must-revalidate`, as the old client's `/static/**` always was; the immutable-by-name follow-on is CRX's, §8). C3 untouched: a chunk is a static asset, zero Worker requests. Pinned (+5 dist-contract, **235 Vitest**): maplibre in its own chunk (the one naming the worker pair), react in its own, a vendor-free entry, the three lazy chunks, the shell's preloads; `detail-panel.spec` awaits the modal's first open. **Smoked on next.cleanplateva.com** (Playwright): cleared first load = 8 assets + 0 data, no lazy chunk; Agree → 33 data; List → `ListView-*.js` arrives, 50 rows; About → `AboutView-*.js`; a list row → `?permit=` panel (+2 data); the hero → `ReceiptModal-*.js` (200 `text/javascript`) → "How this grade was computed"; Escape closes; `?tier=lite` 17 static / 0 Worker. |
| 2026-09-05 | **CRP-M3 — `MapView.tsx` decomposed, zero behavior change ([#189](https://github.com/CannonWest/CleanPlateVA/pull/189); fired inside the CRC session after Cannon re-sequenced M3 + M5 ahead of the flip).** 767 → 441 lines: the layer set to `mapLayers.ts` (pure — `installDataLayers` + `fixDarkRoadLabels` + the radius/ring/count expressions + `HIT_LAYERS`; the map typed structurally as `LayerHost` so the install runs against a recording host), the two popups onto one `useMapPopup` controller each (`show` / `hide` / `key` / `dispose` — the hover card and the stack popover had been eight refs and two near-identical closures), geolocate + the patient auto-locate + the coverage note into `useGeolocate` (`coverageBounds` / `withinCoverage` pure). Pinned (+23, **230 Vitest**): the source's cluster config + `sum`/bucket properties, layer order/types/filters/gates, the theme in every paint, the CRP-M2 ring at every zoom, **the paint radii evaluated by MapLibre's style-spec evaluator against mapHit's JS twins** (11 zooms × 6 stack counts — a hit target that disagrees with the paint is now a red test), the bubble count's hand abbreviation, install idempotence, stale-donut eviction, the road-label fix, the popup controller's lifecycle, the geolocate control's ratified options + both notes + the follow lock. **Walked live** (Playwright on the dev server via `__cpMap`): C2 zero fetches before the ack; the five layers in order; the hover card at zero requests (VCU – Bleecker Street Cafe, A 98/100 + trend); click → `?permit=` panel with Source + Grade breakdown, Back closes; a 7-place stack → "7 places at this point" → popover of 7, Escape closes, a second click dismisses; switch on → 82 bubbles / 196 donuts at z7.5 (`sum` 241 vs `point_count` 231), hover = cursor only, click → z8.5; theme swap → Positron, layers reinstalled, dark donuts 297 → 0, light 108; back → Dark Matter with the road-label fix. Preview redeployed; `/list`, `/about`, `?tier=lite` (17 static, 0 Worker) re-checked on next.cleanplateva.com. |
| 2026-08-29 | Doc created at CRD-M0 from `c-ground-code/runway/cleanplate-react-redesign.md` (the program opener): program frame (§1, ratified this day), parity constitution moved here from the briefing (§2), production asset baseline measured and stamped (§8), acceptance-spec template (§6), fork registry seeded (§10). |
| 2026-08-29 | **CRD-M0 fork rounds (Cannon, `AskUserQuestion`, 4 rounds).** All 14 M0 forks resolved — twelve as recommended (D-CR-DOC/LANG/BUILD-1..3/ROUTE/MAP/STYLE/STATE/TEST/EMBED/STAGE-1), two Cannon's own calls: **D-CR-PERF-1 no wire budget** (the request budget is the only hard gate; §8 re-framed the same day) and **D-CR-ARC-1 split CRV-a + CRV-b** for finer review. D-CR-UX-1 remains open for M1. §3–§5, §7–§8 record the resolved end state. |
| 2026-08-29 | **CRD-M1 — the visual direction, ratified view by view** (same session; mockups on a throwaway :5610, reviewed live). The style tile ("slate & signal": slate chrome, judgment/action color reservation, dark default, system type + tabular-nums) ratified with `mockups/tokens.css` as the vocabulary. The **grade ramp** re-validated: F darkened `#e03131`→`#a61e1e` (fixes the live D↔F normal-vision fail, 7.6→~15); a teal B was ratified then reverted on review — **B stays lime `#94be1b` (Cannon: blue belongs to NEW alone)**, decision record `mockups/ramp-candidates.html`. The **Map ensemble took eleven passes** and produced the cross-view rules of §6.0: Lucide-only icons (no emoji); badges-are-a-sometimes-thing (permanent facts render structurally; **Source** + per-inspection link-outs in the blue-outline box-and-arrow external grammar); the tappable centered grade hero; and the **trend instrument** — absolute rest-100→55-extend-on-demand scale (stress-tested against a one-visit-29 outlier, `mockups/trend-compare.html`), A·C·F band labels, full `sparkline.js` mark grammar (dots/diamonds/verdict-height/ticks), hover enlarge+dim, full-size framed presentation. Inspection rows rebuilt to the full `inspection.js` inventory (purpose, scope, count chips, per-violation ↳ corrective lines, Inspector comments + Additional, grouped checklist with pass bars, temps). List ratified first pass; About after a rebuild to full content parity (hero provenance legend + §01–§06). **Acceptance specs written (§6.1–6.4); D-CR-UX-1 resolved; CRD is complete** — CRF next, teed up as its own briefing. |
| 2026-08-29 | **CRF-M0 — scaffold + pipeline.** The D-CR-BUILD-1 layout landed: Vite at the repo root (v8.2, Rolldown-based), app under `app/`, React 19.2 + TypeScript 7.0 `strict` + Tailwind v4.3 + Vitest 4.1 + Lucide; `docs/mockups/tokens.css` translated to the Tailwind `@theme` (`app/theme.css`, `--cp-*` vars source-of-truth, class-flip `.theme-light`, dark default per C10). Build = `vite build` + a filtered publicDir copy (`copyPublicDir: false` + plugin): `public/data/**` + `_headers` ride into `dist/` byte-identical (C5, pinned by `tests/vitest/dist-contract.spec.ts`), `data-full/**` + the old client (`index.html`, `static/**`) excluded. CI runs both suites (`npm ci`, `vite build`, `vitest run`, `node --test` — 290 legacy tests stay green). Dev = Vite dev server with `public/` passthrough (`data-full/**` unwatched — a 13.6 s cold load without that). Metered-unit check recorded in §3; build command handed to the dashboard post-merge. `wrangler.jsonc` untouched (C4). |
| 2026-09-05 | **CRP-M6 — the cluster toggle: production's proximity clustering as a persisted, default-OFF visitor switch, with Cannon's donut (fired on the post-M2 tree — the M3 decomposition it was gated on was never teed up, so the layers live in `MapView.tsx`).** #174's port re-applied under a switch: the source is ALWAYS configured (`cluster` = the switch, `clusterMaxZoom` 12, `clusterRadius` 40, `clusterProperties` summing `stack` + eight buckets) and the switch flips it in place with `setClusterOptions` — measured live in isolation 23 ms off / 265 ms on (the worker re-index of 26.5k features; 736 ms when the flip lands mid tile-churn), no teardown, no GeoJSON rebuild. The cluster inputs ride EVERY feature (`mapData.ts clusterInputs`): `stack` (1 for a lone dot) + `nA nB nC nD nF nNew nNone nClosed` — per-bucket COUNTS in place of #174's mean-grade sums, all zero on the basic map. **The donut** (`app/donut.ts`): ONE symbol layer (`food-clusters`, installed first, under dots and stacks) whose `icon-image` is a `concat` of theme · size step · the eight sums (`donut:dark:22:445-138-67-27-16-11-4-0`) and whose `text-field` is the hand-abbreviated `sum` in `STACK_INK` on the hole — icon and count on one layer so a re-cluster can never separate them; `map.setMissingStyleImageResolver` parses the id and paints the ring on a 2× canvas (arcs clockwise from 12 o'clock in bucket order, hairline separators in the hole's surface, the closed arc at 0.5 alpha, **no outer ring — Cannon's live call on the first look**), registered with `pixelRatio: 2`. **Paint cost, measured after Cannon's "the zoom feels load-y" on the preview:** a fresh GPU-backed canvas per image cost **3.0 ms each** — a zoom step re-forms ~100 new rings, so the main thread froze 230–376 ms per step; ONE CPU-backed scratch canvas (`willReadFrequently`, sized for the largest step) brings it to **0.23 ms per ring (13×), 27–63 ms per step**. The rings are deterministic for a given roster + filter + zoom but the set is large (509 distinct after six zoom levels around one metro), so they are painted on demand rather than precomputed. **Verified on :5620, pane fronted (a hidden pane freezes rAF — MapLibre loads no tiles at all, which read as a stuck source until `tabs_context` said "hidden"), both themes, both tiers:** three sampled clusters' rings matched `getClusterLeaves` exactly (7A+3B=10 · 8A+4B+1C=13 · 9A+1B+2C+1D+1F = 14 places over 13 points — `sum` counts places, `point_count` counts points); 28 bubbles at camera 12.9 → 0 at 13.1 (CARTO's 512px tiles); click eases to expansion + 0.5; hover = pointer cursor, no card; `listImages()` 37 at state → 145 at metro → **205 after the state→metro→neighborhood tour** (no quantization needed; the basic map draws 2); reload keeps the choice; `?tier=lite` draws neutral rings from all-zero tuples with the grade chips hidden; the switch sits 4px above the theme switch; zero console errors. **Found: a theme swap DIFFS the style in 6.6.0** — `setState` keeps the image manager and fires `style.load` itself (the briefing's "setStyle drops every image" holds only for the full-reload path), so the outgoing theme's donuts lingered (both themes listed after a swap); the reinstall now evicts them (`staleDonutIds`, pinned; measured: dark-only after swapping back). **The popover dismissal on absorb** — never ported before — is production's `webSurvivesZoom` as `mapHit.popoverSurvivesZoom` + a `zoom` listener: verified open at z15 → dismissed falling to 12.5 → survives zooming IN to 16 and falling to 14.2 → Escape closes → with clustering OFF the same fall leaves it open (negative control). Not in `AppState`/`URL_KEYS` (C6); `App.tsx` owns `clusters` beside `dark`, `ClusterSwitch` renders on the map view only. §6.2 markers row rewritten. **207 Vitest** (+21: bucket bake incl. the ring-totals-the-hole invariant, `clusterProperties`, three-tier hit order + `clusterRadiusOf`, the zoom predicate's six cases, the preference module, the id codec round-trip + rejections, arcs, `staleDonutIds`, the icon-image expression run through the style-spec evaluator against a cluster feature, the switch's role/words/click). Flagged, not fixed (out of scope): at 375px the attribution footer wraps to three lines and paints over the theme switch — pre-existing since the footer's Fairfax wording (FFX-M4), the new switch above it is unaffected. |
| 2026-09-05 | **CRP-M2 — the declining ring (Cannon's pick, replacing the CRP-M1 ↓ suffix).** Scoped first: three reds × four widths on A/C/F beside the plain marker, contrast measured ring-vs-fill. Production's `GRADE_COLORS.F` ring is 1.00:1 on an F fill — the declining F, the marker that matters most, shows no ring at all — and `--cp-grade-d` collides with D the same way; `--cp-danger-solid #e03131` (the pre-CRD-M1 F, freed when F darkened) is the one red in the vocabulary that collides with no fill (worst 1.26:1 on D). Cannon's calls: `#e03131` at production's 2.5px, **no zoom gate** (the ring rides the dot wherever the dot is drawn), the ↓ retired outright. Landed as `case` expressions on `LYR_POINTS`' own stroke (`ringColorExpr`/`ringWidthExpr` — MapLibre draws one stroke per circle, so a declining dot trades its white separator for the red; the two-ring halo variant was scoped and not taken): `LYR_POINT_DECLINE`, `installDeclineImage`, and the `DECLINE_ICON*` constants deleted — nothing to re-register per style swap. The bake (`mapData.ts` `declining`: letter-carrying dots only, banded >5 per M1b) is untouched, so the map-data suite pins the same predicate under a new name. Paint stays unpinned (jsdom has no WebGL) — **verified live on the dev server, both themes**: `food-points` reads back the `case` expressions, the decline layer is gone, 24,316 dots render with zero console errors, and MapLibre's own evaluator returns `rgba(224,49,49,1)`/2.5 for injected declining A/C/F dots and the theme's white (.85 dark / .95 light)/1.5 for a plain one. `mapHit.ts` unchanged: the hit test never measured stroke, and the extra pixel rides inside the slop. `mockups/style-tile.html` keeps its dashed-ring legend — it draws CRD-M1's scrapped form and the caption matches its drawing. §6.2 markers row updated. 184 Vitest + 294 node green. |
| 2026-08-31 | **CRP-M1b — the declining semantics rebuilt (Cannon's call on reviewing M1's threshold).** `trend_delta` is now GRADE-to-grade: the exporter re-runs `facility_grade` on the history with the current era removed (`_prior_era` — grade_inputs' own selection subtracted), so the delta compares what the headline READ before the current base landed (second-newest scored broad + ITS follow-ups, adjusted) against today's grade — the old raw broad-to-broad `score_trend` difference said −2 where the era comparison says −7 (pinned in cannon-food `test_grade.RollupIntegrationTests`). And "declining" is BANDED: strictly MORE than 5 points of drop (`TREND_DECLINE_BAND`, ONE definition in `cf_export_site` mirrored by `presentation.ts` AND the old client's `presentation.js` — production's dashed ring bands too); a −5 exactly ships in the delta but does not flag (band-edge pinned in all three suites). Wire shape unchanged; pre-republish payloads carry the old semantic until they age out. The panel's Declining/Improving chip and the List's trend cell stay sign-based readouts of the (new) delta — only the FLAG (map ↓, old ring, exporter bool) bands. Raw-layer note: the Couch rollup's own `declining` (cf_rescore/cf_parse_load) still encodes broad-to-broad — a tagged discrepancy, the site view never reads it. |
| 2026-08-31 | **CRP-M1 — the declining ↓ suffix (Cannon's pick; resolves pass #2's open form).** Graded, active, full-tier dots whose overlay trend is negative carry a small white ↓ beside their letter, letter-zoom gated: `declining` re-enters the bake (`mapData.ts` — true ONLY where a letter rides; closed/NEW/unscored/basic-map stay mute), drawn by `LYR_POINT_DECLINE`. An IMAGE layer, not text: probed 2026-08-31, CARTO's glyph endpoint serves no arrow codepoint in ANY fontstack (U+2193 and the geometric triangles absent from Montserrat/Open Sans/Noto PBF ranges — a text suffix renders tofu), so `installDeclineImage` draws one 5×7 white arrow, re-registered per style swap like all images. `icon-offset` [7,0] keeps the mark inside the fill down to LETTER_ZOOM (dot radius 9.4px there) — white on the basemap would vanish on positron. Scale of the signal: 493 declining graded rows in bucket 00 alone. §6.2 markers row updated; the map-data suite pins the bake (suffix never rides alone; stacks stay judgment-free). |
| 2026-08-30 | **Preview UX pass #4 — the ack-dialog flash fixed + always-on trend labels (Cannon's calls).** (1) Every load flashed the terms at DECIDED visitors: the data provider seeded `roster` as `'awaiting-ack'` unconditionally and only flipped it in a post-paint effect — one painted frame of the blocking dialog. The state now initializes from the decision (`awaiting-ack` only when genuinely undecided outside `?tier=lite`); C2 semantics untouched. (2) The trend instrument prints **every claiming mark's label above itself, always** — score, bare X/Y ratio (" OUT" dropped), ✓-glyph — the old sparkline's grammar in the new style; the panel geometry gained 18px of top headroom (bandTop 8→26, span preserved, every pinned pixel +18) so a 100's label + 1.7× hover enlargement stay in frame; labels ride their mark's `.tp` group and dim in the hover sweep; the hover-only `.tr` readout and the below-floor side label retired. §6.0 scale/grammar/hover rows updated; trend suite re-pinned at the shifted pixels. |
| 2026-08-30 | **Preview UX pass #3 — the hover card's anchor boxes cut (Cannon's annotated-screenshot call).** The "Last broad inspection · Last visit" row leaves the hover: the hero already carries the broad date, and the borrowed trend's endpoint dates carry the span — the boxes were saying it twice. Head → hero → trend section is the whole card now. §6.2 hover row updated; hover suite re-pinned (negative pins on the box labels). |
| 2026-08-30 | **Preview UX pass #2 — clusters withdrawn + the declining ring scrapped; the hover card stays (Cannon's live review of #174).** The bubbling came off after a real look at next.cleanplateva.com — the CRD-M1 no-clusters call is thereby re-ratified BY LIVE TRIAL, not just mockup instinct (source clustering, both cluster layers, the accumulator inputs, and the cluster hit-test tier all reverted; #174 holds the machinery verbatim if it ever returns). The CRV dashed declining ring came off outright — `LYR_DECLINING`, the prerendered ring images, and the `declining` marker property are gone, and **no declining indicator ships until Cannon picks a form** (the derivation `facilityPresentation().declining` and the panel's ↘ trend chip are untouched). The pass-1 hover card re-lay (old zones + the shared panel `TrendSection`) is the surviving keeper. §6.2 markers row re-flipped. **176 Vitest green** (the cluster/ring pins retired with their features). |
| 2026-08-30 | **Preview UX pass #1 — clusters revived + the hover card re-laid (Cannon's review on the dual track).** Proximity clusters return (supersedes CRD-M1's no-clusters mockup rule; §6.2 markers row flipped): MapLibre source clustering ported verbatim from the old client — radius 40, max zoom 12, `sum`/`gradeSum`/`gradeCount` accumulators on EVERY feature so a bubble counts PLACES (not points) and wears the **mean grade of the live scored places inside** (closed excluded; lite keeps the neutral density ramp); hand-abbreviated count labels; hover = cursor only; click = `getClusterExpansionZoom`+0.5. The CRV zoom-scaled dots/stacks ride beneath unchanged (stacks stay neutral); the hit test gains the cluster tier (stacks > dots > clusters, radius by place-count step). The hover card re-laid to the old client's zones (§6.2 hover row flipped): bordered head · grade hero (circle + score/100 + broad date; old NEW/no-grade variants) · anchor boxes · **the panel's trend section borrowed wholesale via the extracted shared `TrendSection`** (one module, two mounts — hover and panel cannot drift; §6.0 line updated), popup back at the old 380 width. Verified on next.cleanplateva.com: the state→metro→dissolve cluster field with real tint texture (C-orange pockets at Tuckahoe/Glen Allen over an A/B field), click-drilling, and the full card (TREND header, band labels, endpoint dates, legend) over live data. **179 Vitest green** (new accumulator pins; hover suite re-pinned). Ops lesson for preview review: a hidden Browser-pane tab freezes rAF — MapLibre neither paints nor binds hover until a visible (re)load; verify with the pane fronted. |
| 2026-08-30 | **The dual-track preview — the React client live at `next.cleanplateva.com` (D-CR-STAGE-1 re-resolved by Cannon).** With CRV complete, Cannon called for both clients live side by side pre-flip; the second-Worker option STAGE-1 had declined at CRD-M0 became the call. `wrangler.preview.jsonc`: `cleanplateva-preview`, `assets.directory ./dist`, same Worker/R2/`run_worker_first`/SPA fallback, manual deploys only — production untouched (verified: `cleanplateva.com` still boots `static/js/app.js`). Two pre-flip gaps closed en route: **(1) D-CR-EMBED-1's build gap** — `base: './'` + the old shell's `<base href="/">` in the Vite entry; built assets now `./assets/*` (router already read `document.baseURI`; the data client's paths were already relative). **(2) The maplibre worker defect, found by the preview host and impossible to see on the dev server:** maplibre-gl v6 resolves `maplibre-gl-worker.mjs` (+ its import `maplibre-gl-shared.mjs`) against its chunk's URL at runtime — bundler-invisible, so `dist/` never carried them and the SPA fallback answered the worker fetch with HTML (module-MIME error, map never loads; proven pre-existing by a pristine-config rebuild — the CRC flip would have shipped it). Fixed with `maplibreWorkerCopy` (§3), pinned by two new dist-contract tests — **177 Vitest green**. Host receipts on the preview: C2 boot shapes (dialog-gated; agree → 1+16+16), C5 byte-identity vs git-stored bytes (manifest + shard sha256), miss path → shell at `max-age=0`, R2 channel MISS→HIT with the contract Cache-Control re-asserted, `/list` deep link → shell, all three views live. CRC-M0's remaining items: the deletion list enumerated exactly, `test:node`'s post-flip shape, the §2 walk as a checklist. |
| 2026-08-30 | **CRVb-M2 — the §6.1 ack dialog; CRV-b complete.** `app/AckDialog.tsx` to the ratified anatomy: the title alone · the terms as ONE verbatim document in an inset recessed scroll box with a persistent scrollbar · Decline left (filled red) / Agree right (filled blue), the shipped labels (filled for the dialog's fork, §6.0). **The single source went component-shaped**: `app/TermsBody.tsx` is Cannon's verbatim terms once, mounted by BOTH About §06's `#aboutTermsBody` and the dialog — the React form of the old DOM clone (About isn't in the DOM on a first load at `/`, so a literal clone was impossible; one module, two mounts, drift impossible). Blocking first load renders the GhostShell (brand + search, inert) over the empty basemap — **verified live: zero data-family fetches undecided** (the 4 `/data` hits were the app's own `/app/data/*.ts` module files); Escape = unpersisted decline (verified: nothing stored, the very next load asked again); Agree/Decline persist and re-run the load (16 overlays on agree). Re-opened from About §06's declined-side action it is an ordinary dialog (✕/backdrop/Escape close) — **the M1 interim is gone**; a changed answer closes an open panel before the tier flips (the old `_decideAck` discipline). Mobile <sm: bottom sheet, stacked full-width actions, Agree on top (verified at 375px). **The proof Agree/Decline strip is DELETED — the last CRF scaffolding retires**; the App smoke now pins the dialog (title alone, label order, no third-choice affordance, C2 zero-fetch). 175 Vitest + 290 legacy green. **CRV-b is complete; CRC (the cutover) teed up as its own briefing — the flip gets a fresh session.** |
| 2026-08-30 | **CRVb-M1 — About at full content parity.** `app/AboutView.tsx` per §6.4: the hero (three-badge provenance legend · the LIVE page's H1 · lede · the ratified four live cards — snapshot from `fetched_at`, newest report, **Places = `counts.active`** (the full manifest's `total` includes the closed supplement — caught live showing 28,087, pinned), coverage ZIPs excluding '?'), §01 whole (weight board with the live family sublines, BOTH worked receipts at the live line composition incl. the 2×2×0.75 COS pair, scope cards, the grade-scale bar in the ramp with the 83·B marker, adjustment cards, the full six-item edge-cases disclosure incl. the hedged-comments sentence), §02 signals + the exact red-flag weights, §03 pipeline (**C8: the step-6 channel chips say "basic map", never the live page's "static lite"** — the one deliberate copy change; "CouchDB" kept, parity), §04 lineage + tiers + the current-access live card (ported `basicMapReason` states), §05 all eight limits at the live full text + the verify card on the real portal URL, **§06 the verbatim single-source terms** (Cannon's words, typographic quotes intact, the real attribution hrefs — `#aboutTermsBody` is the element M2's dialog clones) + the status panel OUTSIDE it owning the one tier switch — **verified live both ways** (agreed→basic: 17 static fetches; declined→grades: 16 overlays; C2 re-load semantics). `/about#aboutTerms` cold-load + the footer link both land §06 scrolled with the title focused (the hook captures the hash before normalization). INTERIM flagged: the declined-side "Review the terms and view grades" agrees outright until M2's dialog (proof-strip parity; production untouched). Also landed: **Cannon's graded-worst-first call** — rows missing the sorted value go LAST on every value sort, both directions (the −1-sentinel parity note from M0 is superseded); and the title-accumulation fix (per-mount base-title capture now strips stale view prefixes — dev HMR compounded "List · List · …"). 172 Vitest + 290 legacy green. |
| 2026-08-30 | **CRVb-M0 — the List.** `app/ListView.tsx` per §6.3, fired same-session on Cannon's explicit authorization. The List is a scrolling DOCUMENT: the band rides docked in the flow (`Toolbar` gained the `docked` variant; the map keeps the floating fixed band), the table's own headers stick, and the attribution line ends the page in-flow (the map keeps the scrim chip — `Attribution` extracted with both variants). Cells per the ratified mockup: grade chip + score (NEW pill / — for ungraded) · name over address·city·zip with the blue-dot NEW word-badge and the muted CLOSED word-badge · compliance % · trend as colored arrow + signed delta (em-dash null) · last visit as ISO date + scope/outcome (`· broad` / `· 2/9 OUT` — focused rows never show a raw score). Sort ports `list.js` verbatim — all seven C6 keys with their null sentinels (score/compliance −1 · trend 0 · date ''), decorated so the derivation runs once per row instead of once per comparison; header clicks toggle-or-asc and reset page; a stale URL page clamps at render. Load-more per D-DATA-11 with the mockup's label ("Show 50 more · N match"), focus kept on the first new row. Row click opens the shared detail panel (verified live: `permit` joins `sort`/`dir`/`page` in the URL). Basic map = the Name column alone (P6, verified). Compliance/Trend drop below md. Deliberate parity note: score-asc keeps the old −1 sentinel, so ungraded/NEW rows lead the worst-first list exactly as the live site does. 163 Vitest + 290 legacy green. |
| 2026-08-30 | **CRVa-M2 — detail panel + receipt; CRV-a complete.** `receipt.ts` ported whole to TS strict (`app/data/receipt.ts` — the cf_lib mirror in integer ten-thousandths, per-finding docket/journeys, code-join normalization, the reconcile-or-degrade discipline) with the §7 pure-derivation suites (see §7 port status). The §6.2 panel (`app/DetailPanel.tsx` + `InspectionRow.tsx` + `ReceiptModal.tsx`): right sheet / bottom sheet (verified at 375px), header + Source in the external grammar, the structural fact line with ≈ as the one badge, the grade hero as ONE tappable box opening the report-card modal (NEW / no-grade variants), the shared instrument as the labeled trend section (panel variant, hover live), history rows at the full `inspection.js` inventory (outcome badges incl. adjudicated ✓-chips, count chips, ↳ corrective lines, grouped checklist w/ pass bars, Inspector comments, temps_v2 + legacy temps). The old panel's red-flags section is NOT ported — §6.2's Must-show doesn't carry it (flags live in About §02 as Heuristic). Detail on click only + standards once — measured live: first click exactly 2 requests, further facility +1, LRU return 0 (C3); `permit` pushes (C6), panel close = replaceState clear, Back walks selections then closes. **Stack click = member-list popover** (`StackPopover.tsx`, Cannon's M1-boundary call — the old spiderfy's surviving half): name-sorted members w/ ramp chips, pick → panel; outside-click/Escape/again-click dismiss. Lite panel keeps the identity + official-VDH hand-off (P6/C8). 155 Vitest + 290 legacy green. **CRV-b (List · About · §6.1 dialog) teed up; the CRV-a briefing archives.** |
| 2026-08-30 | **CRVa-M1 — hover card + the trend instrument.** The §6.0 instrument built ONCE (`app/trend.ts` pure geometry + `app/TrendInstrument.tsx`): absolute rest-100→55 scale with extend-on-demand (floor steps to a 5-multiple strictly below the minimum claimed height; pinned against `trend-compare.html`'s own pixels — 85→y32, 29→floor 25/y76.2, hairlines 17.6/27.2/36.8/46.4), never min/max-normalized, below-floor score printed beside the mark; threshold hairlines w/ dashed-60, A·C·F band labels, endpoint dates; full `sparkline.js` mark grammar (broad line+dots · hollow focused diamonds at COMPLIANCE height, raw focused scores never plot · filled verdict diamonds, all-corrected on the 100 line · baseline ticks · scoreless-broad x-slots); hover enlarge-1.7×/dim-30% + per-mark readout as CSS (`.cp-trend--interactive`, the mockups' own split: panel interactive, card static). The §6.2 hover card (`app/HoverCard.tsx` in a mouse-transparent themed MapLibre popup): identity + grade circle / NEW / no-grade, the two anchor boxes speaking `trendClaim`'s own readouts, the compact instrument — **verified 0 fetches on hover live**; lite keeps the slim tip (P6); pointer resolution ported (`app/mapHit.ts` — slop-padded, inside-wins-by-paint-order, nearest-edge outside, radii mirroring the zoom-scaled layer expressions). MapLibre v6's dynamic anchor verified handling the right edge (the old `fitAnchor` machinery not ported — its splitter/stack-panel world is gone). Theme control per Cannon's M0 review: a real SWITCH bottom-left (`app/ThemeSwitch.tsx`), off the band. Found: unlayered vendor CSS beats `@layer` — popup chrome must live unlayered. 100 Vitest + 290 legacy green. |
| 2026-08-30 | **CRVa-M0 — shell + markers.** The map surface's chrome and marker grammar built to §6.2 on the CRF foundation. The router HOOK (`app/useAppRouter.ts` over the CRF pure functions + an `app/store.ts` reducer): URL-wins on boot/popstate, non-default-only writes vs fresh-read persisted toggles (persist-first so the key drops out), pushState on view/facility change, replaceState for filter churn and in-frame, `/map`→`/` + `?zip=` + `#about` normalization, titles both directions — re-pinned under Vitest (`tests/vitest/router-hook.spec.tsx`). The ONE band (`app/Toolbar.tsx`): brand · view switcher (List/About stubs) · debounced search pill · single-select A–F chips on the frozen `grade` key · Filters popover (restaurants/new/mobile) · Show closed pill (first toggle pulls the lazy closed family) · counts; basic map hides the judgment controls (`toolbar.spec.tsx`). Markers (`app/MapView.tsx` + pure `app/mapData.ts`, ProofMap deleted): ramp dots with rings both bases, letters past z13.5, dashed declining ring in the marker's OWN grade color (mockup), NEW blue letterless, closed gray+dim, **neutral** count-bubble stacks riding the dots' zoom curve (no proximity clusters — their low-zoom restraint re-created by scaling; counts from z12), prerendered dashed-ring images, nav/geolocate bottom-right, patient auto-locate + coverage note ported from `map.js` (`map-data.spec.ts`). Theme: persisted `cleanplateva.theme`, pre-paint script, style-swap reinstall; a PROVISIONAL theme icon rides the band's tail — placement is Cannon's call at review (§6.2 names no theme control). Verified live on :5620: undecided 0 fetches · lite 1+16 · agree 1+16+16 · closed +16 lazy; letters/rings/stacks at z14 both themes. 82 Vitest + 290 legacy green. |
| 2026-08-30 | **CRF-M1 — the V4 client on the new stack.** The data layer ported to TS strict per §5: `app/data/client.ts` (`createFoodApi` whole — manifest-led boot, static-first finder w/ R2 fallback, sha binding, position merge, closed/standards/detail lazy paths, LRU 200, HTML-200-is-a-miss, P4 degrade, call-time ack gate), `app/data/presentation.ts` (grade/scope/trend/date math + `visitsOf`; HTML emitters dropped — React owns markup; `receipt.ts` deferred to CRV-a with its suite), `app/ack.ts` (versioned key, `createAckState`, `forceLiteFromSearch`), `app/router.ts` (the §4 pure functions), `app/search.ts` (terms/phrase/ZIP-prefix + filter predicate), types from v4 §6.2 (`app/data/types.ts` — vocab codes typed open, never closed unions). Six §7 contract suites ported to Vitest (64 tests; legacy 290 stay green). **Proof boot verified on the dev server** (`app/App.tsx` + `ProofMap.tsx` + a §5 provider): undecided → zero data fetches (C2); decline → gray basic map, 1+16 lite requests exactly; agree → full boot with finder off the static channel + 16 `/data-full/` overlays, 25,164 dots in the ratified ramp; `?tier=lite` → basic map, controls hidden (D-ACK-3). Found + fixed: Vite 8/Rolldown's dep optimizer breaks maplibre-gl's worker silently (`optimizeDeps.exclude`, dev-only). Bundle 309 KB gzip (reported, no gate — under the old client's MapLibre-alone wire). Zero view UI — CRV-a next. |
