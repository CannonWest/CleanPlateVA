__pycache__/
*.pyc
.env
.venv/
venv/
*.egg-info/

# Published data snapshots — generated by the data pipeline, not committed
# (yet); the deployed site gets them from a pipeline publish step.
public/data/
